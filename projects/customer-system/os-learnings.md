# OS Learnings — Running Register

This is the running register of every OS-relevant finding surfaced by customer-system work. Bugs, missing capabilities, design gaps, ergonomics issues, and architectural decisions that need to flow back into the Indemn OS itself.

**The principle:** building the customer system is shipping the OS. Every gap is information the OS needs. This file is where we track that flow.

**How to use this file:**
- **Scan it** at the start of any session that might touch OS internals — see what's queued, what's blocking, what's been resolved.
- **Append to it** when customer-system work surfaces something OS-relevant. Don't bury findings in trace artifacts — surface them here.
- **Update status** when OS work moves a finding from queued → in-progress → fixed.
- **Link back** to source artifacts where the finding was first articulated, and forward to OS-side artifacts (in `../product-vision/` or in the OS repo) when the design discussion or implementation lands there.
- **Don't delete entries** when fixed — mark fixed and leave the trail. The history is valuable.

**Status legend:**
- 🔴 Open — not yet addressed
- 🟡 In-progress — actively being worked
- 🟢 Fixed — fix is in main / deployed
- ⚪ Won't fix / by design — with rationale

**Source-of-truth detail** for each finding lives in the linked artifact. This file is the index. Bugs file (`artifacts/2026-04-24-os-bugs-and-shakeout.md`) carries deeper bug detail; this file aggregates bugs + capability gaps + design questions in one place ranked by what they unblock.

---

## What's queued for the OS — by impact on customer-system

### Foundation milestones — vision §2 capabilities now live

| Item | Status | Source | OS-side artifact / commit |
|---|---|---|---|
| **LangSmith tracing on async-deepagents harness** — vision §2 item 7 ("LangSmith on all harnesses for AI observability"). Every associate run on `harnesses/async-deepagents/` emits a full LangSmith trace with the LLM reasoning chain, every `execute` tool call, every middleware step (Skills/Filesystem/SubAgent/Summarization/AnthropicPromptCaching/TodoList), and the entity context the agent was given. **This was the diagnostic layer that let us flip Session 9's "skill-compliance gap" hypothesis into "kernel CLI bug" in 5 minutes** — see Diana@CKSpecialty traces under Bug #16 / #34 below. | 🟢 Live | 2026-04-28 (Session 10) | Project `indemn-os-associates` (id `19302981-aa9e-4869-9137-59a38d7646df`). API key at AWS Secrets `indemn/dev/shared/langsmith-api-key`. Wired in commit `956d7d5` on main: `harnesses/async-deepagents/{main.py + pyproject.toml + uv.lock}`. RunnableConfig dict on `agent.ainvoke()` carries metadata `{associate_id, associate_name, message_id, entity_type, entity_id, runtime_id, correlation_id}` + tags `{associate:<name>, entity_type:<type>, runtime:<id>}` + a human-readable `run_name`. Auto-tracing of LLM/tool/state-graph calls happens because deepagents is LangGraph-based — env vars are sufficient. **How to query traces: see `CLAUDE.md` "When working on X → Debugging an associate's behavior (LangSmith tracing)".** Reference traces from Session 10: `019dd522-ac39-7fc2-bcab-dacdc450cfc5` (CLI broken; agent fell through to create), `019dd589-8d80-7aa3-93af-b59aff572184` (CLI fixed; agent tried email-create instead of update), `019dd5c1-3aab-7ea3-aa08-d007044570a1` (EC v6 + CLI fix; clean classification end-to-end). |

### Critical / blocking the next phase

| Item | Status | Source | OS-side artifact / commit |
|---|---|---|---|
| **Bug #45 — Slack live `fetch_new` returns "No messaging integration available" despite an active Integration entity matching system_type=messaging** — surfaced 2026-04-30 Session 14 close, after building + deploying the Slack adapter. State at observation: Slack Integration entity exists at id `69f3bb5097300b115e7236dd`, owner_type=org, owner_id=`69e23d586a448759a34d3823`, system_type=`messaging`, provider=`slack`, status=`active` (walked through state machine `configured → connected → active`), secret_ref=`indemn/dev/integrations/slack-oauth`. Bot token verified retrievable from AWS Secrets (key `bot_token`, len=57). SlackAdapter registered in kernel as `register_adapter("messaging", "slack", SlackAdapter)` (kernel/integration/adapters/slack.py + __init__.py auto-import) and live-deployed (commit `c87376d`). fetch_new capability activated on SlackMessage with config `{"system_type":"messaging"}`. `POST /api/slackmessages/fetch-new` returns HTTP 500 with body `{"error":"InternalServerError","type":"AdapterNotFoundError","message":"No messaging integration available. Create one with: indemn integration create --system-type messaging ..."}`. **Hypothesis space (resolve in next session):** (a) `kernel/integration/dispatch.py::resolve_integration()` filters on a status enum that does not include `active` (e.g. expects `connected`, or both); (b) the kernel adapter registry keys on `(system_type, provider)` tuple but the API caller only passes `system_type` and the resolver doesn't know to look up by provider; (c) org_id scoping mismatch between auth context and Integration owner_id (note the Integration has owner_id=`69e23d586a448759a34d3823` which is the same as runtime-async-service-token's org); (d) some required `access_roles` field that wasn't set at creation. **Suggested investigation:** read `kernel/integration/dispatch.py::resolve_integration()` start to finish; trace what it queries; compare to the live Integration record. Verification: once resolved, `POST /api/slackmessages/fetch-new` with `{"limit": 5}` should return `{fetched, created, skipped_duplicates, errors}` shape (similar to Drive's working call). | 🟢 Fixed (both layers) | 2026-04-30 Session 14: state captured in CURRENT.md Blocker #1; symptom reproduced live. **2026-05-01 Session 15: dual root cause confirmed** — hypothesis (b) AND hypothesis (d) both true. | **2026-05-01 Session 15 — two-layer fix:** **(45a, data) `access: null` on the Slack Integration → resolver Step 3 query (`access.roles: {$in: role_names}`) cannot match a null path → `AdapterNotFoundError` raised.** Compared live to working Google Workspace org-level integration (`access: {"roles": ["team_member", "platform_admin"]}`); patched Slack Integration to identical shape via `indemn integration update 69f3bb5097300b115e7236dd --data '{"access": {"roles": ["team_member", "platform_admin"]}}'`. Resolver Step 3 then succeeded, surfacing the second bug below. **(45b, code) Slack adapter's `register_adapter("messaging", "slack", SlackAdapter)` passed (system_type, provider) instead of (provider, version) — registered at registry key `messaging:slack` instead of `slack:v1`.** Compared to Google Workspace's correct `register_adapter("google", "v1", GoogleWorkspaceAdapter)`. Fix: one-line change at `kernel/integration/adapters/slack.py:237` to `register_adapter("slack", "v1", SlackAdapter)`. New unit test `TestSlackAdapterRegistration::test_registers_under_slack_v1` pins the registry key against drift. Indemn-os main commit `eea170d` (`fix(adapters): register Slack adapter under (slack, v1) not (messaging, slack) — Bug #45b`); 438 unit tests pass, deployed via `railway up --service indemn-api`. **Verified live 2026-05-01 12:49Z:** `indemn slackmessage fetch-new --data '{"limit": 1}'` returns `{fetched: 0, created: 0, skipped_duplicates: 0, errors: [], created_ids: []}` — same wire shape as Drive. 0 fetched expected (bot not yet in any channels). **Follow-up work in Session 15:** rotate Slack bot token (was pasted in plaintext in Session 14) — deferred per Craig (existing token kept active; rotation as low-priority follow-up); Craig invites `@indemn_os` bot to relevant channels; 90-day backfill; `slack_fetcher` role + `slack-fetcher` skill + Slack-Fetcher actor build + activation closes TD-1 sub-piece 4. **Related design question logged below in "Open design questions" — should resolver treat null/missing `access` field as "no role gate" for org-level integrations? Today's foot-gun: silent unreachability + misleading error message ("No <system> integration available" when one exists but is mis-configured).** **2026-05-01 Session 16 follow-on observation:** during Bug #40 cron_runner verification, found Slack Integration had auto-transitioned `active → error` at 2026-05-01 18:53:00Z (before Session 16 started). Walking the state machine `error → configured → connected → active` restored it. Suspected cause: Bug #48's repeated 404s on `/api/slackmessages/` triggered some kernel-side error-state handler. Since the 20:26Z reset + Bug #48 fix, the integration has stayed `active` through 769+ Slack-Fetcher cron_runner completions over 3 days with zero re-transitions to error — so this was a one-off, NOT a recurring auto-error loop. **No guard added** — couldn't find the specific transition_to(error) callsite in `kernel/integration/`; the transitions visible in the changes collection at 18:53:00Z don't carry an effective_actor_id, suggesting an internal kernel handler or operator-side script. Worth investigating later if it recurs; for now monitor and re-fix on detection (same approach as Bug #12 regression). |
| **Bug #42 — Gemini 2.5 Flash returns `MALFORMED_FUNCTION_CALL` on the deepagents `write_todos` schema (NOT a skill-content issue, NOT agent compliance)** — Session 14 reframing. Original Bug #41-verification framing was "agent reads skill, doesn't act" with hypothesis space (a) sparse trigger descriptor / (b) DEFAULT_PROMPT tuning / (c) skill-prose-imperative / (d) turn budget. **Actual root cause** (LangSmith traces `019ddf6c-b2e9-7f82-873b-d992165becf2`, `019ddf6c-eec0-7dc1-a08c-d391d5d3d950`, `019ddf6d-2d33-7cf3-8709-6962b726de2c` — 100% repro): the agent IS trying to act per DEFAULT_PROMPT step 3 ("Use the todo tool to plan every step") and generates a `write_todos` call. Gemini 2.5 Flash emits Pythonic compositional-tool syntax — `print(default_api.write_todos(todos=[default_api.WriteTodosTodos(content="...", status="in_progress"), ...]))` — instead of valid JSON tool calls. Vertex API rejects with `finish_reason: MALFORMED_FUNCTION_CALL`, candidates_token_count=0, no `tool_calls` emitted. Agent has nothing to act on, terminates. `agent_did_useful_work()` correctly observes no state-changing CLI call → message dead_letter. **Why turn 1 worked but turn 2 failed:** turn 1 calls `execute(command="...")` (flat schema, single string arg) — Gemini 2.5 Flash handles. Turn 2 calls `write_todos(todos=[{content,status}, ...])` (array of typed objects) — schema shape that triggers Pythonic mode in 2.5 Flash. Hypothesis (c) skill-imperative-rewrite would NOT have helped — failure is at the LLM tool-call serialization layer, downstream of reasoning. **LangSmith query gotcha discovered while diagnosing — worth noting for CLAUDE.md § 8:** the `/api/v1/runs/query` endpoint requires `order: "desc"` (lowercase). `order: "DESC"` (uppercase, common SQL convention) returns HTTP 422 with body `{"detail":["body.order: Input should be 'asc' or 'desc'"]}`, which silently parses to 0 results in scripts that read the `runs` field. The example query in CLAUDE.md § 8 happens not to include `order` so it works; explicit ordering must be lowercase. **The fork's earlier "0 traces visible" symptom in Bug #41 verification almost certainly tripped on this — traces have been arriving the entire time post-LangSmith-wiring, the wiring is fine.** No infrastructure issue with LangSmith. **Fix:** `gemini-3-flash-preview` on `location: global` handles the `write_todos` schema correctly. Per Craig: Gemini stays — Claude is off the table. Bug #40's deterministic-execution path (no LLM at all for trivial cron-fired CLI runs) remains the architecturally cleanest long-term fix; Bug #42's resolution is the LLM-as-CLI-shell path with a more capable model. | 🟢 Fixed (per-actor override) — runtime-default change deferred to Craig | 2026-04-30 Session 14 trace investigation. 3 traces showing `default_api.WriteTodosTodos(...)` Pythonic syntax + `finish_reason: MALFORMED_FUNCTION_CALL` confirm root cause. Bug #41-era runtime-async logs (deploy `26038273-…`) showing "Agent completed: 4 messages, tools=['execute']" were correct observations of the symptom but the LangSmith query bug + lack of trace-reading meant the actual finish_reason was hidden. | 2026-04-30 Session 14: Email-Fetcher (id `69f2bf30942e5629f07a8313`) `llm_config` set to `{"model":"google_vertexai:gemini-3-flash-preview","location":"global"}` via `PUT /api/actors/...`. **Live-verified end-to-end 18:10:31-18:15:56Z** (LangSmith trace `019ddf95-d579-7390-9483-beece987389f`, status=success): 7 LLM turns, ALL `finish=STOP`, no MALFORMED anywhere. Agent loaded operating-skill + entity-skills (chained via shell `&&` — sophisticated tool use), called `write_todos` with valid JSON tool call, then `execute('indemn email fetch-new')` (retried with timeout=600 when first call ran long — autonomous error recovery). **Net: 326 new emails fetched** (1645→1971 total; today's count 514→840). **Per-actor scope only — runtime default `gemini-2.5-flash` unchanged:** Meeting-Fetcher / Drive-Fetcher / Slack-Fetcher / EC / TS / IE will all hit Bug #42 when activated unless they get the same per-actor override OR the runtime default flips. **Decision pending Craig:** (a) flip `runtime async-deepagents-dev` default to `gemini-3-flash-preview` (cleaner; affects 23+ actors including reasoning-mode IE; preview-tier so quota/cost may differ); or (b) per-actor override on each (more granular, more work, more flexibility). **Note on Gemini 3 Flash Preview tier:** `preview` means subject-to-change quota/cost/availability; worth tracking when 3 Flash GA's. **Note on doc:** CLAUDE.md § 8's LangSmith debugging recipe should add a one-liner about lowercase `order`. |
| **Bug #41 — Async-deepagents harness lacks `_scheduled` entity handling; fetcher actors fail at agent-side entity-load** — surfaced 2026-04-30 Bug #38 fork verification. Bug #38's kernel-side dispatch fix correctly routes scheduled-cron messages all the way through Temporal claim_message + load_actor + process_with_associate dispatch to the harness queue. The harness's `process_with_associate` activity then does `context = indemn(entity_slug, "get", input.entity_id, "--depth", "2", "--include-related")` (`/app/harness/main.py:69`). For `_scheduled` messages, `entity_slug = "_scheduled"` — there is no `indemn _scheduled` CLI command (it's a kernel-internal entity_type for cron-fired messages). CLI exits with `Error: No such command '_scheduled'`; the activity raises `harness_common.cli.CLIError`; workflow fails after retries; message goes dead_letter. **Effective impact:** even with Bug #38 fixed, scheduled fetcher actors (Email-Fetcher, future Meeting-Fetcher, Drive-Fetcher per TD-1) cannot actually run. Each cron tick succeeds at "the kernel created a message and dispatched a workflow" but fails at "the agent actually ran the fetch." TD-1 still blocked on this. **Suggested fix shape:** the harness's `process_with_associate` activity should special-case `entity_type == "_scheduled"` — skip the entity-load step entirely (no entity to load), pass the message + actor to the agent with empty `_related` context. The agent's job is to run its skill (e.g., "execute `indemn email fetch-new`"), not to operate on an entity. Alternative (cleaner): a separate `ScheduledActorWorkflow` peer to `ProcessMessageWorkflow` that doesn't try to enrich-with-entity; routes to a different harness activity (e.g., `run_scheduled_skill`) that builds a minimal agent context. Tests should pin: harness handles `entity_type=_scheduled` without calling `indemn _scheduled get`; an Email-Fetcher cron tick produces a clean LangSmith trace ending in `indemn email fetch-new` execution. **Related to Bug #40** (no deterministic scheduled-actor execution path) — Bug #40 was the architectural gap; Bug #41 is the specific harness-side manifestation. Fixing Bug #41 with the special-case approach is faster; fixing it via a new ScheduledActorWorkflow + run_scheduled_skill activity is the architecturally aligned approach (matches Bug #40's "deterministic execution path" intent + opens the door to deterministic-only scheduled actors with no LLM). | 🟢 Fixed (harness layer) — agent-compliance issue surfaced as Bug #42 | 2026-04-30 Bug #38 fork verification. Runtime-async logs show `harness_common.cli.CLIError: CLI failed (2): No such command '_scheduled'` at `/app/harness/main.py:69` (process_with_associate activity, line invoking `indemn(entity_slug, "get", ...)` for the message's entity_type). Reproducible: activate any associate with `trigger_schedule` set, wait for cron tick. | 2026-04-30 Bug #41 fork session: indemn-os main commit `96684d5` (`fix(harness): handle synthetic kernel-internal entity_types (Bug #41)`). **Framing-B fix** per the design discussion (vs. framing-A: `entity_type="Actor"` in kernel sweep — would conflate "the agent's focus" with "the agent itself"; vs. framing-C: separate `ScheduledActorWorkflow` — bigger refactor, deferred): extracted `_load_message_context(entity_type, entity_id, associate)` helper in `harnesses/async-deepagents/main.py`. Branches on `entity_type.startswith("_")` — synthetic kernel-internal types skip the CLI load and return a trigger descriptor (`{_synthetic, trigger, trigger_entity_id, associate_name, associate_id, trigger_schedule}`). Watch-driven path unchanged. Generalizes to any future `_*` sentinel (e.g., `_circuit_broken`, `_zombie_recovery`) — no future code changes here. **Tests:** new `harnesses/async-deepagents/tests/test_load_message_context.py` (6 tests) — synthetic types skip CLI, trigger descriptor carries actor identity + schedule, generalizes to any `_*` prefix, watch-driven path preserved with correct CLI args + lowercase slug, helper is sync. Full unit suite: 459 passed (the 1 pre-existing failure on `test_effective_actor_id::test_harness_cli_wrapper_propagates_effective_actor_to_subprocess` is pre-existing test-pollution from `test_agent.py`'s module-level sys.modules.setdefault stubbing `harness_common.cli` — orthogonal to this fix). **Verified live 2026-04-30 17:22-17:27Z:** Email-Fetcher reactivated; cron at 17:25:00Z fired fresh `_scheduled` message id `69f390726c8a8cebec6e58d1`; workflow dispatched cleanly (no CLIError this time); `process_with_associate` activity ran successfully with `_load_message_context` returning the trigger descriptor; agent loaded its skill via `execute('indemn skill get email-fetcher')` (runtime-async logs confirm the full skill content was returned). **Harness layer of Bug #41 verified end-to-end.** **Downstream issue (Bug #42, NEW):** agent reads the skill but does not make the follow-up CLI call to actually run `indemn email fetch-new`. `agent_did_useful_work()` correctly identifies no state-changing call; message marked failed → dead_letter. That's an agent-compliance / skill-content / prompt-tuning concern, NOT a harness-code bug — separate fork or skill iteration. **LangSmith trace gap also surfaced** during verification: 0 root traces visible in the past 15min despite the agent run actually executing (commit `956d7d5` LangSmith wiring may need review post-skills-via-CLI refactor in `7281b83`). Logged as separate concern. Email-Fetcher re-suspended pending main session resumption of TD-1 + Bug #42 resolution. |
| **Bug #38 — Runtime task queue jammed: stuck Temporal workflows + uncaught `WorkflowAlreadyStartedError` + stale messages targeting suspended actors** — surfaced 2026-04-30 Session 14 attempting TD-1 Email-Fetcher activation. **Symptom:** queue-processor logs spam `[WARN] Failed to dispatch workflow <msg-id>: Workflow execution already started` continuously across 100s of stale message workflow_ids dating back 3+ days. LangSmith confirms ZERO root agent traces in past 24h despite IE listed as `active`. New `_scheduled` messages from a freshly-activated scheduled associate (Email-Fetcher, cron `*/5 * * * *`) sit `pending` indefinitely — never claimed, never dispatched. **Three coupled root causes:** (1) `kernel/queue_processor.py:dispatch_associate_workflows:124-150` catches `RPCError` with `RPCStatusCode.ALREADY_EXISTS`, but `temporalio.client.start_workflow` raises `WorkflowAlreadyStartedError` directly (a separate exception class, NOT a subclass of `RPCError`). The `except RPCError` branch never fires for the actual exception thrown; falls through to the generic `except Exception` log warning. Per os-learnings.md historical note: "Pre-Apr 24 infrastructure fixes: Queue processor `WorkflowAlreadyStartedError` removed from temporalio SDK (replaced with `RPCError` + `RPCStatusCode.ALREADY_EXISTS`)" — but the temporalio SDK currently re-raises `WorkflowAlreadyStartedError` from `client.start_workflow`, so the prior fix's assumption no longer holds. (2) Pending messages are never marked terminal when their workflow_id has been started before. Workflow_id is deterministic (`f"msg-{message.id}"`) — if a prior `ProcessMessageWorkflow` for that message was started but ended in failure (e.g., process_with_associate failure-after-retries), Temporal still has it registered. Every dispatch sweep tries again, fails, logs, repeats — perpetual log spam, no progress. (3) Watch-driven message creation does not check whether the target role has any *active* associates. EC is `suspended`; new Emails created (e.g., the 514 from a fetch run) still emit `Email created` events that match EC's watch → 514 ProcessMessageWorkflows queued targeting a suspended actor. Each fails repeatedly. As of 2026-04-30 02:37Z: 1015+ pending email_classifier messages, 28 pending touchpoint_synthesizer messages, all targeting suspended actors. Compounded by Bug #38 root #1, the runtime task queue saturates with retry attempts that can never succeed. **Effective impact:** TD-1 actor activation is blocked. Email-Fetcher's `_scheduled` messages queue but cannot dispatch because the runtime worker (`runtime-69e23d846a448759a34d3829`, shared with EC/TS/IE) is fully consumed by failing retries on stuck workflows. Net new fetched data piles up at `received` status which is fine, but no autonomous processing is happening anywhere. **Suggested fix shape (3 parts; suggest one PR):** (a) `kernel/queue_processor.py` — import `WorkflowAlreadyStartedError` from `temporalio.client`, add it as a sibling `except` clause that pass-silents (or, better, marks the message as `dead_letter` so future sweeps skip it). (b) New CLI / API admin verb to drain stale pending messages — `indemn queue drain --role <name>` or `indemn message bulk-transition --filter '{"status":"pending","target_role":"<role>","created_at":{"$lt":"<X>"}}' --to dead_letter`. (c) `dispatch_associate_workflows` should skip messages whose target role has zero active associates (route to HumanReviewWorkflow per existing branch, OR mark `parked_no_active_associate` and skip until an active actor exists). Verification: queue stats should show pending counts dropping, runtime logs should stop spamming "already started," LangSmith should show fresh agent traces within 1 cron tick of activating Email-Fetcher. | 🟢 Fixed | 2026-04-30 Session 14 — Email-Fetcher activation revealed kernel-side jam. Logs from Railway services `indemn-queue-processor` (dispatch retry storm), `indemn-runtime-async` (`asyncio.exceptions.CancelledError` mid-langgraph stack indicating cancelled in-flight runs), `indemn-temporal-worker` (`Failed activation on workflow BulkExecuteWorkflow` warnings for past Bug #37 attempts). | 2026-04-30 Bug #38 fork session: indemn-os main commits `1026d78` (`fix(queue+activities): unjam dispatch + tolerate malformed rows (Bug #38, Bug #37 follow-on)`) + followup `c36969b` (`fix(queue): sort dispatch sweep pending-first to make forward progress (Bug #38 followup)`). **Three-part fix landed in one PR per the suggestion:** (a) extracted `_dispatch_one_message(message, client)` helper in `kernel/queue_processor.py`. New `except WorkflowAlreadyStartedError` clause sibling to (now-removed) `except RPCError` — imports from `temporalio.exceptions` (the SDK 1.25 location, NOT `temporalio.service`). (b) New `_handle_already_started(message, client, workflow_id)` queries `client.get_workflow_handle(workflow_id).describe()` — RUNNING/CONTINUED_AS_NEW leaves message alone; terminal statuses (COMPLETED/FAILED/CANCELED/TERMINATED/TIMED_OUT) mark message as `dead_letter` via direct motor `update_one` (bypasses Pydantic load — status-only change). describe() failure logs and continues; next sweep retries the diagnosis. (c) New `parked` Message status added to `kernel/message/schema.py` Literal. Dispatch logic distinguishes "active associate exists" (ProcessMessageWorkflow) from "associates exist but none active" (autonomous role; park message via motor update_one) from "no associate-type actors at all" (human role; HumanReviewWorkflow fallback unchanged). Dispatch sweep query updated to `{"$in": ["pending", "parked"]}` so when operator reactivates a suspended associate, next sweep dispatches the parked queue without operator intervention. Followup `c36969b`: added `.sort([("status", -1), ("created_at", 1)])` to the sweep query so 'pending' comes before 'parked' within each batch — without this, an existing parked backlog kept shadowing still-pending messages and the sweep made no forward progress. **Tests:** new `tests/unit/test_dispatch_workflow_already_started.py` (16 tests) — 4 shape pins (imports, except clause, schema, parked-in-query) + 12 behavior tests covering RUNNING leave-alone, terminal → dead_letter, describe failure handling, suspended-actor parking, no-associates-at-all → HumanReview, active-associate dispatching. Full unit suite: 422 tests pass, zero regressions. **Verified live 2026-04-30 13:14-13:21Z:** pre-deploy 1015 EC + 28 TS + 1 EF pending + 2 IE pending; post-deploy all transitioned cleanly (1015 EC parked, 28 TS parked, 1 EF parked, 2 IE went processing → terminal-cleanup → dead_letter — confirming the WorkflowAlreadyStartedError catch + describe fired correctly on previously-stuck IE workflows). Email-Fetcher reactivated; cron tick 13:20:00Z fired a fresh `_scheduled` message that dispatched cleanly through the kernel side (no log spam). The agent run failed downstream in the harness on the entity-load step (`indemn _scheduled get <id>` — see new entry in Medium section: harness lacks _scheduled handling, related to Bug #40); that's a separate harness gap, NOT Bug #38. Email-Fetcher re-suspended pending main session resumption of TD-1. queue-processor logs post-deploy: no more "Failed to dispatch workflow ...: Workflow execution already started" warnings. **Acceleration mechanic delivered:** kernel-side dispatch is unblocked; Bug #41 (harness `_scheduled` handling, see new entry) is the remaining blocker for actual Email-Fetcher autonomous operation. |
| **Bug #37 follow-on — bulk-delete + update workflow paths poison on malformed entities; only the list endpoint has `skip_invalid` opt-in** — surfaced 2026-04-30 Session 14 attempting Bug #37 row cleanup. The Bug #37 fix added `_DomainQuery.to_list(skip_invalid=False)` and wired `skip_invalid=True` only into `_register_list_route` in `kernel/api/registration.py:354-364`. Other read paths still strict: (a) `bulk-delete` workflow loads target entities via `find_scoped` → Pydantic validation fails → workflow `FAILED`. (b) PUT `/api/{entity}/{id}` loads the entity before applying patch → same failure. (c) `_register_bulk_route::start_bulk` dry-run preview also fails. Two known-bad rows (`Email 69ea548e0a2b41b769606e92` Oneleet, `69ea556f0a2b41b769607387` Linear Orbit) are unreachable through the standard kernel paths. **Suggested fix shape:** propagate `skip_invalid=True` to the workflow-loading code paths AND PUT update path, OR add a kernel admin route that accepts an `_id` and bypasses Pydantic validation for the targeted DELETE/UPDATE-of-broken-fields case. Tests should pin: malformed-doc bulk-delete with `_id` filter succeeds; malformed-doc PUT with `{<malformed_field>: null}` succeeds. | 🟢 Fixed (bulk-delete path); PUT path explicitly out of scope | 2026-04-30 Session 14 (TD-1 pre-flight cleanup attempt). Live verified: `bulk-delete` with `--filter '{"_id":"<malformed-id>"}'` workflow lifecycle FAILED with generic "Workflow execution failed" reason; PUT `{"company": null}` returns HTTP 400 with same Pydantic `is_instance_of` ObjectId error as the GET path pre-fix. | 2026-04-30 Bug #38 fork session: bundled with Bug #38 in commit `1026d78` (`fix(queue+activities): unjam dispatch + tolerate malformed rows (Bug #38, Bug #37 follow-on)`). **Two-layer fix in `kernel/temporal/activities.py`:** (1) Propagated `skip_invalid=True` to both bulk activity `to_list()` calls — `process_bulk_batch:204` and `preview_bulk_operation:311`. Valid rows iterate normally; malformed rows are skipped with a warning log naming type+_id. (2) Added DELETE-cleanup pass in `process_bulk_batch`: after iterating Pydantic-valid entities, the activity queries motor for matched `_id`s in this batch's offset window that aren't in `entities` (because skip_invalid dropped them), then `delete_many`'s those bad `_id`s in the same transaction. Per-id warning log. Audit chain skipped for malformed rows by necessity (can't compute changes from a doc that doesn't validate). This unblocks the actual cleanup-of-Bug-#37-rows use case end-to-end. **Tests:** new `tests/unit/test_bulk_workflow_skip_invalid.py` (4 tests) — shape pins for `skip_invalid=True` propagation in both activities + DELETE-cleanup pass + strict-default contract preservation. **Verified live 2026-04-30 13:18Z:** `indemn email bulk-delete --filter '{"_id":{"$in":["69ea548e0a2b41b769606e92","69ea556f0a2b41b769607387"]}}' --no-dry-run` → workflow `bulk-177aaf835c34` lifecycle COMPLETED with matched=2 succeeded=2 errored=0; both `_id`s subsequently 404 on GET. **PUT path explicitly deferred** as out-of-scope: the use case "patch a malformed field on a malformed row" is rare; the bulk-delete cleanup pass handles known-bad rows. PUT lenient-load is a separate future fork if needed (would require either a get_scoped_lenient helper or a force-update admin route). |
| **Bug #36 — Gmail + Calendar `fetch_new` adapters silently ignore the `until` parameter** — narrow-window fetch returns everything from `since` forward to the API page cap (500 for Gmail). Observed live during Armadillo trace setup: `email fetch-new` with a 4-hour window (Apr 2 18:00–22:00 UTC) returned 500 emails spanning Apr 2 → Apr 29, no warning. Same defect on Calendar: `meeting fetch-new` with Apr 28 18:00–22:00 UTC returned 6 meetings including one at 4/29 12:14 (next-day) and one at 4/28 22:59 (after `until`). Operators have no way to scope ingestion; narrow-window intent cascades into wide ingestion; with EC active this would fire 500 EC runs for a 3-email-intent. Breaks trace-as-build discipline because the adapter widens scope silently. Root cause was deeper than the bug entry's first framing: `**params` on the public adapter methods silently absorbed any unknown kwargs (operators could pass `until`, `query`, `mailbox`, anything — all dropped without warning). | 🟢 Fixed | 2026-04-29 Session 12 (Armadillo trace setup) | 2026-04-29 Session 12: indemn-os main commit `477a98f` — `kernel/integration/adapters/google_workspace.py`. Two-part fix: (1) plumb `until` end-to-end through `fetch`, `fetch_emails`, `_list_user_conferences`, `_list_gmail_messages`. Gmail: `before:<date>` (date-precision API) + sub-day client-side filter on parsed `Date:` header. Meet: AND-combine `start_time<="{until}"` into the conferenceRecords filter. (2) Replace `**params` with `**unknown_params` and raise `AdapterValidationError` listing supported params (`Supported: since, until, user_emails, limit.`). 15 new unit tests in `tests/unit/test_google_workspace_adapter.py`. Full 390-test unit suite green. Deployed via `railway up --service indemn-api` (deploy `05b8d75a-…`, SUCCESS). Live verified: unknown-param request returns AdapterValidationError; 30-min narrow window returns `fetched: 0` (pre-fix would have returned the API's 500-message page cap). **Material side-effect:** 500 Email entities + 6 Meeting entities created in dev OS as a result of the bug; cleanup or accept-and-fix-forward TBD after the trace concludes. **Outlook adapter has the same `**params` pattern** — same fix being propagated in a follow-up commit. |
| **Bug #35 — deepagents skill discovery double-nested path against backend root_dir** — every EC iteration v3 → v7 had been ineffective because the agent never read the email-classifier associate skill. Confirmed via LangSmith system-prompt inspection (trace `019dd602-ddb9-7f03-946c-fcee7413e11f`): `Available Skills: (No skills available yet)`. Root cause (Session 12): `_write_skills_to_filesystem` wrote skills to absolute `/workspace/{activity_id}/skills/email-classifier/SKILL.md` correctly, but RETURNED a relative path `"{activity_id}/skills"` to `create_deep_agent(skills=[...])`. deepagents 0.5.3 `FilesystemBackend._resolve_path` (parent of `LocalShellBackend`) resolves relative paths against `cwd=root_dir`, which is also `/workspace/{activity_id}` — so the relative path double-nested to `/workspace/{X}/{X}/skills` and matched nothing. Hypothesis (a) from Session 11 handoff Section 3 confirmed exactly. **Do NOT inline skills into system prompts** (Craig: "BAD! TERRIBLE DESIGN AND NOT ALIGNED WITH THE VISION"). | 🟢 Fixed | 2026-04-28 Session 11 LangSmith investigation; trace `019dd602-ddb9-7f03-946c-fcee7413e11f`; Session 12 root-cause via deepagents 0.5.3 source read + local repro | 2026-04-29 Session 12: TWO stacked fixes required. **Fix 1 — commit `8141a80`** on indemn-os main: single-line change at `harnesses/async-deepagents/main.py:115` returning `skills_lib_dir` (absolute) instead of relative `f"{activity_id}/skills"`. Verified locally: relative → 0 skills found; absolute → 1 found. Deployed `be045d86-…`. Live test: path now correct (`/workspace/act-{X}/skills` shown in system prompt), but agent STILL saw "(No skills available yet)" — exposing **Fix 2 — commit `2ba6f63`**: `_write_skills_to_filesystem` was hand-rolling YAML frontmatter via f-string; the `email-classifier` skill record has no `description` field, so the harness fell back to `f"Skill: {name}"` = `"Skill: email-classifier"` and wrote `description: Skill: email-classifier` — invalid YAML (unquoted scalar with two colons → "mapping values are not allowed here"). Container logs from the post-fix-1 run made this explicit. Fix 2 replaces the hand-rolled f-string with `yaml.safe_dump`, which auto-quotes any value containing special chars. Deployed via `railway up` (deploy `906a47cc-…`, SUCCESS). **Live verification 2026-04-29 Session 12 (LangSmith trace `019dd93d-9bdc-7571-b83e-dc29422a1c92`):** system prompt now reads "**Available Skills:** - **email-classifier**: Skill: email-classifier  -> Read `/workspace/act-{X}/skills/email-classifier/SKILL.md` for full instructions". Skill discovery functional end-to-end. **Note on agent compliance (separate concern, NOT part of Bug #35):** the agent saw the skill in its system prompt but did NOT call `read_file` on the SKILL.md path during the run (Diana@CKSpecialty trace 019dd93d). Tool calls were `indemn skill get Email` (entity skill, per harness DEFAULT_PROMPT) and `indemn email transition --to processed`. Email was already at `classified` from the prior pre-fix-2 run, so the agent took the shortest path to a terminal state without reading the associate skill. Future skill iterations (v8) need either DEFAULT_PROMPT updates directing the agent to read the associate skill BEFORE acting, OR a more compelling skill description that drives the agent to load it under progressive disclosure. The skill record itself is also missing a real `description` field — currently relying on the harness fallback. **FOLLOW-UP refactor in same session (commit `7281b83` — `refactor(harness): load skills via CLI in DEFAULT_PROMPT, drop deepagents skills layer`):** the entire deepagents filesystem-skills mechanism was dropped. Reasoning: it was designed for "many skills, agent dynamically chooses" — our associates have ONE skill each, so progressive-disclosure-via-filesystem is the wrong fit. New canonical pattern: agent runs `execute('indemn skill get <name>')` (system-prompt directive), skill content arrives as tool result on turn 1 and stays in agent's message history; symmetric with how associates load entity skills + everything else in the OS. Eliminates Bug #35 class entirely — no path resolution against backend root_dir, no YAML escaping, no SKILL.md format to maintain. The two stacked fixes (`8141a80`, `2ba6f63`) are no longer load-bearing; the layer they fixed is gone. Earlier "Do NOT inline skills into system prompts" guidance is superseded — the new pattern loads via tool-result on turn 1, not static system-prompt content; system-prompt has only a directive telling the agent to use the CLI. **EC v9 verified working under the new pattern on Armadillo's first-contact email Apr 29.** |
| **Touchpoint forward source pointers (Option B)** — Add `source_entity_type: str` + `source_entity_id: objectid` fields to Touchpoint; Synthesizer populates them on create. Without this, the Intelligence Extractor has no path from Touchpoint to the source Meeting/Email content. Closes the structural information-access asymmetry surfaced by the GR Little trace Apr 24. | 🟢 Fixed | `artifacts/2026-04-24-extractor-pipeline-gap.md`, `artifacts/2026-04-24-extractor-procedure-and-requirements.md` | 2026-04-27: `indemn entity modify Touchpoint --add-field` + `indemn-api` redeploy + Synth skill v3 (`projects/customer-system/skills/touchpoint-synthesizer.md` updated, content_hash `4ddfb844ac84...`). Both new fields visible in auto-generated entity skill; new Touchpoints from the Synth will populate them. |
| **Entity-resolution kernel capability** — given a candidate (domain, name, email, similar-description), return likely existing matches with confidence scores. Associates call before creating. Root fix for Bug #16 (the 446-Company auto-create explosion). Required for reliable hydration at scale. | 🟢 Fixed | `artifacts/2026-04-24-trace-plan-and-design-notes.md`, memory `project_customer_system_entity_identity.md`, Bug #16 detail | 2026-04-27: merged to main as `a5a1c97` (feature commit `579c713`), deployed via `railway up --service indemn-api`, activated on Company via `PUT /api/entitydefinitions/Company/enable-capability`. **Verified live against Alliance trace data:** resolving `{"name": "Alliance Insurance"}` correctly surfaces canonical Alliance + the dupe; resolving `{"name": "FoxQuilt"}` surfaces both `Foxquilt` (lowercase-equality, 1.0) and `Fox Quilt` (fuzzy, 0.94); nonexistent name returns 0 candidates. Architecture: kernel-domain-agnostic capability, two built-in strategies (`field_equality` with normalizer registry — email/domain/lowercase_trim/none — and `fuzzy_string` via rapidfuzz token_set_ratio), composable per-entity via `activated_capabilities`. **Contract: returns ranked candidates, never auto-picks.** Score 1.0 = exact-match-after-normalization; <1.0 = fuzzy. Multiple candidates tied at 1.0 = ambiguity surfaced honestly; caller decides. 29 unit tests pin behavior. Skill generator emits a Resolve section when activated. Vector search (MongoDB Atlas Vector Search) deferred per plan — added when fuzzy proves insufficient. **Subsumes Bug #16 + Bug #17** once associate skills (Email Classifier, Touchpoint Synthesizer) are updated to call resolve before creating — that domain work is the next step. |
| **Bug #29 — Entity-definition replacement doesn't evict old API routes** — `register_domain_entity()` overwrites `ENTITY_REGISTRY` but `register_entity_routes()` calls `app.include_router(router)` which appends rather than replaces. Stale closures keep the old class. Workaround today: redeploy. Fix: in `kernel/api/registration.py`, evict matching prefix routes from `app.router.routes` before include. | 🟢 Fixed | `artifacts/2026-04-24-os-bugs-and-shakeout.md` Bug #29 | 2026-04-27 fork session: merged to main as `83d2494` (feature commit `0bd4e50`). New `_evict_routes_for_prefix(app, prefix)` helper removes routes whose path equals the prefix or starts with `prefix + "/"`; called inside `register_entity_routes` before `app.include_router(router)`. Trailing-slash check guards against prefix-lookalikes (e.g. `/api/companys2` is NOT evicted by `/api/companys`). 7 unit tests (incl. end-to-end TestClient roundtrip proving a re-registered handler wins over the stale one). **Deployed 2026-04-27** via `railway up --service indemn-api`. Live entity-def iteration without restart now works as advertised. |
| **Bug #23 — `bulk-delete` silently drops MongoDB operator filters** (`$in`, `$gte`, `$ne`, `$oid`, `$date`). Only simple equality filters work. Combined with Bug #24 (`bulk status` reports COMPLETED even on 0 matches), users can't tell the operation did nothing. Blocks any non-trivial cleanup at scale. | 🟢 Fixed | `artifacts/2026-04-24-os-bugs-and-shakeout.md` Bug #23, Bug #24, Bug #32 | 2026-04-27 fork session: merged to main as `0ed8c80` (feature commit `62f3254`) + alias-fix commit `6b8c62e` + Bug #32 fix `b5e4757`. New `kernel/api/_filter_safelist.py::parse_filter` provides a shared three-layer safelist: field-name allowlist via `entity_cls.model_fields` (with Pydantic alias support so `_id` works alongside the canonical `id`), operator allowlist (`$in`/`$nin`/`$ne`/`$gt`/`$gte`/`$lt`/`$lte`/`$exists`), and per-field type coercion (ObjectId hex / `$oid` → `bson.ObjectId`; ISO 8601 / `$date` → `datetime`; including operator operands and `$in` list elements). `_parse_list_filter` becomes a thin wrapper. Per-entity `_register_bulk_route::start_bulk` validates `filter_query` at the API boundary → fast 400 on bad input. Activity re-runs `parse_filter` to produce typed values for `find_scoped()` (typed values can't cross Temporal serialization). Bug #24: `BulkExecuteWorkflow` returns `{matched, succeeded, errored, errors}` with status `completed_no_match` when matched=0. `GET /api/bulk/{id}` fetches `handle.result()` on terminal states and merges into response. **Verified live against dev API:** Test 1 `$in` operator deleted 2 throwaway companies (matched=2, succeeded=2); Test 2 `$gte` datetime via list-endpoint returned 2 matching companies; Test 3 no-match filter returned `completed_no_match`; Test 4 bad input (`{"_id":{"$in":["not-hex"]}}`) returned 400 with field-level error. Bug #32 surfaced during Test 2's dry-run path → fixed in same merge. 56 unit tests pass + full 283-test suite green. |
| **Bug #32 — `preview_bulk_operation` ObjectId serialization + missing retry policy → infinite retry storm** — every dry-run that matched any docs hung `RUNNING` forever because `e.model_dump()` returned raw `ObjectId` values that Temporal's data converter couldn't serialize, then the no-retry-policy workflow retried unboundedly. Pre-existing latent bug exposed by Bug #23's correct `current_org_id.set()` fix (old code's missing org_id meant samples were empty in non-admin queries → no serialization needed). Fix: use `kernel.api.serialize.to_dict()` for sample entities + add `RetryPolicy(maximum_attempts=3, non_retryable_error_types=["PermanentProcessingError"])` to the dry-run activity call. | 🟢 Fixed | `artifacts/2026-04-24-os-bugs-and-shakeout.md` Bug #32 | 2026-04-27 fork session: shipped in same branch as Bug #23/#24 (commit `b5e4757`). |
| **Bug #30 — Declarative index reconciliation + sparse-via-partial-filter (kernel architectural)** — kernel previously only ADDED indexes (idempotent `create_index`); never DROPPED ones the operator stopped requesting. Removing a `unique: true` field flag left the index in MongoDB silently. Plus there was no path for "unique-when-set" semantics (plain MongoDB sparse doesn't help when Pydantic writes nulls explicitly). Fix: new `kernel/entity/indexes.py` with `reconcile_indexes(coll, defn)` that diffs current vs desired and drops/creates as needed; new `sparse: bool` on FieldDefinition that translates to `partialFilterExpression: {<field>: {$type: <bson_type>}}`. Replaces both additive create_index loops in `kernel/db.py` (startup + register_domain_entity). Plus `modify_fields` extension to `PUT /api/entitydefinitions/{name}/modify` for clean field-spec updates without remove+add. Plus `setup_logging()` added to API startup so kernel INFO logs reach Railway. | 🟢 Fixed | 2026-04-27 Alliance trace; subsumes Bug #2 (Meeting create), Bug #25 (Company create) | 2026-04-27: deployed via `railway up --service indemn-api`. Verified end-to-end (two manual Meetings + two Companies coexist without collision). See `artifacts/2026-04-24-os-bugs-and-shakeout.md` Bug #30 for full journey including initial wrong hypothesis. Establishes the principle: entity definition is source of truth, MongoDB follows. |

### High / significant capability or reliability gap

| Item | Status | Source | OS-side artifact / commit |
|---|---|---|---|
| **Bug #50 — Mongo queue visibility-recovery + cron_runner heartbeat were independent timers; recovery loop never capped at max_attempts** — surfaced 2026-05-04 mid-session investigating Email/Slack fetcher chronic stuckness post-cron_runner-flip. Two coupled defects. **(50a) Bug #49 (Session 16) added a Temporal activity heartbeat in the cron_runner branch — keeps the activity alive past 90s heartbeat_timeout. But Mongo's `message_queue.visibility_timeout` (5 min, set on every claim in `kernel/message/mongodb_bus.py`) was independent — nothing extended it while the runtime was still working.** Slow subprocesses (Email's 11-mailbox walk, Slack on backed-up watermark) raced the queue's recovery sweep: pod A still working, queue recovers at 5 min, pod B claims it (multi-pod is by design), pod A's later `complete` hits 404 "Message not found" (race-and-truncate symptom). Observed live 2026-05-04: `cron_runner success: associate=Slack Fetcher` + `Cron command succeeded but queue complete failed for message X: CLI failed (1): Error 404`. Net effect: subprocesses succeed but bookkeeping fails, watermark never advances, backlog grows. **(50b) Pre-fix `kernel/queue_processor.py::check_visibility_timeouts` unconditionally recovered every timed-out `processing` message back to `pending` with no max_attempts check.** The bus's `claim` path increments `attempt_count` on every claim, but nothing capped it — so a stuck message (visibility-timeout race) could attempt 7+ times indefinitely (observed: stuck email_fetcher message `69f89bec1f2c3ee82ecb66c4` at attempt_count=7 / max_attempts=3, last_error "Command 'indemn email fetch-new' timed out after 600.0 seconds"). The explicit `bus.fail` path enforced max_attempts; this implicit visibility-recovery path was the gap. | 🟢 Fixed end-to-end | 2026-05-04 mid-session: visibility-timeout race surfaced in runtime-async logs (`Heartbeat failed (1/10)... timed out after 30.0s`); attempt_count cap gap surfaced via direct mongo query on stuck message. | 2026-05-04 indemn-os main commit `18ab3b9` (`fix(queue+harness): wire queue visibility extension + cap visibility-recovery at max_attempts — Bug #50`) merged via `7c3a54c`. Three production code changes + tests: **(a)** `kernel/api/queue_routes.py` new `POST /api/message_queues/{id}/extend-visibility` endpoint resets visibility_timeout = now + 5min; idempotent on terminal status (completed/dead_letter/failed), refuses pending. **(b)** `indemn_os/src/indemn_os/queue_commands.py` new `indemn queue extend-visibility <id>` CLI mirroring complete/fail verbs. **(c)** `harnesses/async-deepagents/main.py` cron_runner heartbeat loop now calls `await asyncio.to_thread(indemn, "queue", "extend-visibility", message_id)` alongside `activity.heartbeat()` — same 30s cadence; CLIError caught + logged so single failure doesn't crash the loop. **(d)** `kernel/queue_processor.py::check_visibility_timeouts` split into two update_many calls — first dead-letters messages where `attempt_count >= max_attempts` via `$expr: {$gte: ["$attempt_count", "$max_attempts"]}`, then recovers the rest. Logs `Dead-lettered N messages over max_attempts (Bug #50)` when capping. **Tests:** 4 new tests in `tests/unit/test_check_visibility_timeouts_attempt_cap.py` (shape pins on $expr branch + recovery branch + ordering + behavior with mocked collection); 8 new tests in `tests/unit/test_extend_visibility_endpoint.py` (route registration, terminal idempotency, 5-min offset, processing-only guard, happy path, 404, 400 on pending, all terminal statuses); 1 new shape pin in `harnesses/async-deepagents/tests/test_cron_runner.py::test_cron_heartbeat_loop_extends_queue_visibility` (queue extend-visibility called via asyncio.to_thread, uses input.message_id, CLIError caught + log.warning). 477 kernel unit tests pass (12 new), no regressions. **Deployment:** indemn-api + indemn-queue-processor + indemn-runtime-async all redeployed (build + roll-out took ~2x because first round of `railway up` was triggered from the operating-system worktree CWD, uploading the wrong source — caught + corrected by re-running from `/Users/home/Repositories/indemn-os` directly; lesson logged separately as a CLI gap). New image hashes: api `3253bbf9...`, queue-processor `58f17e34...`, runtime-async `874723f6...`. **Verified live 2026-05-04 19:50+:** vis_gap > 5min observed on multiple email_fetcher messages (10.9min, 8.8min, 7.8min — proving extend-visibility fires on heartbeat); queue_processor logs `Dead-lettered 1 messages over max_attempts (Bug #50)` repeatedly (proving 50b cap fires); slack/meeting/drive fetchers completing cleanly post-deploy. Email fetcher still chronically slow due to a separate fetch_new bottleneck (next row). **Pre-deploy backlog drained manually**: 86 stuck email+slack `_scheduled` messages (status=pending or processing, both racing the recovery loop) marked `dead_letter` via direct mongo update (audit trail preserved with explicit `last_error` reason). Slack 3-day watermark gap (post-Session-15 stuckness) drained via chunked `indemn slackmessage fetch-new --data '{"since":..., "until":...}'` — first 6h chunk timed out at the CLI (10:07s ReadTimeout) but succeeded server-side, advancing the watermark from 4488min stale to 14min current. |
| **`fetch_new` capability does sequential `save_tracked()` per entity — chronic bottleneck for high-volume fetchers** — surfaced 2026-05-04 investigating Email Fetcher's chronic >5min subprocess time. **Verified there is NO LLM call in the fetch path** (cron_runner mode is genuinely deterministic; LangSmith traces stay at 0; verified via grep on `kernel/integration/adapters/google_workspace.py` and `kernel/capability/fetch_new.py`). Real bottleneck: `kernel/capability/fetch_new.py:77-96` does `for item in raw_results: await entity.save_tracked(...)` — sequential per-entity loop. Each `save_tracked` does the full kernel save path (validation + change record with hash chain + watch evaluation + selective message emission). At ~150-300ms per save × N new entities × multi-mailbox fan-out, Email Fetcher (11 mailboxes × accumulated emails since last successful watermark) chronically takes 5-10 min per cron tick. Slack Fetcher seldom hits this because it's often `fetched: 0` (empty); Meeting + Drive are low-traffic. **The visibility-extend fix (Bug #50a) keeps the activity alive while this slow loop runs, but the loop being slow is itself the foundation issue** — slows ingestion, increases failure surface, makes the multi-pod race much more likely. **Two reasonable fix shapes:** (a) **bulk_save_tracked** — batched insert with one validation pass + one summary change record + one watch-evaluation pass (kernel-level fix; affects every adapter; bigger lift; right long-term answer). (b) **per-tick chunk cap** in `fetch_new` — `params.get("limit", DEFAULT_TICK_LIMIT)` bounds per-call work; subsequent ticks pick up the rest; subprocess stays well under visibility window (smaller fix; symptom-treating). **Suggested:** ship (b) immediately to bound subprocess time, then plan (a) as proper foundation work. **Reframe of original concern about "9-minute fetch with LLM":** there is no LLM. The 9 min is pure save_tracked loop time on an 11-mailbox fan-out with backed-up watermark. | 🟢 Fixed (Session 18) — `bulk_save_tracked()` shipped in `kernel/entity/save.py`: single validation pass → insert_many(ordered=False) → in-memory hash-chained change records → batched watch evaluation. Wired from `kernel/capability/fetch_new.py`. 12 unit tests. Per-tick chunk-cap (Session 17 bridging fix) still in place as additional safety; bulk_save makes it redundant rather than load-bearing. | 2026-05-04 mid-session investigation; Email Fetcher zero `cron_runner success: associate=Email Fetcher` log lines vs Slack/Meeting/Drive succeeding within visibility window; code-read confirmed sequential per-entity save loop in `kernel/capability/fetch_new.py:77-96` | TBD — bulk_save_tracked is the right fix; per-tick chunk cap is the small bridging fix. Both candidates for next OS sprint. |
| **Bug #37 — Email list endpoint returns HTTP 400 when ANY single document has malformed `company` field** — `GET /api/emails/?limit=100&offset=0` (no filter) returns `{"error":"ValidationError","errors":[{"loc":["company"],"msg":"Input should be an instance of ObjectId","type":"is_instance_of"}]}`. Some Email document has a non-ObjectId `company` value (likely a stringified dict from a pre-Bug-#9-fix associate run, or a manual create with the wrong shape). Pydantic strict validation on read-back fails on that one row → entire list response aborts. One bad row poisons the endpoint for everyone. No CLI or API recovery path: can't list to find the bad row, can't bulk-fix it, can't sample. **Fix shape (revised after vision-bias review):** route-only fix is too narrow — every list-y caller in the kernel is exposed to the same crash. Sidecar `validation_errors[]` adds API-shape complexity for info that logging already surfaces. Instead: opt-in tolerance at `_DomainQuery.to_list(skip_invalid=False)` — default preserves strict behavior (migrations / audit code keep the loud failure); the user-facing list route opts in. Warnings name entity type + bad doc `_id` for operator cleanup. | 🟢 Fixed (code) — data cleanup pending | 2026-04-29 Session 12 (Armadillo trace orchestration) + Phase 1 DB query confirmed exactly 2 bad rows: Email `69ea548e0a2b41b769606e92` (`company={"name":"Oneleet"}`) and `69ea556f0a2b41b769607387` (`company={"name":"Linear Orbit, Inc."}`). | 2026-04-29 Session 12: indemn-os main commit `a5aa89f` — `kernel/entity/base.py` `_DomainQuery.to_list(skip_invalid=False)` + `kernel/api/registration.py` `_register_list_route` opts in for domain entities (kernel entities use Beanie, kept on existing path). 6 unit tests in `tests/unit/test_domain_query_skip_invalid.py` pin both behaviors. 402-test suite green. Deployed via `railway up --service indemn-api` (deploy `ca4416df-…`, SUCCESS). Live verified: `GET /api/emails/?limit=100` returns HTTP 200 with 99 emails (was 400); container log shows `Skipping malformed Email document _id=69ea548e0a2b41b769606e92 in to_list (skip_invalid=True)`. **Data cleanup of the 2 bad rows is customer-system domain work** — both have a real Company name (Oneleet, Linear Orbit) that could be resolved against canonicals, OR set to null and re-classified by EC. Not bundled into this kernel fix. |
| **Deal-lifecycle automation gap (Deal-creator associate + Proposal-at-DISCOVERY auto-create + Touchpoint↔Deal chicken-and-egg)** — surfaced 2026-04-29 Armadillo trace Step 5/6. When a new Company gets its first external Touchpoint, the autonomous flow has no associate that creates the Deal. Currently human-orchestrated. Same applies to Proposal-at-DISCOVERY: per the breakthrough ("Proposal is created at DISCOVERY as an empty spine"), a Proposal entity should auto-instantiate when the Deal enters discovery — but no associate does this either. Plus a chicken-and-egg: TS creates the Touchpoint with `deal=null` (Deal doesn't exist yet); something else has to backfill the Touchpoint.deal field after the Deal is created. **The autonomous flow needs:** (a) a Deal-Pipeline-Manager associate that watches Company creation events / first-external-Touchpoint events and creates a Deal at `contact` stage with sensible defaults; (b) a stage-transition watcher that creates the empty Proposal when Deal enters `discovery`; (c) a Touchpoint backfill step (or change ordering: create Deal first, then Touchpoint with deal populated). Currently a human runs `indemn deal create`, `indemn proposal create`, and `indemn touchpoint update` manually after each new prospect's first contact. | 🔴 Open | 2026-04-29 Session 12 Armadillo trace Step 5 (no Deal existed for Armadillo when TS finished); Step 6 (had to manually create Deal + transition to discovery + backfill Touchpoint.deal on both Touchpoints) |
| **Employee entity-resolve not activated** — surfaced 2026-04-29 Armadillo trace Step 4. TS skill says "resolve participants as Contacts (external) or Employees (internal)" — but `indemn employee entity-resolve` returns "No such command 'entity-resolve'." Worked around by `indemn employee list` + Python filter on email. Fix: activate `entity_resolve` capability on Employee with a `field_equality` strategy on `email` (with `lowercase_trim` normalizer) — same shape as Contact's activation. One CLI call to enable. Without it, TS can't resolve participants as Employees autonomously. | 🟢 Fixed (Session 18) — `PUT /api/entitydefinitions/Employee/enable-capability` with `field_equality` on email + `lowercase_trim` normalizer. Verified: `indemn employee entity-resolve --data '{"candidate":{"email":"kyle@indemn.ai"}}'` returns Kyle Geoghan at score 1.0. | 2026-04-29 Session 12 Armadillo trace (TS Step on Apr 2 email when resolving Kyle as Employee) |
| **Bug #16 — Associates auto-create Companies despite skill saying never** — symptom; root is the missing entity-resolution capability above. Will be subsumed by that fix. | 🟢 Fixed (Session 10) | Bug #16 | 2026-04-27: kernel primitive shipped (`entity_resolve` capability, deployed, activated on Company). 2026-04-28 (Session 9): EC skill v3 updated to call resolve-before-create; reactivation auto-created a Company anyway, attributed to "skill compliance gap on multi-candidate branch". 2026-04-28 (Session 10): **Actual root cause was the kernel CLI bug Bug #34 below** — `entity-resolve` was returning HTTP 422, agent saw a tool error and fell through to create. CLI fixed via commit `db97694`. EC v6 + the CLI fix verified end-to-end on Diana@CKSpecialty (LangSmith trace `019dd5c1-3aab-7ea3-aa08-d007044570a1`) — no more auto-created duplicate Companies. EC v6 still has known partial gaps on the Contact-create + multi-1.0 branches (defer to v7 / eval coverage). |
| **Bug #34 (NEW) — published `indemn_os` CLI missing `entity_resolve` from `_COLLECTION_LEVEL_CAPS`** — the published CLI hardcoded `_COLLECTION_LEVEL_CAPS = {"fetch_new"}` in `indemn_os/src/indemn_os/main.py:202`; kernel side has `{"fetch_new", "entity_resolve"}` in `kernel/capability/__init__.py`. Effect: `indemn <entity> entity-resolve --data '{"candidate":...}'` (no entity_id) routed to the entity-level path which list-and-loops with `?limit=1000`, the API caps list at 100 → `Error 422: query.limit > 100`. The Email Classifier saw this as a tool failure and fell through to `company create`, auto-generating duplicate Companies — the failure mode prior sessions attributed to "skill-compliance gap". Confirmed via LangSmith trace `019dd522-ac39-7fc2-bcab-dacdc450cfc5` on the Diana@CKSpecialty reprocess 2026-04-28. **Skill content was correct in the DB the whole time; the CLI tool was broken.** | 🟢 Fixed | 2026-04-28 Session 10 | Commit `db97694` on main: `_COLLECTION_LEVEL_CAPS = {"fetch_new", "entity_resolve"}` + comment requiring sync with `kernel/capability/__init__.py::COLLECTION_LEVEL_CAPABILITIES`. Verified end-to-end against dev API. **Follow-on filed:** `kernel/cli/app.py` has the same divergence (no `_COLLECTION_LEVEL` set at all — every cap routes through `_make_cap_cmd` as entity-level). Lower priority since the harness uses the published `indemn_os` CLI; worth a separate fix. Test `tests/unit/test_adapter_cli_papercuts.py:92` already pins both surfaces against drift. |
| **Bug #17 — No meeting-to-company classifier in pipeline** — Touchpoint Synthesizer assumes `meeting.company` is set; `fetch_new` leaves it null. Subsumed by entity-resolution + Option B. | 🟡 Kernel ready | Bug #17 | 2026-04-27: kernel primitive shipped (entity_resolve). **Pending: Touchpoint Synthesizer skill calls resolve with `{"name": <attendee company>, "domain": <attendee email domain>}` per meeting attendee.** Domain skill work. |
| **Bug #22 — Service-token untraceability** — all associates authenticate as Platform Admin via shared service token. Changes collection records `actor_id = Platform Admin` for everything; can't tell which associate made a mutation. Blocks forensics. Fix: per-associate service tokens OR `effective_actor_id` field on change records. | 🟢 Fixed | Bug #22 | 2026-04-27: merged to main as `e9d45e8` (feature commit `dae5bf2`) + trace serializer fix (`056cba2`); deployed via `railway up` to both `indemn-api` and `indemn-runtime-async`. **Chose `effective_actor_id` (not per-associate tokens)** — same shape as the existing `causation_message_id` propagation chain: env var (`INDEMN_EFFECTIVE_ACTOR_ID`) → CLI client header (`X-Effective-Actor-Id`) → auth middleware contextvar → `save_tracked` → `ChangeRecord.effective_actor_id`. Per-associate tokens would have been architecturally cleaner long-term but a much bigger lift (provisioning, AWS Secrets per associate, rotation); they solve a security isolation problem we don't have yet. The `effective_actor_id` approach gives forensics today with zero token-management overhead. **Verified end-to-end against live API:** posted Company create with `X-Effective-Actor-Id: <Email Classifier>`, `/api/trace/entity/Company/<id>` returned both `actor_id=<Platform Admin>` and `effective_actor_id=<Email Classifier>`. Compound index on `(org_id, effective_actor_id, timestamp)` makes per-associate forensics queries cheap. 10 unit tests + full 226-test suite green. **Going forward every harness-driven mutation carries the associate identity.** Per-associate tokens can layer on later for security separation when we need it. |
| **Bug #9 — Associates pass dicts instead of ObjectId strings → dead letters** — LLM passes `{"company": {"name": "Oneleet"}}` when it should pass `{"company": "<objectid>"}`. Fix: skill prompt enhancement + auto-generated entity skill should teach the pattern unmissably. | 🟢 Fixed | Bug #9 | 2026-04-27 fork session: merged to main as `31bb594` (feature commit `9f83ac5`). Two-layer fix at the API boundary in `_resolve_relationship_dict_inputs(entity_cls, name, data)` called BEFORE `_coerce_objectid_fields` in both create and update handlers. **Default reject** — dict on relationship field → 400 with offending field + target entity type + canonical shape (`_id` 24-char hex) + the offending value verbatim + a pointer to `entity-resolve` for resolving an _id. **Opt-in entity_resolve fallback** via new `auto_resolve: bool = False` flag on FieldDefinition; when set on a relationship field AND target has `entity_resolve` activated (Bug #31), the boundary calls resolve with the dict as candidate. Auto-link only on a SINGLE 1.0 candidate — anything else (zero, multiple at 1.0, only fuzzy) returns 400 with the candidate list, preserving Bug #31's "never auto-pick" contract. The skill examples shipped earlier (commit `b83fa08`) teach the canonical shape; this fix is defense-in-depth so a misbehaving or new LLM gets a self-correcting 400 instead of a dead-letter. 14 unit tests; full 310-test suite green. **Verified live:** POST/PUT email with `{"company": {"name": "..."}}` → 400 with full shape hint and value in message; happy path with hex string still works (idempotent on non-dict values). Deployed `indemn-api` 2026-04-27. |
| **`--include-related` doesn't follow reverse relationships** — kernel's `--include-related` follows forward references (fields where `is_relationship: true`) but not reverse refs. For Touchpoint, this means Meeting.touchpoint→Touchpoint is invisible. Either fix (general — kernel feature) or rename the flag. Subsumed by Option B for the Touchpoint case but the underlying gap remains. | 🟢 Fixed | `artifacts/2026-04-24-extractor-pipeline-gap.md` | 2026-04-28 fork session: merged to main as `fbe83e6` (feature commit `401325f`). Pre-flight code-read found a SECOND latent bug in the same code path: `kernel/api/registration.py:299` was reading `context.get("related_entities", [])` from `_build_context`'s output, but `_build_context` keys related entities by lowercase target-entity name (`"company"`, `"contact"`) — never under `"related_entities"`. So pre-fix `_related` was ALWAYS `[]` for everyone, even forward refs. Fix shape: new helper `_build_related_entities(entity, depth)` in `kernel/message/emit.py` walks BOTH directions and returns a flat list; each entry carries `_entity_type`, `_relationship_direction` (`"forward"`/`"reverse"`), `_via_field`. `_build_context` left untouched (load-bearing for watch-emit message enrichment per vision-map §5.3 entity-local invariant). Skill-generator's Reading section updated to teach the new shape (propagates to every entity skill via the always-fresh-on-GET fix shipped Apr 28). Self-relationships handled (Proposal.supersedes shows older as forward, newer as reverse, self excluded via `_id $ne`). Documented limitation: domain entities pointing at a kernel entity DO surface; kernel entities pointing at this entity do NOT (no EntityDefinition row to walk) — Pydantic-class introspection is a follow-on if it bites. 10 new unit tests; full 330-test suite green. **Deployed 2026-04-28** via `railway up --service indemn-api`; health check passing post-deploy. **Followup commit `9bb7b0a`** (Apr 28): first deploy hit 500s in prod with `'ObjectId' object is not iterable` — helper used `_serialize_for_context` (preserves raw ObjectId/datetime, fine for in-process watch-emit context but not JSON-serializable). Switched to `kernel.api.serialize.to_dict` (same path every other API route uses). Added regression test pinning JSON-safety. 11 tests; 331 total green. **Verified live against Touchpoint `69efba8350cea0a476d6fa90` (Alliance Apr 27 trace):** returns 6 related entities — Company `69e41a82…98eb` (forward via `company`, "Alliance Insurance"), Deal `69e79a31…5a32` (forward via `deal`, "Alliance Renewal Associate"), Commitment / Signal / Decision (reverse via `touchpoint`), Email `69efc96a…83e9` "v1 proposal" (reverse via `touchpoint`). Both directions, metadata correct, JSON-safe. Alliance constellation queryable from any Touchpoint. |
| **Cross-invocation tool-cache leak in async harness** — `/large_tool_results/` is shared across agent invocations in the same runtime container. An agent's `grep` matched content from a *different* prior agent's cached results during the GR Little trace. Fix: scope per `message_id` in `harnesses/_base/harness_common/backend.py`. | 🟢 Fixed | `artifacts/2026-04-24-extractor-pipeline-gap.md` (Secondary issues) | 2026-04-27 fork session: merged to main as `ac6d475` (feature commit `4e7e83d`). `_root_dir_for_activity()` helper + `build_backend(activity_id)` + `build_agent(activity_id)` plumb-through + cleanup in main.py finally block. 6 unit tests pass. **Deployed 2026-04-27** via `railway up --service indemn-runtime-async`. |
| **Silent workflow stuck-state** — when an agent completes without producing entity state changes, the harness used to mark the message COMPLETED anyway, leaving the watched entity stuck. Variants: (a) agent makes only read-only calls and ends with "nothing to extract" content; (b) agent attempts mutating calls that all fail (E11000, shell-escape errors) and narrates "the email already exists". | 🟢 Fixed (Session 10) | `artifacts/2026-04-24-extractor-pipeline-gap.md` (Secondary issues); 2026-04-28 IE on Touchpoint `69f0d8cb…0f`; 2026-04-28 Session 10 EC on Diana (LangSmith trace `019dd589-…`) | 2026-04-27 fork session merged `67f006c` with initial `agent_did_useful_work()` helper. 2026-04-28 (Session 9): coverage gap surfaced on IE (read-only + "nothing to extract" content silently marked complete). 2026-04-28 (Session 10): **EC also hit it** — agent attempted `email create` twice (failed), narrated "the email already exists", helper returned True via the narrative-content fallback. **Tightened in commit `d914d76` on main** (`harnesses/async-deepagents/completion_logic.py`): new contract requires ≥1 *successful* mutating CLI call. Walks messages, builds `tool_call_id → command` for every mutating `execute` call, matches each tool result message back via `tool_call_id`, checks `[Command succeeded with exit code 0]` / no `[stderr]` / no `[Command failed]` markers. Removes the narrative-content fallback. Domain-agnostic — harness still doesn't know what each associate is supposed to do, just observes "did any state-changing call succeed?". Read-only verbs/capabilities now: `list, get, entity-resolve` (mutating capabilities like `auto-classify, fetch-new, stale-check` intentionally fall through to the mutating bucket). 24/24 tests passing including the new Diana failure pattern test. **Per OS vision discussion (Craig + Claude, Session 10): the harness reports observable facts about tool execution. Detection of "agent didn't fulfill skill intent" lives in evals (Phase E) and observability on top of LangSmith — NOT in the harness.** The original hypothesis ("check if watched entity transitioned") was too domain-aware; the tightened check just confirms whether *any* state change succeeded. |
| **`--include-related` does not follow polymorphic Option B `source_entity_id` refs on Touchpoint** — surfaced 2026-04-28. Touchpoint's `source_entity_id` has `is_relationship: false` because the target is polymorphic (Email or Meeting depending on `source_entity_type`). The kernel's `--include-related` flag only follows fields where `is_relationship: true` with a fixed `relationship_target`. Effect: associates calling `indemn touchpoint get <id> --include-related` get an empty `_related` even though the Touchpoint has Option B source pointers populated. The IE skill (pre-Apr-27, predates Option B) tells the agent to use this flag — and the agent then can't navigate to the source content. **Workaround for skill content (apply now):** instruct agents to use the explicit two-step pattern — `indemn touchpoint get <id>` to read Option B fields, then `indemn <source_entity_type.lower()> get <source_entity_id>` to load the source. **Long-term kernel option:** support polymorphic relationships in the entity framework — a field marked `is_polymorphic_relationship: true` with `target_type_field: "source_entity_type"` so `--include-related` resolves the target type at runtime. Worth raising with the bugfix fork once the immediate skill update is in. | 🟢 Fixed (Session 18) — `is_polymorphic_relationship` + `target_type_field` added to FieldDefinition; `_build_related_entities` now resolves polymorphic refs at runtime per-entity. Touchpoint's `source_entity_id` can now be followed via `--include-related` once the entity definition is updated with `is_polymorphic_relationship: true, target_type_field: "source_entity_type"`. | 2026-04-28 Phase B1 trace investigating IE silence on Touchpoint `69f0d8cbbca2d725ce90ed0f` | Skill update in progress (this session). |
| **List endpoint accepts no arbitrary field filters** — `GET /api/{entity}/` only supports `status`, `search`, `limit`, `offset`, `sort`. Blocks: reverse lookups (Meeting where touchpoint=X), duplicate detection refinement, cadence queries. CLI shows it too — `--data` filter doesn't exist on list. Fix: extend list endpoint with safelisted field filters; expose as `--data` on CLI. | 🟢 Fixed (Session 18) — root cause was CLI missing `--data` flag on list; API endpoint's `filter` param was always correct but unreachable. CLI now passes `--data` as `?filter=`. parse_filter validates + 400s on unknown fields. | `artifacts/2026-04-24-extractor-procedure-and-requirements.md` § Capability #2 | 2026-04-27: merged to main as `df92cca` (feature commit `6293260`), deployed via `railway up --service indemn-api`. **2026-04-28 finding:** the fix has regressed or has gaps. Verified live: `GET /api/touchpoints/?data={"source_entity_type":"Email"}` returns 3 random Touchpoints with `source_entity_type: null` — the filter was silently ignored, NOT applied. Even `?data={"totally_made_up_field":"foo"}` returns HTTP 200 with all results — the unknown-field-returns-400 behavior the original fix promised isn't happening. Possible causes: (a) URL-encoding of nested ObjectId/`$oid` wrappers fails parsing → fallback to no filter; (b) the field-validation safelist accepts then drops unknown fields silently; (c) recent CLI/API deploy reverted the change. Worth raising with the bugfix fork. **Customer-system workaround in skill content:** prefer back-reference fields for idempotency checks (e.g., check `Email.touchpoint` to know if a TS Touchpoint already exists) instead of querying by `source_entity_id`. Also: CLI's `email list` lacks `--data` flag entirely (separate gap — propagation issue from the original fix). |
| **Bug #10 — No backfill / re-trigger mechanism for historical entities** — when a watch is added to a role, only future events fire it. Existing entities are invisible. Cost us significant work in the GR Little trace (we manually created the Touchpoint because the Synth's `Meeting created` watch couldn't refire on an already-ingested meeting). Fix: `indemn <entity> reprocess <id> --role <role_name>` or role-level backfill capability. | 🟢 Fixed | Bug #10, `artifacts/2026-04-24-extractor-procedure-and-requirements.md` § Capability #6 | 2026-04-27 fork session: merged to main as `662dc2d` (feature commit `767a3ef`). New `kernel/message/reprocess.py::reprocess_for_role(entity, role_name, event_type="created")` emits ONE message scoped to the named role's watch — not broadcast. Validates the role has a matching watch (entity_type + event_type) and surfaces `ReprocessError` (→ 400) with the role's actual watches when there's no match, so callers can self-correct. Fresh `correlation_id` per call (new chain), `causation_id="reprocess:<hex>"` sentinel marks origin, `event_metadata.reprocess=True` + `reprocess_requested_by=<actor_id>` thread Bug #22's forensics through. Scoped watches that resolve to None also raise (don't silently emit a message no one will claim). Multiple watches on same role+event use highest `context_depth`. New `POST /api/{collection}/{id}/reprocess` (read perm — reprocess doesn't mutate the entity). Auto-registered CLI verb `indemn <entity> reprocess <id> --role <role> [--event-type <event>]`. 13 unit tests; full 296-test suite green. **Verified live against dev API:** Test 1 happy path returned `{message_id, correlation_id, causation_id: "reprocess:<hex>"}`; Test 2 unknown role → 400 with "add one first"; Test 3 wrong event_type → 400 listing actual events; Test 4 missing entity → 404; Test 5 missing `role` field → 400; Test 6 idempotent reprocess → distinct correlation_ids. Deployed `indemn-api` 2026-04-27. |
| **Bug #18 — Synth doesn't update `Meeting.touchpoint` back-reference** — only 5 of 67 meetings got the back-pointer set during Apr 24 backfill. Skill needs to enforce the post-create update OR the relationship needs to flip direction. Subsumed structurally by Option B. | 🔴 Open — main-session | Bug #18 | **Handed back to main session.** Fix is skill-level (Touchpoint Synthesizer behavior), not kernel. Option B (`source_entity_type` + `source_entity_id` on Touchpoint, shipped Apr 27) structurally provides the navigation the back-reference was supposed to enable, so the skill change is opt-in cleanup vs. mandatory: either enforce the post-create update in the Synth skill, or flip the relationship direction in the Email/Meeting entity definitions. Both are customer-system territory. |
| **API returns generic "Internal Server Error" body on 500 — no validation detail, no exception message.** Surfaced again 2026-04-27 during the Alliance trace: `POST /api/meetings/` returns 500 with body `Internal Server Error` for valid-looking input matching the entity schema (`{"title": "...", "date": "2026-02-01T18:02:00Z"}`). Same class as Bug #25 (Company), Bug #26 (Deal). **This is the root issue** making the create-500 family of bugs (#25/#26/Meeting) hard to diagnose — the API should return 400 with field-level validation errors, or at minimum include exception detail in the 500 response body. Likely fix in `kernel/api/registration.py` or `kernel/api/errors.py` (global exception handler). Without this, every associate hitting a create error has no way to self-correct. **Combined with the auto-generated entity skill not teaching JSON shape examples**, this means associates and humans both have to guess the right payload until they hit a working one. | 🟢 Fixed | 2026-04-27 Alliance trace; reinforces Bug #25, Bug #26 | 2026-04-27 fork session: merged to main as `cf5acd8` (feature commit `914fc61`). `kernel/api/errors.py` gains a Pydantic `ValidationError` → 400 handler (returns field-level `errors` array with loc/msg/type per error) + catch-all `Exception` → 500 handler (returns `{error, type, message}` JSON; logs full traceback; bounds message at 4096 chars to prevent runaway responses). Existing typed handlers still take precedence. 10 unit tests pass. **Deployed 2026-04-27** via `railway up --service indemn-api`. Health check passing; Meeting create 500 (next bug) is now self-diagnosable against the live API. |
| **Meeting create returns HTTP 500** — `POST /api/meetings/` with valid-shape JSON (matching entity_definitions.Meeting field types) returns 500. Tested with both the CLI (`indemn meeting create --data ...`) and direct curl. Affects the Alliance trace (no historical Meeting entities can be created for Feb 1 / Apr 8 calls without resolving). May be the same root as Bug #25 (Company) and Bug #26 (Deal); or may be Meeting-specific. Cannot self-diagnose without the broader 500-detail fix above. | 🟢 Fixed | 2026-04-27 Alliance trace | 2026-04-27 fork session: fixed by Bug #30 (declarative index reconciliation + sparse→partialFilterExpression translation). Root cause: Meeting entity has `external_ref: {unique: true}` (a nullable field), and MongoDB unique indexes treat explicit null as a value, so two manual meetings with `external_ref: null` collided. Fix: kernel now supports `sparse: true` on FieldDefinition, which translates to `partialFilterExpression: {<field>: {$type: <bson_type>}}` — excludes both null and missing. The Meeting entity def was modified via `PUT /api/entitydefinitions/Meeting/modify` to set `external_ref.sparse=true`; reconciler dropped the old non-partial unique index and recreated it with the partial filter. Verified: two manual meetings without external_ref now coexist. Same fix unblocks Bug #25 (Company create). See `artifacts/2026-04-24-os-bugs-and-shakeout.md` Bug #30 for the full diagnosis journey (initial wrong hypothesis: stale index; actual root cause: unique-on-nullable-field semantics). |
| **Auto-generated entity skill needs JSON-shape examples** — surfaced again 2026-04-27 during the Alliance trace. Reinforces `Generated entity skill teaches actual filter syntax` (existing item below) AND extends it: the skill should also emit a working example `--data` JSON payload per command. Otherwise an associate reading `indemn meeting create --data '...'` doesn't know what `'...'` should look like, must look at an existing entity (which requires `list` to return non-empty), and even then doesn't know which fields are auto-populated vs required. Combined with the 500-without-detail issue, this is what makes the create flow unworkable for autonomous associates. | 🟢 Fixed | 2026-04-27 Alliance trace; extends `Generated entity skill teaches actual filter syntax` | 2026-04-27 fork session: merged to main as `b83fa08` (feature commit `ab987c6`). `kernel/skill/generator.py` now renders a working JSON payload between `--data` quotes for both `create` (every required field with type-appropriate placeholder; ObjectId → 24-hex string, datetime → ISO 8601, enum → first allowed value; state field excluded) and `update` (representative subset of writable fields; state field excluded). 18 new unit tests pass (28 total in the file). **Deployed 2026-04-27** via `railway up --service indemn-api`. |
| **Bug #30 sparse-on-nullable-unique fix needs to be propagated to all entities with the pattern (not just Meeting)** — Bug #30's fix added `sparse: true` support on FieldDefinition + the `partialFilterExpression` translation; it was applied to `Meeting.external_ref`. But other entities have the same pattern (e.g., `Email.external_ref`, likely `Document.drive_file_id` if it's marked unique, possibly others). Surfaced 2026-04-27 during Alliance trace Step 2: `POST /api/emails/` with `external_ref: null` collided on the `org_id_1_external_ref_1` unique index. Fix: audit all entity definitions for `unique: true` fields that are also nullable, set `sparse: true` on each, run reconciler. Particularly: Email.external_ref. Could also be a one-time migration that auto-detects and fixes the pattern across all entities. | 🟢 Fixed | 2026-04-27 Alliance trace Step 2 | 2026-04-28 fork session: merged to main as `db23749` (feature commit `2948491`). Skipped the audit-and-update-each-def approach in favor of a kernel-level prevention. The reconciler now AUTO-EMITS `partialFilterExpression` for any field where `unique=True AND not required AND default is None`. New helper `_is_effectively_nullable(fdef)`. Operators no longer need to remember `sparse: true` on every nullable+unique field — the kernel takes a stand: `unique: true` on an `Optional` field means "unique-when-set", full stop. The "null counts as a value for uniqueness" semantic was MongoDB's surprising default and never what anyone wanted. **Healing on deploy** (logged at indemn-api startup): 2 latent create-500 traps auto-fixed: `deals.deal_id` and `emails.external_ref` both went from `unique=True, partialFilter=None` to `unique=True, partialFilter={field: {$type: <bson_type>}}`. 4 new unit tests + existing tests updated to assert the new contract; full 320-test suite green. **Prevents future Bug #30-class incidents entirely** — every nullable+unique field is correctly indexed by default. |
| **Stale entity-skill rendering: skills aren't auto-regenerated when the generator changes** — surfaced 2026-04-27. After the JSON-example generator landed (Bug above, 🟢 Fixed), only entities whose definitions were touched after the deploy got re-rendered skills. Older entities (Contact `updated_at: 2026-04-18`, Touchpoint, etc.) still serve their pre-fix skill content. Associates reading those skills don't get the new examples. Fix: on generator deploy, run a migration that re-renders all skills for all entity definitions. Or: on every `indemn skill get`, re-render from current entity definition rather than serving cached content. Latter is safer + always fresh. Affects every quality improvement to the generator going forward. | 🟢 Fixed | 2026-04-27 Alliance trace Step 1 (Contact + Touchpoint skills lack JSON examples even after generator fix landed) | 2026-04-28 fork session: merged to main as `e83a17d` (feature commit `2f163b1`). Picked the always-fresh-on-GET option (safer + always fresh). New `_refresh_entity_skill(skill)` helper called inside `get_skill`, `get_skill_by_name`, and `list_skills`. For `type="entity"` it looks up the current EntityDefinition by `(name, org_id)` and re-renders via `generate_entity_skill` — the stored Skill row becomes a cache, not source of truth. The EntityDefinition IS the source of truth and the generator IS the rendering function. Hash recomputed alongside content only when content actually changes (no spurious churn). Associate skills (`type="associate"`) still serve stored content with content_hash tamper detection — short-circuited so authored content can never be phantom-overwritten. Fallback: if the EntityDefinition was deleted but the Skill record persists, serve stored content (data-integrity edge case). Org isolation pinned by test (lookup uses skill's own org_id, not the request contextvar). 6 unit tests; full 316-test suite green. **Verified live:** Contact skill (`updated_at: 2026-04-18` — never touched after generator improvements) now serves 2121-char content including JSON `--data` create example (post b83fa08), the Reading section (post f4fc121), `--include-related` docs (post f4fc121). Every future generator improvement now propagates to every entity instantly. |
| **Email entity field still references `Interaction` instead of `Touchpoint`** — surfaced 2026-04-27. The Apr 23 rename (Interaction → Touchpoint) updated the kernel + most entities, but `Email.interaction` (objectid → Interaction) still exists in the entity definition. The `interactions` collection also still exists in MongoDB. Fix: update Email entity definition to rename `interaction` → `touchpoint` with `relationship_target: "Touchpoint"`; either drop the `interactions` collection or redirect it. May affect other entities too — audit needed. | 🟢 Fixed | 2026-04-27 Alliance trace Step 2 + 2026-04-28 Phase B1 trace surfaced it again on the Touchpoint Synthesizer's back-reference step | 2026-04-28 (this session): two-step fix — (1) `indemn entity migrate Email --rename "interaction touchpoint" --no-dry-run` renamed the field across 1139 Email documents + the EntityDefinition's field key. (2) `PUT /api/entitydefinitions/Email/modify` with `modify_fields` updated `relationship_target` from `Interaction` to `Touchpoint`. Verified end-to-end by setting `Email.touchpoint` on a real email and reading it back. **Audit:** scanned all 26 entity definitions — only Email had the leftover. **`interactions` collection in MongoDB:** kept (it's the kernel chat/voice session log — different entity, not customer-system Touchpoint, IDs do not overlap with `touchpoints`). 23 docs in there are kernel data we don't touch. Customer-system entity work, not OS/kernel. |
| **Slack ingestion is foundational, not a deferrable Phase F** — surfaced repeatedly during Alliance trace 2026-04-27. The Capability Document, the Apr 7-8 Retention Associate / compound-outcome thread, and the Apr 21 mpdm Cam packaging — all are Slack-sourced Touchpoints carrying load-bearing analytical content. Without Slack ingestion, customer-system loses the internal-thinking layer of the timeline. Roadmap.md updated to move Slack from deferred Phase F to early Phase B. New work needed: Slack adapter (channels + DMs + MPDMs); Document.source enum to add `slack_file_attachment`; backfill mechanism for historical Slack content. Same priority class as the email Gmail adapter that already exists. | 🟢 Done (Session 14-15) — Slack adapter built, 90-day backfill drained (860 msgs), Slack-Fetcher active at cron */5 | 2026-04-27 Alliance trace Steps 3, 5, 7 | **Handed back to main session.** Cross-cutting (kernel adapter + customer-system integration setup + Slack channel/DM mapping decisions). The kernel adapter half is potential fork work, but the customer-system half (which channels are customer-relevant, how to authorize the Slack integration entity, MPDM membership rules) drives the design. Main session owns the integration setup; fork can pick up the kernel adapter once the design is anchored. |
| **Document.source enum missing `slack_file_attachment`** — surfaced 2026-04-27. Document.source today only allows google_drive, email_attachment, manual_upload. The Capability Document came from a Slack file attachment and had to be created with source=manual_upload as a workaround. Fix: extend the enum + update the Touchpoint Synthesizer skill + the (forthcoming) Slack adapter to use the new value. | 🟢 Done (Session 14) — enum extended, TD-1 done-test verified | 2026-04-27 Alliance trace Step 3 | **Handed back to main session.** Entity-definition update (customer-system `Document` is a domain entity, not kernel). Trivial change (`indemn entity modify Document --add-enum-value slack_file_attachment`) but coupled to the Slack ingestion work above. |
| **Proposal state machine lacks transition path to `superseded`** — surfaced 2026-04-27. The Proposal lifecycle has `superseded` as a defined state but no transitions defined to reach it. When v2 was generated today, v1 had to be transitioned to `rejected` (the only available terminal state from `under_review`) — but v1 isn't really rejected, it's superseded by v2. Fix: add transition `under_review → superseded`, `sent → superseded`, possibly `accepted → superseded` (in case of re-engagement). Plus enable Proposal entity to carry a `supersedes` self-reference field so v_next ↔ v_prior is queryable. Affects every multi-version proposal workflow. | 🔴 Open — main-session | 2026-04-27 Alliance trace Step 8 | **Handed back to main session.** Entity-definition update on the Proposal domain entity (state machine transitions + new `supersedes` self-reference field). Customer-system territory; Alliance v2 already exposed the gap. |
| **Bug #10 backfill mechanism continues to bite** — surfaced again 2026-04-27 during Alliance trace. The Feb 1 Meeting + the Apr 8 Meeting are both past-30-day-window — even if Apollo/Granola had captured them, the OS lost the ability to ingest them. The trace had to manually reconstruct from secondary sources (Slack auto-summaries, Apr 25 handoff). Reinforces: `indemn meeting reprocess <id>` + an OS pattern for "copy transcripts into Document at ingestion time so they survive the source's retention window." | 🟡 Partial — `reprocess` CLI shipped (Bug #10 above 🟢); transcript-retention-at-ingestion-time still open | 2026-04-27 Alliance trace Steps 1, 6 | The `reprocess` half is fixed (`662dc2d`). The "copy transcripts into Document at ingestion time so they survive 30-day retention" half is still open — no kernel work yet. Separate ingestion-side change (Gmail/Meet adapters) — file under "ingestion durability" when picked up. |
| **Touchpoint scope determination has chicken-and-egg with Contact resolution** — surfaced 2026-04-27. Touchpoint Synthesizer needs to set scope=external/internal based on whether participants are Contacts (external) or Employees (internal). But at first encounter, those Contacts don't exist yet — they get created as part of the same Touchpoint creation cascade. Either the Synthesizer must default to external when participants are unknown (likely customer-side) and revise later, OR the participant-resolution must happen before Touchpoint creation. Worth designing the order explicitly in the Synth skill. | 🔴 Open — main-session | 2026-04-27 Alliance trace Step 1 (gap A1.6) | **Handed back to main session.** Skill design (Touchpoint Synthesizer ordering of operations: contact-resolve-then-create vs create-and-revise). Customer-system territory; informed by the B1 trace work the main session is running. |

### Medium / ergonomics + observability

| Item | Status | Source | OS-side artifact / commit |
|---|---|---|---|
| **CLI debugging gap — `indemn` lacks first-class diagnostics for stuck cron_runner runs; operators reach for raw mongo + Railway log filters + `time` instead** — surfaced 2026-05-04 mid-session debugging Email Fetcher chronic stuckness. The OS vision is "the CLI is the interface"; in practice the entire investigation reached for non-CLI tools because the CLI's diagnostics are too coarse. **Specific gaps observed:** (a) `indemn trace cascade <correlation_id>` shows the message claim event but NOT the cron_runner OTEL span timings (Bug #49 added `cron_runner.run` span with `result.fetched|created|skipped_duplicates|errors_count|outcome` — but only Grafana surfaces it). (b) No CLI for "show me the last N cron runs for actor X with their durations + outcomes." Today: query Mongo `message_log` directly. (c) No CLI for "show me visibility_timeout history on this message." Today: query Mongo `message_queue` directly. (d) `indemn message <id>` doesn't exist — only `indemn queue dead-letter`, `indemn queue retry <id>`, `indemn queue complete/fail/extend-visibility <id>`. To inspect a specific in-flight message you query Mongo. (e) No CLI for "show me OTEL spans for this entity" (Grafana only). (f) No CLI for "list runtime processes claiming this role" (multi-pod observability). (g) `INDEMN_SERVICE_TOKEN` env-var auth pattern works (per `indemn_os/src/indemn_os/client.py:19` resolution order) but isn't documented in `indemn auth --help` — every operator hits `Error 401` and reaches for `indemn auth login --password` before discovering the env-var path. **Effective impact:** a 30-minute investigation that should have been 5 minutes if the CLI surfaced what's already in OTEL + the kernel; reached for `mongosh` + `railway logs --build/deployment` + `aws secretsmanager get-secret-value` + `time` instead. Trace-as-build discipline says "anything the OS does, you should be able to do via CLI first" — diagnostics are part of "what the OS does" and they're behind the curve. **Suggested fix shape:** new `indemn diagnose` command group: `indemn diagnose actor <id>` (recent runs + durations + outcomes), `indemn diagnose message <id>` (full lifecycle including visibility_timeout history), `indemn diagnose cron <actor_name>` (last N cron ticks with durations + bottleneck breakdown from OTEL spans). Plus document the `INDEMN_SERVICE_TOKEN` env var in `indemn auth --help` so operators don't get stuck on 401 + don't have to ask Craig for password. | 🟢 Fixed (Session 18) — `indemn diagnose` command group shipped: `actor <id>`, `message <id>`, `cron <name>`. API endpoints at `/api/_diagnose/*`. Auth help docstring updated with INDEMN_SERVICE_TOKEN. 12 unit tests. Remaining gaps (OTEL span query from CLI, multi-pod listing) are lower priority follow-ons. | 2026-05-04 mid-session: 30-min email fetcher debug used mongo + railway + aws + time. Should have been 5 min via CLI. | TBD — incremental: each `indemn diagnose <thing>` is its own small fix. Total scope: maybe 200 lines + ~10 tests. Plus the auth-help docstring change is one line. |
| **Bug #12 (REFRAME) — `mongodb-uri` AWS Secret was NEVER regressing; our local mongosh wrapper assumed a non-private-link host** — Sessions 15 + 16 both "re-fixed" Bug #12 by overwriting the secret with `dev-indemn.mifra5.mongodb.net` (publicly resolvable from local laptops). The Railway platform deploy process correctly restored `dev-indemn-pl-0.mifra5.mongodb.net` (private-link host that resolves only inside the AWS VPC where indemn-api runs). Per Craig 2026-05-04: "the mongo DB in the secret is needed. its just us that needs to use this other value." The shared platform secret is correct; our local-only diagnostic tooling needs a different URI. The "regression" was the auto-restore doing its job. **Effective real bug:** `scripts/secrets-proxy/mongosh-connect.sh` reads the platform secret directly. Should read a separate local-friendly URI or do the host-swap inline so Sessions 15+16-style well-meaning "fixes" don't break the platform. | 🟢 Fixed | 2026-05-04 mid-session: Craig pointed out the platform NEEDS the private-link host; auto-restore is correct behavior, not a regression | 2026-05-04 same session: `scripts/secrets-proxy/mongosh-connect.sh` updated to do an inline `-pl-0` → `` host swap (`LOCAL_URI="${URI/-pl-0/}"`). Pattern catches both `dev-indemn-pl-0` → `dev-indemn` and `prod-indemn-pl-0` → `prod-indemn`. Comment block in the script explicitly documents the rationale + tells future operators NOT to "fix" this by writing the public host back to the shared secret. **Verified:** `mongosh-connect.sh dev indemn_os --eval 'db.runCommand({ping:1})'` returns `ok: 1`. Original Bug #12 "fix" history rows above remain (audit trail for the misframing). |
| **Bug #39 — Domain entity `--collection-name` is silently ignored by API route generation** — surfaced 2026-04-30 Session 14 creating ReviewItem entity. `indemn entity create ReviewItem --collection-name review_items ...` accepts the value (entity definition's `collection_name` field shows `review_items` in GET response) AND MongoDB stores the docs in the `review_items` collection. But the auto-generated FastAPI route is at `/api/reviewitems/` — derived via `inflect.engine().plural(name.lower())` (Bug #15 fix in `kernel/cli/app.py`) — and `/api/review_items/` returns 404. The CLI's `indemn reviewitem ...` works correctly because it builds URLs using the same inflect pluralization. **Effective bug:** explicit `--collection-name` is a half-honored override — MongoDB respects it, route generation doesn't. | 🟢 Fixed | 2026-04-30 Session 14 (ReviewItem creation) | 2026-05-01 Session 15: indemn-os main commit `00b7407` (`fix(api): route generation honors explicit --collection-name override — Bug #39`). New `_route_slug_for(entity_name, entity_cls)` helper resolves: (1) `entity_cls._collection_name` (set by EntityDefinition factory when operator passed `--collection-name`), (2) `entity_cls.Settings.name` (kernel Beanie entities, e.g. Actor → "actors"), (3) `entity_name.lower() + "s"` (last-resort fallback). 5 unit tests in `tests/unit/test_route_slug_resolution.py` pin each branch. Full unit suite 456 pass (was 451). Deployed via `railway up --service indemn-api`. |
| **Bug #47 — chat-deepagents Railway env `INDEMN_SERVICE_TOKEN` was stale (didn't match AWS Secrets Manager value)** — surfaced 2026-05-01 Session 15 attempting chat-side log-touchpoint test. Service was returning 502 for an unknown duration before this session. Railway env had `indemn_zg-7hVO...` but AWS Secrets Manager `indemn/dev/shared/runtime-chat-service-token` had `indemn_XilsQcCAes0YV...` (which is the working production value, also matches async-service-token AWS Secret). Service was failing startup with `Error 401: Invalid service token` on the `register-instance` call → app exit. **Fix Session 15:** `railway variables --service indemn-runtime-chat --set INDEMN_SERVICE_TOKEN=<AWS-Secrets-value>` + `railway redeploy`. Service came back healthy in ~30s. **Lesson:** treat AWS Secrets Manager as source-of-truth for service tokens; Railway env should mirror, not diverge. Future hardening: a CLI command or pre-deploy check that diffs Railway env vs AWS Secrets and warns on drift. | 🟢 Fixed | 2026-05-01 Session 15 chat-side log-touchpoint test attempt | 2026-05-01 Session 15: Railway env update + redeploy. No code change. |
| **Bug #40 — No deterministic scheduled-actor execution path for trivial CLI runs (forces mode=hybrid + LLM)** — surfaced 2026-04-30 Session 14 attempting Email-Fetcher activation per TD-1 design. Roadmap.md TD-1 specifies: "one actor per source, **deterministic mode (no LLM)**, trigger_schedule cron." But the kernel/harness today has no path to execute a scheduled actor's CLI command without invoking the deepagents agent (LLM) — `mode=deterministic` only knows about Rules (`set_fields`, `force_reasoning`), neither of which fits "run `indemn email fetch-new`." Workaround used: created Email-Fetcher with `mode=hybrid` + a trivial skill saying "execute fetch-new and report." This works (when the runtime is healthy) but burns LLM tokens for every cron fire — at 5min cadence × 3 fetchers × 24h ≈ 800 LLM calls/day for what should be a deterministic shell exec. | 🟢 Fixed | 2026-04-30 Session 14 (Email-Fetcher activation; roadmap.md TD-1 design intent) | **2026-05-01 Session 16: chose Option A (`cron_runner` actor mode) over Option B (new `ScheduledActorWorkflow` peer).** Rationale: Option A extends the existing actor.mode primitive (`deterministic | reasoning | hybrid | cron_runner`), uses Bug #41's existing harness foothold (`_load_message_context` already branches on `_*` synthetic types), routes through unchanged dispatch path. Option B would have been ~2x the work for purely aesthetic separation with no observable runtime difference. Per CLAUDE.md "no abstraction for abstraction's sake." **Implementation:** `kernel_entities/actor.py` adds `cron_runner` to mode Literal. `harnesses/async-deepagents/cron_runner.py` (NEW, ~200 LOC): `parse_command_from_skill` extracts argv from `## Command` section / fenced bash block, restricts to `indemn` CLI (no arbitrary shell), single-command-only. `run_cron_skill` validates trigger is synthetic `_*`, loads first skill via CLI, parses command, exec via existing `harness_common.cli.indemn()` helper (env-var propagation INDEMN_EFFECTIVE_ACTOR_ID + INDEMN_CAUSATION_MESSAGE_ID intact), inspects JSON `errors` field (non-empty → fail), marks queue complete/fail. `harnesses/async-deepagents/main.py::process_with_associate` branches on `actor.mode == "cron_runner"` BEFORE building the agent — skips deepagents entirely. **22 unit tests** in `harnesses/async-deepagents/tests/test_cron_runner.py` (parser shape × 11, executor happy + 7 failure modes × 8, sync-helper pin, mode literal pin via Pydantic field annotation introspection). Plus `tests/test_load_message_context.py` stub-list update for `harness.cron_runner`. Full kernel + harness suite 524/524 (1 pre-existing pollution failure on `test_effective_actor_id` orthogonal). **Skills:** all 4 fetcher skills rewritten to cron_runner shape (`## Command` section + single-line `indemn <entity> fetch-new` + `## Why this exists` rationale); pushed to dev OS as v3 each. Files exist in repo at `projects/customer-system/skills/{email,meeting,drive,slack}-fetcher.md` (3 NEW files; slack-fetcher.md updated). **Deployment:** indemn-api + indemn-queue-processor + indemn-temporal-worker (Pydantic Literal change) + indemn-runtime-async (cron_runner module) all redeployed. Initial runtime deploy hit `ModuleNotFoundError: No module named 'cron_runner'` due to `from cron_runner import` (the repo-local form); fixed to `from harness.cron_runner import` (the `/app/harness/` package layout in container) and redeployed. **Live verification 2026-05-01 19:59:58Z (flip) → 2026-05-04 (3 days post-flip):** all 4 fetchers (Email-Fetcher, Meeting-Fetcher, Drive-Fetcher, Slack-Fetcher) flipped to `mode=cron_runner` via `PUT /api/actors/<id> --data '{"mode": "cron_runner"}'`. cron_runner exec + success log lines visible in runtime logs for all 4. **Cumulative health 3 days post-flip: 1863 cron_runner completions across 4 fetchers (Email 770, Meeting 259, Drive 65, Slack 769); 11 dead_letter (all from Bug #38's orphan-workflow cleanup — runtime restarts); 0 LangSmith traces for any of the 4 fetcher associates since 20:01:00Z (LLM-free 100% of the time).** Bug #40 cost-reduction objective (~1000 LLM calls/day eliminated) delivered. **Surfaced during this work, fixed in same session: Bug #48** (CLI URL slug regression — separate row below). **Related:** Slack-Fetcher's first cron_runner exec failed 404 due to Bug #48's CLI URL bug + Slack Integration was at status=error from earlier transient failure. Cron_runner correctly detected + reported error via WARNING log + queue fail (proper error path validated). Indemn-os main commits pending push: cron_runner (kernel actor.py + harness cron_runner.py + main.py + tests) and the Bug #48 follow-on. |
| **Bug #43 — Drive adapter scope understated in TD-1; original "exists, needs systematic ingestion wired" was wrong** — surfaced 2026-04-30 Session 14 attempting Drive-Fetcher activation. The TD-1 roadmap entry implied the adapter was ~80% done. **Actual state at session start:** `kernel/integration/adapters/google_workspace.py` had only `_drive_service(user_email)` — a 7-line helper. NO `fetch_documents()`, NO Document creation, NO watermark, NO file-type handling. **Resolution Session 14 (in-session, NOT deferred — per Craig's "this is the roadmap"):** built it inline. (a) Added `external_ref` + extended Document entity; (b) wrote `fetch_documents()` on GoogleWorkspaceAdapter (per-user iteration via DWD, paginate by pageToken, dedup by drive_file_id, format dicts with name/mime_type/drive_url/file_size/created_date/source/external_ref); (c) activated `fetch_new` capability on Document with `fetch_method=fetch_documents`; (d) 6 unit tests in `TestFetchDocumentsContract` — all GREEN; full unit suite 437/437. Deployed via `railway up --service indemn-api`. Live-verified end-to-end: 30-day backfill across 11 users returned `fetched=1161 created=493 skipped=668 errors=0`. Drive-Fetcher actor (id `69f3abbe268936150b46a0fa`, hourly cron) created + activated. **File-type-specific content extraction** (Google Docs/Sheets export, PDF text extraction, image metadata-only) deferred to follow-up — current implementation populates metadata + drive_url (operators can fetch content via the URL). | 🟢 Fixed (TD-1 sub-piece 5 complete) | 2026-04-30 Session 14: `grep -n "def " kernel/integration/adapters/google_workspace.py` showed only `_drive_service` for Drive. End-state: `fetch_documents` + `_format_drive_doc` methods + 6 tests. | 2026-04-30 Session 14 in-session work; commit pending end-of-session push. **Follow-up:** content-extraction enrichment pass (download + text-extract for Google Docs/Sheets/PDFs) is its own enhancement, not blocking ingestion. Suggest a separate `enrich_documents` capability or content-extraction skill on the Drive-Fetcher cron. |
| **Bug #44 — voice-deepagents harness fully canonical + Railway-deployed end-to-end Session 15.** Original v1 (Session 14, commit `62f47f9`) was architecturally wrong — used `livekit.agents.Agent` with single custom `execute` tool. v2 (Session 15) mirrors chat-deepagents: deepagents `create_deep_agent` + harness_common (Interaction lifecycle, Attention with 30s heartbeat, runtime registration, backend factory) + three-layer LLM config + LiveKit-as-I/O-only via DeepagentsLLM adapter. Deployed to Railway service `indemn-runtime-voice` (id `df2349d6-0939-43e3-9261-87dfaf70e6ec`); end-to-end multi-turn voice round-trip created real Touchpoint `69f4ed4f03e56394d808bc88` (Branch + Dan Spiegel + summary). Voice OS Assistant actor (id `69f4c62d03e56394d808b79c`) bound to voice runtime. Pre-v1 framing — Session 14 (revised): originally logged with the framing "voice-deepagents harness does NOT exist." Per Craig's "we are implementing EVERYTHING to completion" + "We already use LiveKit and have livekit self hosted on a GPU," **harness was built end-to-end this session.** Code shipped at `harnesses/voice-deepagents/` in indemn-os main commit `62f47f9` (Drive + Slack adapter commits = `c87376d`; voice = `62f47f9`). 522 lines across 7 files: tools.py (execute CLI wrapper, restricted to `indemn` commands), assistant.py (IndemnVoiceAssistant Agent class with system prompt directing skill load on turn 1), main.py (LiveKit Agents WorkerOptions entrypoint — Deepgram STT + gemini-3-flash-preview LLM + Cartesia TTS + Silero VAD + EnglishModel turn detector), pyproject.toml (livekit-agents 1.x + plugins), Dockerfile (Python 3.12 + ffmpeg/libsndfile/libportaudio2 + indemn-os CLI + VAD/turn-detector models pre-cached at build), tests/test_tools.py (6 tests pinning execute contract). Same architectural pattern as voice-livekit (Indemn's customer voice product) — same plugins, same framework, same vendor relationships (Deepgram + Cartesia, no new vendors). Tool surface is the `indemn` CLI, symmetric with async-deepagents (commit `7281b83`'s skills-via-CLI pattern). LLM is gemini-3-flash-preview/global, matching async runtime default and benefiting from Bug #42's resolution. **OS state created this session:** Runtime entity `voice-deepagents-dev` (id `69f3b7fc97300b115e7236a0`, kind realtime_voice, framework livekit, llm_config gemini-3-flash-preview/global), service token at AWS Secrets `indemn/dev/shared/runtime-voice-service-token`. **Per-actor default_assistant deferred per Craig's call: shared OS Assistant is fine for now.** **Remaining for live voice (Craig actions, NOT a code gap):** (1) inject LIVEKIT_URL/key/secret + DEEPGRAM_API_KEY + CARTESIA_API_KEY + GCP_SERVICE_ACCOUNT_JSON + INDEMN_SERVICE_TOKEN into a deployment env (Railway service or run on existing GPU instance alongside voice-livekit); (2) `railway up --service indemn-runtime-voice` (or equivalent Docker run on the GPU box); (3) optionally clone OS Assistant onto the voice runtime as a "Voice OS Assistant" actor for testing the dispatch path. Full deployment runbook at `projects/customer-system/artifacts/2026-04-30-voice-deepagents-runbook.md`. **Per-actor default_assistant pattern still pending** — kernel sweep that auto-provisions a `default_assistant` associate per human Actor on create. Per CLAUDE.md design but Craig confirmed shared OS Assistant is sufficient for TD-1; future work when team-adoption forces it. | 🟢 Fixed end-to-end Session 15 (deployed + verified); 🔴 default_assistant pattern deferred | 2026-04-30 Session 14 inline build per Craig's "we are implementing EVERYTHING to completion."; 2026-05-01 Session 15 v2 rebuild + Railway deploy + multi-turn round-trip. | 2026-04-30 Session 14: indemn-os main commit `62f47f9` (v1 wrong shape, deleted Session 15). 2026-05-01 Session 15: indemn-os main commits `6671dea` (v2 canonical rebuild) + `a35913f` (follow-ups) + `0e1fff4` (bootstrap + workspace fallback + agent_name dispatch) + `33d12f7` (sys.executable for indemn) + `27e9304` (LangSmith metadata) + `fcdd8f8` (Dockerfile CMD start) + `cdc8479` (drop turn_detector). Plus chat + voice CLI-only skill loading migration `7197f08`. Railway service `indemn-runtime-voice` deployed. Voice round-trip Touchpoint `69f4ed4f03e56394d808bc88`. Runbook v2 at `projects/customer-system/artifacts/2026-04-30-voice-deepagents-runbook.md` (rewritten Session 15). **Per-actor default_assistant** TBD — kernel sweep + Actor lifecycle hook is future work; shared Voice OS Assistant covers TD-1. |
| **chat-deepagents harness — propagate CLI-via-prompt skill loading** — done Session 15. Voice migrated alongside (was using same filesystem-skills pattern). | 🟢 Fixed | 2026-04-29 Session 12 architectural decision after Bug #35 closeout (Craig: "we should also do this implementation for the chat deep agents") | 2026-05-01 Session 15: indemn-os main commit `7197f08` (`refactor(harnesses): chat + voice load skills via CLI in DEFAULT_PROMPT — drop deepagents skills layer`). Mirrors async-deepagents `7281b83`. New `build_system_prompt(associate)` helper composes per-associate system prompts with `execute('indemn skill get <ref>')` directives. Drops `_write_skills_to_filesystem` from both harnesses' session.py. 8 new unit tests in `chat-deepagents/tests/test_agent.py`. Eliminates the Bug #35 class entirely from chat + voice. Verified live: chat-side log-touchpoint test created Touchpoint `69f4f2ca03e56394d808bd6d`; voice multi-turn created `69f4ed4f03e56394d808bc88`. |
| **Bug #25 — `company create --data` returns HTTP 500** — opaque error blocks programmatic / scripted Company creation. Hit when re-creating Amynta after accidental delete. | 🟢 Fixed | Bug #25 | 2026-04-27: subsumed by Bug #30 (declarative index reconciliation + sparse → partialFilterExpression). Same root cause as the Meeting create 500: unique index on a nullable field treats explicit null as a value. Bug #30's auto-emit `partialFilterExpression` (Apr 28 merge `db23749`) ensures any nullable+unique field uses the partial filter automatically. (Status row was stale 🔴 in os-learnings.md until 2026-04-28 fork-session cleanup; bugs artifact summary table had been updated but this row hadn't.) |
| **Bug #26 — `deal update --data` returns HTTP 500 when updating company ref** — same pattern as #25; blocks repointing a Deal at a different Company. | 🟢 Fixed (auto-healed by burst #4) | Bug #26 | 2026-04-28 fork pre-flight code-read: `_coerce_objectid_fields` in `kernel/api/registration.py:337` (update path) handles string ObjectId → bson.ObjectId for `Optional[ObjectId]` fields; `factory.py` maps `"objectid"` field-type to `ObjectId`. Path exists for the canonical shape `--data '{"company": "<24-hex>"}'`. Bug #25 (filed alongside #26 with same hypothesis) was actually a unique-on-nullable-index trap fixed by Bug #30. Bug #28's auto-emit `partialFilterExpression` (Apr 28 merge `db23749`) auto-heals every nullable+unique field at deploy time including any latent traps on Deal. **Verified live 2026-04-28:** `PUT /api/deals/69e79a31f0d2c6b73f885a32 --data '{"company": "<existing-hex>"}'` returns HTTP 200 with the deal entity. No 500. Same code path as Bug #25 — the auto-heal pattern from burst #4 covers Deal as well as Company / Email / Meeting. |
| **Bug #27 — `created_by` is null on every Company record** — `save_tracked()` should auto-populate `created_by` from `current_actor_id` on insert. Forensic gap on the entity itself. | 🟢 Fixed | Bug #27 | 2026-04-28 fork session: merged to main as `0b5a484`. New `_resolve_created_by(actor_id)` helper in `kernel/entity/save.py` returns `current_effective_actor_id.get() or actor_id` (prefers the associate the harness is acting as — same convention as Bug #22's effective_actor_id field on the changes collection — falls back to the authenticated session actor for human-driven mutations). `save_tracked_impl` calls it on insert when `entity.created_by is None`, so caller-provided values (seed data, migrations) stay. Only on insert — updates do NOT rewrite history. **Verified live:** new Touchpoint `69f127924f26262990e301cf` created via `indemn touchpoint create` had `created_by: 69e23d586a448759a34d3824` (Platform Admin actor, no effective_actor — exactly the human-driven fallback). 5 unit tests; full 370-test suite green. Existing entities with `created_by: null` are NOT backfilled (would need to touch every entity AND every change record's hash chain). **Deployed** via `railway up --service indemn-api` + `--service indemn-temporal-worker`. |
| **Bug #19 — Change records occasionally have non-Date `timestamp` fields** — bulk operations or some code paths write strings. Should always be server-side `datetime.now(UTC)`. | 🟢 Fixed (going forward) | Bug #19 | 2026-04-28 fork session: merged to main as `cd07fb8` (feature commit `7e72756`), deployed via `railway up --service indemn-api`. **Root cause was different and worse than bug filing implied.** Top-level `timestamp` is and always was Date — verified via mongo scan of all 56,485 records, all uniformly `BSON Date`. The actual symptom was on `changes[].old_value` / `new_value` sub-fields, but tracing back: those mismatches come from the entity's `date` field itself being stored as a STRING in MongoDB. Live evidence: 2 entities (Touchpoint `69ea7f70…74f3f`, Email `69efc96a…83e9`) had `date` stored as `"2026-04-21T19:08:15"` / `"2026-02-11T22:00:00"` — strings, not ISODates. **Real bug:** PUT `/api/{entity}/{id}` updates fields via `setattr(entity, key, value)` which doesn't trigger Pydantic validation; a JSON payload `{"date": "..."}` slipped through to MongoDB as a string. POST went through Pydantic `__init__` and coerced; PUT didn't. Sort/filter on datetime fields silently misbehaves in that state (string sort != datetime sort), AND the changes hash chain accumulates type-mismatch noise. Fix: new `_coerce_datetime_fields` peer to `_coerce_objectid_fields` in `kernel/api/registration.py` walks Pydantic field annotations, finds datetime / Optional[datetime] / list[datetime] fields, parses string values via `fromisoformat` (trailing `Z` → `+00:00`). Wired into both create and update paths. Bad strings → `HTTPException 400` with field name + offending value. **Verified live (affirmative path):** PUT `{"date": "2026-02-11T22:00:00"}` on the Email returned 200; mongo confirms `date` now stored as `ISODate('2026-02-11T22:00:00.000Z')`; `_compute_changes` correctly detects no logical change because the loaded entity's date had already been Pydantic-coerced to datetime in-memory on read — so the persistence-as-Date is a side-effect of save_tracked re-writing the typed entity. **Verified live (negative path):** PUT `{"date": "definitely-not-iso-format"}` returns HTTP 400 with `"Field 'date' expects ISO 8601 datetime, got 'definitely-not-iso-format'. Parse error: ..."`. 12 new unit tests; full 353-test suite green. **Persisting historical state — by design:** the 2 pre-fix string-typed sub-field records (Email `69f0df1abca2d725ce90ed57` Apr 28 16:23, Touchpoint `69eaa1cbff375a32fa25a96d` Apr 23 22:48) remain. Both carry `current_hash` + `previous_hash` and ARE part of the SHA-256 audit chain. Mutating them in place to normalize the type would invalidate `current_hash` and cascade-break every subsequent record's `previous_hash` in the org chain — defeating the tamper-evidence purpose. Going forward no new such records get created; the historical records are honest state. **Self-inflicted Touchpoint corruption (`69ea7f70…74f3f` `date: "not-a-real-date"`) was repaired 2026-04-28 via direct mongo write** to `ISODate('2026-04-21T19:08:15.000Z')` (the original pre-corruption value). Entity now loads cleanly via API (verified `GET /api/touchpoints/<id>` returns 200). **Self-inflicted complication:** during deploy verification I sent a deliberate `"not-a-real-date"` PUT to Touchpoint `69ea7f70…74f3f` against the OLD instance during a Railway rolling deploy (returned 200 then). The Touchpoint's `date` is now stuck as `"not-a-real-date"` and Pydantic can't load the entity from disk. Repair requires direct mongo write (set back to `ISODate('2026-04-21T19:08:15.000Z')` — the original pre-corruption value). Awaiting Craig authorization on that single-line cleanup. |
| **Bug #20 — Actor CLI inconsistent with auto-generated entity CLIs** — `indemn actor` has `create/list/get/update/add-role/add-auth` but no `transition`, `delete`, `bulk-*`. Documented workflows reference `indemn actor transition <id> --to suspended` which doesn't work. Workaround: curl the API. Also: `indemn actor list --type` filter is silently ignored (Bug #28). | 🟢 Fixed | Bug #20, Bug #28 | 2026-04-28 fork session: merged to main as `e9d04da` (feature commit `008efcd`). Added `actor transition <id> --to <state> [--reason ...]` (posts to auto-registered `/api/actors/{id}/transition`) + `actor delete <id> [--yes]` (routes through `bulk-delete` with single-_id filter — same audit + watch-evaluation path as every other delete; the Bug #23 operator-safelist fix made `$oid` coercion reliable). Confirmation prompt steers operators toward `transition --to suspended` for retired actors. **Verified live:** active→suspended→active round-trip on the `OS Assistant` associate succeeds with status reflected in subsequent GETs. **Bug #28 fix:** `actor list --type associate` was sending `?type=...` as a top-level query param the auto-list endpoint ignored; switched `--type` and `--status` to flow through the standard `?filter={...}` safelist (Bug #23) so they actually filter. **Verified live:** filter returned 4 associates (only `type=associate`) instead of 23+ all actors. |
| **Bug #21 — Transition API field naming inconsistent: `to` vs `target_state`** — docs reference `target_state`, API expects `to`. CLI's `--to` flag works because it's mapped internally; users curl-ing the API hit the mismatch. Pick one and sync. | 🟢 Fixed | Bug #21 | 2026-04-28 fork session: merged to main as `e9d04da` (feature commit `008efcd`). Picked `to` as canonical (matches the CLI's `--to` flag, shorter, and what the API has always actually accepted). Updated `docs/guides/adding-entities.md:135` to use `to` with an inline note explaining the rename so anyone landing on it from an older source sees the correct shape. No back-compat alias added — `target_state` never worked, so nothing in the wild is scripted against it. **Verified live:** POST to `/api/actors/<id>/transition` with body `{"target_state": "suspended"}` still returns `400 "'to' state is required"`, confirming `to` is canonical and there's a clear error if someone uses the old field name. |
| **Generated entity skill teaches actual filter syntax** — `indemn skill get <Entity>` lists fields and state transitions but doesn't teach how to filter lists or navigate relationships. Agents improvise (e.g. `--filter` which doesn't exist) and fail. Fix: `kernel/skill/generator.py` should emit concrete CLI recipes once Capability #2 (arbitrary field filters) lands. | 🟢 Fixed | `artifacts/2026-04-24-extractor-procedure-and-requirements.md` § Capability #3 | 2026-04-27 fork session: merged to main as `f4fc121` (feature commit `6af2166`). Skill now teaches `--status`/`--search`/`--limit`/`--offset`/`--sort` recipes, `--depth N --include-related` for forward navigation, ObjectId-as-hex-string warning when relationship fields present, AND fixes Bug #6 (collection-level capabilities like `fetch_new` no longer emit `<id>` and `--auto`). Centralized `COLLECTION_LEVEL_CAPABILITIES` in `kernel/capability/__init__.py`. 18 unit tests pass. **Deployed 2026-04-27** via `railway up --service indemn-api`. (Full filter teaching extends when Capability #2 lands; JSON-shape example payloads still pending — see new High-section item.) |
| **Bug #5 — `fetch-new --help` is unhelpful** — triple-dash flags (`---cap`, `---slug`) leak into help output; no documentation of `--data` JSON shape. Internal routing params shouldn't be exposed. | 🟢 Fixed | Bug #5 | 2026-04-28 fork session: merged to main as `896a4b0` (feature commit `e0e873a`). Root: closure-binding via default-parameter capture (`_cap=cap["name"], _slug=slug`) — Python idiom for late-binding loop variables — leaks into Typer's option parser, which treats every function parameter as a CLI option (underscored prefix → triple-dash). Fix: factory functions (`_make_cap_cmd`, `_make_collection_cap_cmd`) close over the values without exposing them on the command's signature. **Fixed in BOTH CLI surfaces:** `kernel/cli/app.py` (kernel-container CLI used by harnesses inside the kernel image) AND `indemn_os/src/indemn_os/main.py` (the pip-installed user-facing `indemn`). `--data` also gained per-capability help with example shape (`fetch_new` shows `{"since": "<ISO 8601>", "user_emails": [...]}`) plus pointer to `indemn skill get <Entity>`. **Side-finding logged:** the two CLI codebases duplicate ~all entity-command-registration logic — fix-in-one-place isn't enough; future structural cleanup candidate. **Verified live 2026-04-28:** `indemn meeting fetch-new --help` now shows only `--data` (with shape docs) + `--help`, no triple-dash anywhere. **Deployed** via `railway up --service indemn-api` + `--service indemn-temporal-worker`. |
| **Bug #6 — Auto-generated entity skill has wrong fetch-new syntax** — skill template emits `indemn meeting fetch-new <id> --auto` but `fetch-new` is collection-level (no `<id>`) and `--auto` doesn't apply. Agents follow wrong instructions. Fix: special-case collection-level capabilities in `kernel/skill/generator.py`. | 🟢 Fixed | Bug #6 | 2026-04-27 fork session: subsumed by merge `f4fc121` (feature commit `6af2166`, same kernel/skill/generator.py rewrite). Collection-level capabilities now render with `--data '{...}'` and no `<id>`/`--auto`. **Deployed 2026-04-27** via `railway up --service indemn-api`. |
| **Bug #11 — `/api/queue/stats` returns 404** — observability endpoint missing or registered at a different path. CLI `indemn queue stats` works (uses different path). Document the actual path or alias. | 🟢 Fixed | Bug #11 | 2026-04-28 fork session: merged to main as `e9d04da` (feature commit `008efcd`). The actual handler was at `/api/_meta/queue-stats`; CLAUDE.md and other docs referenced `/api/queue/stats`. Aliased — both routes now hit the same `_aggregate_queue_stats` helper. The `/api/queue/stats` path is canonical for human use (matches the `indemn queue stats` mental model); `/api/_meta/queue-stats` stays for back-compat. **Verified live:** both paths return identical 6-row role × status aggregate. |
| **Bug #12 — AWS Secrets Manager `mongodb-uri` secret has wrong host** — points to `dev-indemn-pl-0.mifra5.mongodb.net` (DNS not-found). Actual host is `dev-indemn.mifra5.mongodb.net`. `mongosh-connect.sh` wrapper fails. Direct connection with corrected URI works. | 🟢 Fixed (re-fixed 2026-05-04 Session 16 — secret was reverted between Session 15 and Session 16 by an unknown writer) | Bug #12 | 2026-05-01 Session 15: pulled the working URI from chat-deepagents Railway env (the live production value), `aws secretsmanager update-secret --secret-id indemn/dev/shared/mongodb-uri`. Verified `mongosh-connect.sh dev tiledesk --eval 'db.runCommand({ping:1})'` returns ok=1. **2026-05-04 Session 16 regression detected:** secret was back at the broken `dev-indemn-pl-0` host when re-querying mongo for cron_runner soak metrics. Re-pulled working URI from `railway variables --kv --service indemn-runtime-chat`, ran the same `aws secretsmanager update-secret` command. Verified ping=1. **Open question:** what process reverts this secret? The chat runtime's Railway env still has the correct value, so the runtime services aren't broken — only operator tooling (`mongosh-connect.sh`) gets bitten on local laptops. Worth checking before any future session starts; consider a `pre-session-hook` validation script or just adopt the convention of re-fixing on detection. |
| **Bug #49 — cron_runner doesn't heartbeat during subprocess wait; long-running fetches get killed by Temporal's 90s heartbeat_timeout** — surfaced 2026-05-04 Session 16 close investigating 11 dead_letter messages over the 3-day soak (5 Email-Fetcher + 6 Slack-Fetcher + 0 Meeting/Drive — Slack-heaviest because Slack fetches are slowest). Initial framing was "Bug #38 orphan cleanup, not a real cron_runner failure." Direct investigation via Temporal CLI showed otherwise: `temporal workflow describe msg-69f81d4a1f2c3ee82ecb65bf` returned `Status: FAILED, Failure: Activity task timed out → Cause: activity Heartbeat timeout`, RunTime 8m15s. **Root cause:** `process_with_associate` activity has `heartbeat_timeout=timedelta(seconds=90)` (per `kernel/temporal/workflows.py`). The agent path runs an `_heartbeat_loop` that fires `activity.heartbeat()` every 30s during `agent.ainvoke()`. The cron_runner branch I shipped in Bug #40 v1 had NO heartbeat — it called `run_cron_skill` (sync) which blocked on `subprocess.run` for the duration of the fetch. Fetches under 90s succeeded; fetches over 90s (Slack with rate-limited channels, Drive on first crawl, Email under heavy load) hit heartbeat_timeout, activity got cancelled, both retries failed identically, workflow ended FAILED, message orphaned at status=processing → visibility timeout → next dispatch sweep → WorkflowAlreadyStartedError → Bug #38 cleanup → dead_letter. **The Slack > Email > 0 split is exactly the predicted pattern** (Slack fetches are slowest; Meeting + Drive cron tick every 15min/hourly so each tick had less new content). **Fix:** in `harnesses/async-deepagents/main.py::process_with_associate`'s cron_runner branch, mirror the agent path's pattern — call `activity.heartbeat("starting_cron_runner")` immediately, then `asyncio.create_task(_cron_heartbeat_loop)` (sleeps 30s + heartbeats), wrap the sync `run_cron_skill` in `await asyncio.to_thread(...)` so the heartbeat task runs concurrently on the event loop. Cancel + await heartbeat task in finally. Activity-level concern lives in the calling activity, NOT inside cron_runner.py — keeps `run_cron_skill` sync + simple, mirrors the agent path exactly. **Tests:** new `test_process_with_associate_heartbeats_cron_runner_branch` source-level pin in `test_cron_runner.py` checks the branch contains `activity.heartbeat("starting_cron_runner")`, `_cron_heartbeat_loop`, `asyncio.create_task`, `asyncio.to_thread(run_cron_skill`, and `cron_heartbeat_task.cancel()`. **Plus OTEL span emission added in same fix:** `cron_runner.run` span via `opentelemetry.trace.get_tracer(__name__)` with attributes `associate.id`, `associate.name`, `message.id`, `entity.type`, `argv`, `tool`, `result.fetched|created|skipped_duplicates`, `result.errors_count`, `outcome`. Lives under the parent activity span (TracingInterceptor) so the full chain is queryable in Grafana by trace_id, plus filterable by `associate.id` matching the muscle memory of CLAUDE.md § 8 (which queries LangSmith for agent runs by `associate_id`). Per vision §2 item 7 — "OTEL on every operation, LangSmith for AI observability." cron_runner is system-level so OTEL is the canonically-correct surface; LangSmith stays at 0 traces for cron_runner runs and that's correct. **2 new tests** (`test_run_cron_skill_emits_otel_span_with_attributes`, `test_run_cron_skill_otel_span_records_failure_outcome`) using `opentelemetry.sdk.trace.export.in_memory_span_exporter`. **Verification: deploy + observe over next 24h** — expected dead_letter rate should drop from ~0.6% (11 / 1863) to near-zero on cron_runner runs (Bug #38 orphans from genuine runtime restarts will still occur but should be << current rate). | 🟢 Fixed (Bug #40 v2) | 2026-05-04 Session 16 dead_letter root-cause investigation. Temporal `temporal workflow describe msg-69f81d4a1f2c3ee82ecb65bf` showed `Status: FAILED, Cause: activity Heartbeat timeout`, RunTime 8m15s. | 2026-05-04 Session 16: pending end-of-session push. Code: `harnesses/async-deepagents/main.py` (heartbeat loop wrapper) + `harnesses/async-deepagents/cron_runner.py` (OTEL `cron_runner.run` span). Tests: 25 cron_runner tests (was 22; +3 for OTEL × 2 + heartbeat pin × 1). Deployed via `railway up --service indemn-runtime-async`. |
| **Bug #48 — `indemn` CLI URL slug ignores `--collection-name` override (client-side counterpart of Bug #39)** — surfaced 2026-05-01 Session 16 during Bug #40 verification. Bug #39 (Session 15, commit `00b7407`) made the kernel route generation honor `entity_cls._collection_name` via new `_route_slug_for(name, cls)` helper. The CLI client (`indemn_os/src/indemn_os/main.py:111` + `bulk_commands.py:18`) was NOT updated — it still computed URLs from naive `slug = name.lower()` + `f"/api/{slug}s/..."`. For entities where `collection_name == name.lower() + "s"` this happens to match (Email → emails, Meeting → meetings, Document → documents). For entities where it diverges (SlackMessage's `collection_name=slack_messages` per inflect engine — Bug #15), the CLI 404s on every operation: `indemn slackmessage list` → `/api/slackmessages/` → 404, while server route is `/api/slack_messages/`. **Effective impact:** Slack-Fetcher's cron_runner exec hit 404 on `indemn slackmessage fetch-new`. cron_runner detected the failure correctly (WARNING log + queue fail) — proper error path. But the fetcher couldn't actually fetch. **Two-part fix shape:** (1) server-side: `kernel/api/meta.py::get_entity_metadata` (list endpoint) didn't include `collection` field at all — added it via `_route_slug_for(name, cls)`. The detail endpoint `get_entity_detail_metadata` had `collection` but used the broken `cls.Settings.name if hasattr(cls, "Settings") else entity_name.lower() + "s"` fallback (which returned naive plural for every domain entity since they don't have Beanie Settings — collection_name lives on `_collection_name`). Replaced with `_route_slug_for(entity_name, cls)`. (2) client-side: split `cli_name = name.lower()` (singular Typer subcommand — operators still type `indemn slackmessage list`) from `slug = meta.get("collection") or (cli_name + "s")` (URL collection slug). All `f"/api/{slug}s/..."` templates → `f"/api/{slug}/..."`. `register_bulk_commands(name, entity_app)` now takes `url_slug` kwarg sourced from meta. **9 unit tests:** `tests/unit/test_meta_collection_field.py` × 4 (list endpoint imports `_route_slug_for`, list response shape, detail endpoint shape, end-to-end domain-entity scenario) + `tests/unit/test_cli_url_slug_resolution.py` × 5 (CLI uses `meta.get("collection")`, no `{slug}s` patterns left, bulk_commands templates, `url_slug` kwarg shape, Typer subcommand stays singular). Source-level pins via `inspect.getsource` rather than spinning up Typer — guards against the one-line regression that broke this. **Deployment:** indemn-api redeployed (meta endpoint now returns collection). indemn-runtime-async redeployed (Dockerfile pip-installs indemn_os during build → harness picks up new client URLs). Local CLI reinstalled in bot-service venv (`/Users/home/Repositories/bot-service/.venv/bin/pip install -e /Users/home/Repositories/indemn-os/indemn_os/`). **Verified live 2026-05-01 20:30+:** `indemn slackmessage list --limit 3` returns 3 entries (was 404 pre-fix); `indemn slackmessage fetch-new` returns `{fetched: 1, created: 0, errors: []}`; runtime cron_runner success log: `cron_runner success: associate=Slack Fetcher message=69f50c321f2c3ee82ecb5f6d result={'fetched': 1, 'created': 0, ...}`. **3-day soak (2026-05-01 → 2026-05-04):** 769 Slack-Fetcher cron_runner completions, 6 dead_letter (all Bug #38 orphan cleanups, not 404s). **Open follow-on:** `kernel/cli/app.py` has the same divergence (parallel CLI surface used inside the kernel image for `indemn skill`, `indemn entity`, etc.) — same fix should be propagated. Current Session 16 scope was the user-facing `indemn_os/` CLI; kernel/cli/app.py wasn't audited but likely has the same naive-plural pattern. Lower priority since the harness uses `indemn_os/` for all entity operations. | 🟢 Fixed | 2026-05-01 Session 16 (Bug #40 verification surfaced it via Slack-Fetcher 404s) | 2026-05-01 Session 16: indemn-os main commits pending end-of-session push: kernel/api/meta.py (add collection field via _route_slug_for) + indemn_os/src/indemn_os/{main.py, bulk_commands.py} (use meta.collection, split cli_name from URL slug) + 2 new test files. Test suite 515 → 524 (9 new). Deployed via `railway up --service indemn-api` + `railway up --service indemn-runtime-async`. |
| **Bug #15 — Naive entity collection pluralization** — `Company` → `companys`; `Opportunity` → `opportunitys`. Auto-derivation uses `name.lower() + "s"`. English plurals harder than that. Fix: use `inflect` library OR require explicit `collection_name` in entity defs. Existing collections need migration or doc update. | 🟢 Fixed (accept and fix forward) | Bug #15 | 2026-04-28 fork session: merged to main as `1314a21` (feature commit `a355d2a`). Per Craig: existing collections (`companys`, `opportunitys`) NOT migrated — would require lockstep updates to API URLs, all 56,485+ change-record entity_type strings, all relationship_target references, and all watch entity_types. High blast radius for cosmetic typo. Going forward, `--collection-name` defaults to `inflect.engine().plural(name.lower())` in BOTH CLI surfaces (kernel + indemn_os). Operators who need to land in an existing typo'd collection (cross-org clones) pass `--collection-name` explicitly, same as today. Added `inflect>=7.0` dep, doc note in `docs/guides/adding-entities.md`. 5 unit tests pin Company → companies, Opportunity → opportunities, Email → emails (no regression on already-clean), Person → people (irregular), and cross-CLI agreement. No deploy needed (CLI runs on user laptops). |
| **Bug #2 — `indemn {entity} delete` (singular) doesn't exist** — only `bulk-delete --filter '{...}'` which is async via Temporal (no synchronous confirmation). Fix: add singular `delete` to entity registration in `kernel/cli/app.py`. | 🟢 Fixed | Bug #2 | 2026-04-28 fork session: merged to main as `d037987` (feature commit `f6f9fbe`). New `delete <entity_id>` command auto-registered for every entity type in BOTH CLI surfaces (kernel/cli/app.py + indemn_os/src/indemn_os/main.py). Routes through `bulk-delete` with `{"_id": entity_id}` filter so the kernel-side audit + watch evaluation paths still run (Bug #23's `$oid` coercion makes the single-id filter reliable). Confirmation prompt with `--yes` opt-out. **Verified live:** `indemn touchpoint delete --help` shows the new command with full description. **Deployed** via `railway up --service indemn-api`. |
| **Bug #3 — `bulk status` output lacks deletion counts** — returns workflow_id + status only; no `records_seen`, `succeeded`, `skipped`, `errored`. To verify a bulk-delete worked, must separately list and count. Fields exist in the workflow state — just need surfacing. | 🟢 Fixed | Bug #3 | 2026-04-28 fork session: already shipped by burst #4's Bug #24 fix (commit `0ed8c80`). `BulkExecuteWorkflow` returns `{matched, succeeded, errored, errors}`, `GET /api/bulk/{id}` merges them into the response. **Verified live:** `indemn bulk status bulk-444d5298fef1` returned `{matched: 1, succeeded: 1, errored: 0, errors: [], lifecycle_status: "COMPLETED", status: "completed", ...}`. No additional code change needed — confirmed during the Bug #2/#4 cluster work. |
| **Bug #4 — `bulk-delete --filter '{}'` silently no-ops** — `{}` should match all (with confirmation) or be rejected with helpful error. Today returns started → completed with 0 deleted, no error. Pair with #23 to make bulk-delete trustworthy. | 🟢 Fixed | Bug #4 | 2026-04-28 fork session: merged to main as `d037987` (feature commit `f6f9fbe`). API-boundary check in `_register_bulk_route::start_bulk` rejects `filter_query == {}` for `delete` and `update` operations unless `match_all: true` is set. **Pre-burst-#4 footgun was actually masking a worse one:** the old code's missing org_id contextvar made `find_scoped({})` skip its scoping clause and match nothing. Burst #4 (Bug #23) fixed the contextvar, which would have turned the silent no-op into "delete every entity in the org." This fix prevents that. Per-entity `bulk-delete` CLI exposes the opt-in as `--all` flag with help text pointing to dry-run for verification. **Verified live:** empty filter without match_all → 400 with helpful message; with `match_all: true` → workflow starts (dry-run only in test). 5 unit tests pin: empty-filter rejection shape, validation runs before start_workflow, `--all` flag presence on both CLI surfaces, singular delete uses single-_id filter + has confirmation prompt. **Deployed** via `railway up --service indemn-api`. |
| **Bug #7 — Adapter noisy "fileId" warnings** — Google Workspace adapter logs `WARNING: Failed to export shared doc: Missing required parameter "fileId"` ~50+ times per fetch. Should short-circuit when fileId is empty. | 🟢 Fixed | Bug #7 | 2026-04-28 fork session: merged to main as `896a4b0` (feature commit `e0e873a`). Root: `elif notes_doc_id == transcript_doc_id` evaluated `"" == ""` as True, so meetings with no docs fell into the elif branch and called `_export_doc(email, "")` — Drive API rejected with "Missing required parameter 'fileId'" warning per call (~50+ per fetch). Two-layer fix: (1) truthy-guarded both branches in `_build_meeting` (`elif notes_doc_id and notes_doc_id == transcript_doc_id`); (2) defense-in-depth at top of `_export_doc` returns `""` immediately on empty input, no API call. Future callers that forget the truthy guard still don't generate noise. **Verified live 2026-04-28** against a real fetch (5 meetings, 18 users): zero "Missing required parameter 'fileId'" warnings in logs. (One unrelated `HttpError 404 File not found` warning surfaced — that's a real-world deleted-doc 404, correctly caught + logged informatively, not the bug.) **Deployed** via `railway up --service indemn-api` + `--service indemn-temporal-worker`. |
| **Bug #8 — Adapter swallows all per-user errors in `fetch()`** — broad `except Exception` masks systemic failures (the Bug #1 root cause was hidden by this). Fix: track success rate; raise if all/most users fail with same error class. | 🟢 Fixed | Bug #8 | 2026-04-28 fork session: merged to main as `896a4b0` (feature commit `e0e873a`). New `_PerUserErrorTracker` class records per-user successes / failures across both fetch loops (meetings, emails). Decision rule: if ZERO users succeeded AND ≥3 (or ≥50% of attempted users, whichever is greater) failed with the SAME error class, raise `AdapterError` with `"All N/M attempted users failed to <op> with <ExceptionClass>. Sample (<user>): <msg>. This looks systemic, not per-user."`. Per-user transient issues (one mailbox locked, one user has no inbox) stay quiet; every-user-failed-the-same-way (Bug #1-class) surfaces as the fetch-blocking event it actually is. 6 unit tests pin the threshold logic. **Verified live (success path) 2026-04-28**: fetch ran across 18 users, all succeeded, tracker raised nothing. Failure-path raise behavior is pinned by unit tests; no synthetic systemic failure was triggered against prod. **Deployed** via `railway up --service indemn-api` + `--service indemn-temporal-worker`. |
| **Bug #13 — Railway doesn't auto-deploy on `git push origin main`** — `docs/guides/development.md` describes auto-deploy; reality is `railway up --service <name>` is required. Decide direction (configure auto-deploy or update docs) and align. | 🟢 Fixed (docs aligned to manual-deploy reality) | Bug #13 | Doc was already corrected 2026-04-28 (`docs/guides/development.md` § 8: "Deployment is **manual** via the Railway CLI. Pushing to `main` does NOT trigger a deploy on its own"). 2026-05-01 Session 15: indemn-os main commit `bee1a7e` adds voice-deepagents + harness_common rows to the deploy-target table per the new Railway service introduced this session. |
| **CLI `INDEMN_API_URL` defaults to `localhost:8000`, ignores `api_url` in credentials** — `indemn_os/src/indemn_os/client.py:18` reads `os.environ.get("INDEMN_API_URL", "http://localhost:8000")`. The credentials file at `~/.indemn/credentials` ALREADY carries `"api_url": "https://api.os.indemn.ai"` (set by `indemn auth login`) but the client never reads it. Result: any CLI invocation without the env var pre-set silently routes to localhost, fails (or, with a misleading 401 if something is listening locally), and the user has no clear path to fix. Trivial fix: read `api_url` from the credentials file when the env var is unset. Discovered 2026-04-28 fork session. | 🟢 Fixed | 2026-04-28 fork session investigation | 2026-04-28 fork session: merged to main as `ccc8714` (feature commit `0cefd3c`). 3-tier resolution for both `base_url` and `token`: env var (highest, for service tokens / dev override) → `~/.indemn/credentials` (set by `indemn auth login`) → fallback default (localhost / empty). Existing local-dev workflows with no creds file unchanged. Harness containers that set `INDEMN_SERVICE_TOKEN` + `INDEMN_API_URL` via env vars unchanged (env wins). Only behavior change: post-`indemn auth login` workflows now actually work without manually exporting `INDEMN_API_URL`. 7 unit tests; full 360-test suite green. **Verified live:** `unset INDEMN_API_URL && indemn touchpoint get <id>` succeeds, reading the URL from the credentials file. No Railway redeploy needed (CLI runs on user laptops; harness containers set env vars explicitly). |
| **Bug #19 followon — unit-test fixtures with SimpleNamespace skip the real serialization path** — surfaced 2026-04-28 fork session during `--include-related` verification. The 10 unit tests for `_build_related_entities` used SimpleNamespace stand-ins where `model_dump()` is a custom lambda. `kernel.api.serialize.to_dict` calls `entity.model_dump(by_alias=True)` and falls through to `__dict__` on exception — which the test fixtures provide. So the tests exercised the fallback path, never the real Pydantic-model + ObjectId-conversion path. The first deploy hit a 500 in prod that the unit tests couldn't catch. **Pattern fix:** any new helper that processes "real entity → API response dict" wants at least one integration test against a constructed Pydantic instance, not just SimpleNamespace mocks. Affects future fork-session work; no code action right now. | 🔴 Open (process improvement) | 2026-04-28 fork session post-mortem on `--include-related` first deploy | TBD |
| **Bug #46 — kernel `fetch_new` watermark hardcoded to `date` field; silently broken for SlackMessage + Document** — surfaced 2026-05-01 Session 15 after backfilling 860 SlackMessages and seeing identical drain pattern (`fetched=50, created=0, skipped=50`) on every subsequent fetch. Cross-checked Drive: same pattern (`fetched=5, created=0, skipped=5`) — Drive-Fetcher had been silently re-fetching the same files hourly for ~17 hours since Session 14 activation. **Root cause:** `kernel/capability/fetch_new.py` line 38 hardcoded `entity_cls.find_scoped({}).sort("-date").limit(1)` to compute the incremental watermark. Works for Email + Meeting (which carry a `date` field — actual email/meeting timestamp). Silently broken for SlackMessage (uses `posted_at`) + Document (uses `created_date`) — sort either failed or returned an entity with `date=None`, the try/except swallowed it, `since` stayed unset, adapter fetched all available channel/folder history every cron tick. Dedup-by-external-ref made the bug invisible (correctness preserved; `created` always 0; rate budget burned on wasted Slack API + Drive API + LLM calls). **Fix:** candidate-field list `("date", "posted_at", "created_date")` in WATERMARK_FIELD_CANDIDATES. First field that exists on the entity AND has a non-null value on the latest record wins; falls through to "no since" (preserves "fetch all" semantic) if none match. Per-candidate try/except handles missing-field sort failures. **8 unit tests** in `tests/unit/test_fetch_new_watermark.py` pin: each candidate scenario, fall-through on null/missing/empty, explicit `since` takes precedence, and a constant-shape test guards the candidate ordering against drift. Indemn-os main commit `20c074c` (`fix(capability): fetch_new watermark falls through date→posted_at→created_date — Bug #46`); 446 unit tests pass; deployed via `railway up --service indemn-api`. **Verified live 2026-05-01 13:35Z:** SlackMessage post-fix: `fetched=1, created=0, skipped=1` (boundary message; adapter is `>=` watermark vs DB external_ref `>`) → second call: `fetched=0` (fully drained). Document post-fix: `fetched=5, created=5, skipped=0` — **5 NEW Drive files surfaced that would've been silently missed forever under old behavior.** Drive-Fetcher and (once activated) Slack-Fetcher now do real incremental work per cron tick, not wasted re-fetches. **Trace-as-build success story:** the bug was invisible to unit tests (correctness preserved), invisible to logs (no errors, just 0 created), and would have been invisible to dashboards. Surfaced only by running it manually on real backfilled data and noticing the drain pattern doesn't converge. | 🟢 Fixed | 2026-05-01 Session 15: backfill drain pattern surfaced the bug; cross-verified against Drive | 2026-05-01 Session 15 indemn-os main commit `20c074c`. 8 new unit tests. **Side-effect on Drive history:** Drive-Fetcher had been running hourly since Session 14 with broken watermark. Files created after Session 14's 30-day-backfill window (1161 fetched / 493 created / 668 skipped) AND before today's fix were being missed by every subsequent hourly run because they fell outside the limit-bounded "most recent N" window the adapter returned with no `since`. **Action item:** consider running an explicit `indemn document fetch-new --data '{"since": "2026-04-30T18:00:00Z"}'` once to backfill the gap. Likely small (few dozen files) but worth doing for completeness. |
| **Bug #45c (kernel design) — `kernel/integration/resolver.py::resolve_integration()` Step 3 silently fails when `access` is null/missing on an org-level Integration** — surfaced 2026-05-01 Session 15 root-causing Bug #45a/b. **Today's behavior:** Step 3 query is `Integration.find_one({owner_type: "org", org_id, system_type, status: "active", "access.roles": {"$in": role_names}})`. When `access` is null, MongoDB cannot resolve the `access.roles` path → no document matches → resolver raises `AdapterNotFoundError(f"No {system_type} integration available. Create one with: indemn integration create...")`. **Effective foot-gun:** (1) the operator successfully creates an org-level Integration, sees it as `active`, but it's silently unreachable; (2) the error message says "no integration available" which is misleading — one DOES exist, it just can't be matched; (3) there's no validation at create-time that flags missing `access` for `owner_type: org`. **Design question:** should the resolver treat null/missing `access` on org-level Integrations as "no role gate" (any actor in the org can use it), matching common operator intent for one-org-one-Indemn-tenant deployments? Today's strict semantic ("missing access = unreachable") is the safer default for multi-team orgs but is the opposite of what most operators expect. **Two reasonable fixes:** (a) **resolver-tolerant** — `if integration.access is None or not integration.access.get("roles"): match` — treats null as "no gate." Backwards-compatible (any org-level Integration that explicitly sets `access.roles` keeps role-gating). (b) **fail-fast at create** — Integration validator rejects `owner_type: org` with `access: null`. Forces explicit operator intent. Neither is harder than ~20 lines + a test. **Suggested:** combine both — resolver-tolerant (avoids breaking existing manually-patched Integrations) + a CLI warning at create-time when `owner_type: org` without `access.roles` ("This Integration will be reachable by any actor in the org. Add --access-roles to gate."). | 🔴 Open (kernel design) | 2026-05-01 Session 15 Bug #45 root-cause | TBD — fork session candidate. Low-priority since the workaround (set access.roles explicitly) is easy and now documented. Revisit when next operator hits the same trap or before TD-11 (external-customer hardening). |

### Open design questions (need discussion before implementation)

**Handed to main session for Craig + design conversation.** These five
questions all touch domain shape (entity types, state-machine
transitions, sub-stage modeling, referrer fields, Playbook hydration
mechanism) — not OS kernel mechanics. Resolving them needs the main
session's customer-system context. Fork sessions should NOT pick these
up cold.


| Item | Status | Source |
|---|---|---|
| **Opportunity vs Problem entity** — should the unmapped-pain case (a problem we noticed but no clear AssociateType fit) get its own entity, or should Opportunity loosen `mapped_associate` to nullable with state `unmapped → mapped → validated → proposed → addressed → resolved`? | 🔴 Open | `artifacts/2026-04-24-design-dialogue-playbook-artifact-proposal.md` § Topic 2 |
| **Auto-Contact creation should pull richer fields from email signatures (title, phone, address)** — surfaced 2026-04-29 Armadillo trace Step 1/4. When EC v9 / TS create a Contact for a new sender, they only populate `name + email + company + how_met`. The email body / signature carries `title` (e.g. "Chief Executive Officer"), `phone` (e.g. "(201) 873 6920"), `address` (e.g. "1400 Main Street, Suite 164, Clarksville, IN 47129") — none of which land on the Contact entity. Contact entity may not even have these fields today; if not, add them to the entity definition AND have EC/TS parse the signature when creating. The trace artifact suffers because Contact entities are bare. | 🔴 Open | 2026-04-29 Session 12 Armadillo trace (Matan's email had full signature; only `email` + `name` made it onto the Contact) |
| **Internal Kyle docs spanning multiple prospects (e.g. Apr 8 Warranty Prep covering Amynta + Fair + Armadillo) have no path into OS** — surfaced 2026-04-29 Armadillo trace context-gathering. Kyle's `Warranty Prep — Amynta/Guardsman call + Fair + Armadillo (Apr 8, 2026)` doc lives in his personal Drive and contains rich strategic context (warranty wedge thesis, Fair case study with 27.7% close rate, three-play framing) that affects ALL THREE prospects. There's no Slack/Drive ingestion adapter for arbitrary internal docs. Even if there were, the doc-to-multi-Company linking is novel — Document.company is a single FK today; this doc would need to link to 3 Companies as scope=internal Touchpoints under each. **Design question: when an internal doc spans N prospects, does it become N scope=internal Touchpoints (each linking to one Company), one Document with a list of Companies, or something else?** Current trace skips this content; the artifact loses signal. (Related: existing Slack-ingestion entry above. This finding extends that — Slack/Drive ingestion + multi-Company doc linking are coupled.) | 🔴 Open | 2026-04-29 Session 12 Armadillo trace (Apr 8 Warranty Prep doc surfaced in Kyle's Drive via DWD; couldn't ingest into OS as Touchpoint) |
| **Company hydration / enrichment process** — when a Company is auto-created from a first-touch email (e.g. Armadillo from Matan's intro on 2026-04-02), the entity has only `name`, `domain`, `cohort`. No description, industry, headquarters, employee count, leadership, web presence, etc. We should have a process that hydrates the Company entity continuously — could be (a) one-shot on-creation enrichment associate that pulls from Apollo / LinkedIn / web at the moment a new Company is created, (b) on-every-interaction re-evaluation that adds new fields as we learn from Touchpoints, (c) weekly/daily Company-sync sweep that re-checks every Company against external sources, or (d) a hybrid. Sources include: Apollo (already integrated), email signatures from related Emails, web/LinkedIn scraping. Triggered by: new Company create event (via watch), new Touchpoint linking to Company, scheduled cron, or manual. **Open design question** — surfaces from the 2026-04-29 Armadillo trace where `Company{name, domain, cohort}` is bare and ALL the rich context (15-person sales team, real-estate-agent partnerships, 1.5M agent network, etc.) lives only in the Apr 28 meeting transcript. The customer-system vision treats the Company as the constellation hub; a bare hub provides no constellation. | 🔴 Open | 2026-04-29 Session 12 Armadillo trace findings (Step 1 outcome) |
| **Document-as-artifact pattern for emails** — Proposal→Document is designed. But where does a *drafted* email live? Email entity with status `drafting`? Document with `mime_type: message/rfc822`? Hybrid? | 🔴 Open | memory `project_customer_system_artifact_entity_pattern.md`; `artifacts/2026-04-24-design-dialogue-playbook-artifact-proposal.md` § Topic 3 |
| **Stages — 12 with sub-stages, multi-select for archetypes** (Kyle's Apr 24 ask) — Stage entity gets sub-stages, OR a separate `qualification_state` column, OR Stage records gain a `parent_stage` field for hierarchy. | 🔴 Open | `artifacts/2026-04-24-kyle-sync-recap.md`; full transcript at `artifacts/context/2026-04-24-kyle-craig-sync-transcript.txt` |
| **Origin / referrer tracking** (Pat Klene → GR Little example, Kyle's Apr 24 ask) — field on Deal (`source_referrer: Contact_id`)? New Introduction entity? Extending Conference into general "Introduction"? | 🔴 Open | `artifacts/2026-04-24-kyle-sync-recap.md`; full transcript |
| **Playbook hydrates from historical extractions** (long-term) — once the pipeline is reliable and we have many Touchpoints across stages, the Playbook records should refine from observed patterns. Mechanism for that refinement (manual? scheduled associate? human-in-the-loop?). | 🔴 Open | `artifacts/2026-04-24-playbook-entity-vision.md` |

---

## Fixed — historical record

| Item | Status | Fix | Date |
|---|---|---|---|
| **Bug #1** — Google Meet adapter rejected date-only `since` filter (Meet API requires full RFC3339; broad `except` swallowed the per-user error so `fetched: 0` for everyone). Fix: `_normalize_rfc3339()` helper in `kernel/integration/adapters/google_workspace.py`. | 🟢 Fixed | Commit `8afda9d` | 2026-04-24 |
| **Bug #14** — CLI POST/PUT timeout was 60s — too short for large `fetch-new` calls. Fix: bumped to 600s default, configurable via `INDEMN_CLI_TIMEOUT`. | 🟢 Fixed | Commit `f0dfe89` | 2026-04-24 |
| **Bug #10 (one-time)** — 21 Apr 20-21 test meetings never became Touchpoints because Synth role/watch was added after they were created (root cause = #10 — no backfill mechanism). Resolution for the data: `indemn meeting bulk-delete` cleared them. The underlying capability gap remains open above. | 🟢 Resolved (data) | Manual cleanup | 2026-04-24 |
| **Pre-Apr 24 infrastructure fixes:** Queue processor `WorkflowAlreadyStartedError` removed from temporalio SDK (replaced with `RPCError` + `RPCStatusCode.ALREADY_EXISTS`); runtime service token regenerated and propagated; "Interaction" renamed to "Touchpoint" (kernel collision); 6 entity definitions had missing `is_state_field: true` on status fields; async harness skills loaded via deepagents progressive disclosure (not concatenated into system prompt); async harness heartbeat during `ainvoke()` (Temporal was cancelling long-running activities); model upgraded to `gemini-2.5-flash` on enterprise Vertex project. | 🟢 Fixed | Multiple commits | 2026-04-22/23 |

---

## Notes on prioritization

**The top-4 to make the autonomous Extractor work** (per `extractor-procedure-and-requirements.md`):
1. Touchpoint `source_entity_*` fields + Synth populates them — **Option B**
2. Workflow detects empty-output agent as failure
3. Per-invocation tool-result isolation
4. Generated entity skill teaches filter syntax

These four together take the Extractor from "structurally broken" to "works reliably on the happy path." Everything below them is quality-of-life or scale reliability.

**Top blockers for running the pipeline at scale:**
- Entity-resolution capability (root fix for #16 / 446-Company)
- Option B (extraction unblocks)
- Bug #29 (entity-def iteration unblocks)

**Top observability gaps:**
- Bug #22 (per-associate service tokens for forensics)
- Bug #27 (`created_by` on entities)
- Bug #11 (`/api/queue/stats` endpoint)

**The acceleration mechanic is real here:** each fix above makes the next thing 2-3x faster. Option B done means the Extractor works. Extractor working means we can backfill all team meetings and emails. Backfill done means we hydrate Alliance + Arches + every prospect. Hydrated graph means the Artifact Generator has real data. Artifact Generator means dashboards have real data. Dashboards mean the team adopts. Adoption means we discover the next round of OS gaps.

---

## How findings get added here

When customer-system work surfaces something OS-relevant:

1. **Add it to `artifacts/2026-04-24-os-bugs-and-shakeout.md`** if it's a bug — that file holds the deep detail (symptom, root cause, proposed fix).
2. **Add it to `artifacts/2026-04-24-extractor-procedure-and-requirements.md`** if it's a *capability gap* — a missing OS feature, not just a bug.
3. **Add it to `INDEX.md` Open Questions** if it's a design question that needs discussion.
4. **Add a row to this file** in the appropriate severity section, with a 1-2 sentence summary and links to (1)/(2)/(3) above.
5. **When status changes** (in-progress, fixed), update this row. Don't delete it.
6. **When the OS-side write-up happens** (in `../product-vision/` or directly in `/Users/home/Repositories/indemn-os/docs/`), link it in the "OS-side artifact / commit" column.

This file is the queue. The artifacts hold the depth. The OS docs hold the resolved state.
