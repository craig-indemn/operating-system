# Customer System

The first real system built on the Indemn Operating System. A customer delivery/success system that becomes the source of truth for customer information — who our customers are, what we've built for them, what state things are in, and what needs attention. More broadly: the company's operating system. Everything Indemn does should be in this system.

Kyle is pressing for this. It doubles as the proving ground for the OS domain modeling process.

## Status

**Session 2026-04-29 (Session 12) — Bug #35 + #36 + #37 all closed; Hard Rule #1 inverted (EC v9); Armadillo traced end-to-end as designed; Kyle alignment doc + Armadillo trace artifact shipped.**

Combined output of the main session and a parallel kernel-fix session. Three load-bearing bugs closed, the autonomous new-prospect flow validated on a real prospect for the first time.

**Kernel fixes shipped to indemn-os main this session (parallel work):**
- Bug #35 — deepagents skill discovery. Two stacked fixes (commits `8141a80`, `2ba6f63`): absolute-path return from `_write_skills_to_filesystem`, then `yaml.safe_dump` for frontmatter. Live-verified.
- Bug #36 — Gmail + Calendar `fetch_new` adapters silently ignored `until` parameter (commit `477a98f`). Root cause was deeper than first framing: `**params` on public adapter methods silently absorbed any unknown kwargs. Fix plumbs `until` end-to-end (Gmail `before:` operator + sub-day filter, Meet `timeMax`-equivalent), replaces `**params` with `**unknown_params` raising `AdapterValidationError`. 15 new unit tests. Outlook adapter has the same pattern — fix being propagated.
- Bug #37 — Email list endpoint poisoned by malformed `company` field (commit `a5aa89f`). Fix: opt-in tolerance via `_DomainQuery.to_list(skip_invalid=False)` — strict default preserved for migrations/audit, user-facing list opts in. 2 bad rows identified for data cleanup (Emails `69ea548e…6e92` Oneleet, `69ea556f…7387` Linear Orbit) — customer-system domain work, not bundled.

**Customer-system trace work (main session):**
- **Hard Rule #1 inverted in EC** (v7 → v9 in DB, content_hash `9eef4959ae701614`). Old rule "Never auto-create a Company" replaced with "Resolve before create on Company" — resolve-first IS the dedup defense (now reliable post-Bug-#34/#36). Step 5 decision table updated so the 0/0 outcome auto-creates Company + Contact instead of stalling at `needs_review`. Verified live on Armadillo's first-contact email.
- **Armadillo traced end-to-end through the OS as designed.** First trace post-Hard-Rule-#1-inversion. Origin: Matan Slagter (CEO Armadillo) → David Wellner (COO) intro at InsurtechNY (Apr 2). Discovery call Apr 28 with David. Autonomous flow created Company `69f22186…444d` + 2 Contacts (Matan, David). Manual orchestration created Deal `69f223cc…4470` (discovery stage) + linked Touchpoints (gap surfaced — Deal-creator associate doesn't exist yet). 14 entities extracted by IE against the DISCOVERY Playbook.
- **Artifacts shipped to Kyle** — yesterday's roadmap doc (Drive + Slack) + Armadillo trace (HTML attached as Slack file, not Drive — Drive renders HTML as raw source). Caveat-framed: "still in flux, building intuition for what each stage produces, not a polished deliverable."
- **Brain-dump request sent to Kyle** for TFG / John Scanland — second prospect for the next trace pass.

**Design questions / capability gaps logged this session (in `os-learnings.md`):**
- Deal-lifecycle automation gap (Deal-creator associate + Proposal-at-DISCOVERY auto-create + Touchpoint↔Deal chicken-and-egg) — High, Open
- Employee `entity_resolve` not activated — High, Open
- Company hydration is bare on auto-create (need continuous enrichment process) — Open Design Q
- Contact richer-field parsing from email signatures (title, phone, address fall on the floor) — Open Design Q
- Internal docs spanning multiple prospects (e.g. Kyle's Apr 8 Warranty Prep covering Amynta + Fair + Armadillo) — Open Design Q

**Pipeline associates state at session end:**
- Email Classifier — **suspended** (kill switch held; v9 in DB ready). Reactivate when next prospect's first email lands.
- Touchpoint Synthesizer — **suspended** (unchanged).
- Intelligence Extractor — **active** (untouched).

**Material state in dev OS:**
- Armadillo constellation queryable end-to-end: 1 Company + 1 Deal + 2 Contacts + 2 Touchpoints + 14 extracted entities (3 Operations, 3 Opportunities, 1 Decision, 2 Commitments, 4 Signals, 2 CustomerSystem, 1 Task) all linked.
- Bug #36 side-effect: 500 unrelated Emails + 6 unrelated Meetings created on Kyle's mailbox via the broken adapter. Sit at status `received`; EC suspended so untouched. Cleanup or accept-fix-forward TBD.
- 2 malformed Email rows pending cleanup (Bug #37 data side).

---

**Session 2026-04-28 (late evening — Session 11) — EC v7 + Document Drive sync shipped; discovered every EC iteration v3→v7 has been ineffective because deepagents skill discovery is broken; harness fix attempted but not fully resolved. Session ended for clean handoff to Session 12.**

**Full Session 11 handoff:** `/Users/home/Repositories/operating-system/.claude/worktrees/roadmap/projects/customer-system/artifacts/2026-04-28-session-11-handoff.md` — load-bearing for next session, read in full.

The session that ended Sessions 9 + 10's "we need a better skill iteration" framing by discovering that the agent has never been reading the email-classifier associate skill content at all. Despite Hard Rules being added through versions v3 → v7, the agent's behavior has been driven entirely by the harness DEFAULT_PROMPT plus what it learns from `indemn skill get <Entity>` (auto-generated entity skills, NOT the associate skill).

**Five sequential Diana@CKSpecialty traces in LangSmith project `indemn-os-associates`** — see Session 11 handoff Section 2 for the full table. Run #5 (trace `019dd602-ddb9-7f03-946c-fcee7413e11f`) is the smoking gun: agent finally called Contact resolve (which we attributed to v7's Hard Rule #10), but **system prompt directly inspected says "(No skills available yet. You can create skills in act-…/skills)"** — meaning the agent never actually saw v7's content. The Step 3 improvement came from reading the Contact ENTITY skill, not the associate skill.

**What landed (despite the deepagents finding):**
- EC skill v7 (content_hash `897d05babf0d3b83…`) — Hard Rules #10 (mandatory Contact resolve) + #11 (Contact-first ordering), strengthened multi-1.0 ambiguity language with Diana as worked example. **Not yet read by agent at runtime** — pending Bug #35 fix.
- Document `69efbdea4d65e2ca69b0dd80` Drive metadata sync — `drive_file_id` + `drive_url` set. Entity matches reality.
- Harness commit `5ec4e9f` on indemn-os main — pass skills LIBRARY directory to `deepagents.create_deep_agent(skills=[...])` instead of per-skill subdir paths. Based on reading deepagents docs: `skills` takes library dirs, each containing skill subdirs with their own SKILL.md. **Deployed and tested — system prompt STILL says "(No skills available yet)".** The library-dir fix is necessary but not sufficient. Hypothesis space documented in Session 11 handoff Section 3 — most likely culprit is path resolution against `LocalShellBackend.root_dir` (which is `/workspace/{activity_id}` per Bug #3 fix; passing relative path `{activity_id}/skills` may double-nest).

**Architectural alignments confirmed (Craig + Claude):**
1. **DON'T inline skills into system prompts.** OS vision requires deepagents progressive disclosure to work properly. Inlining bypasses the mechanism instead of fixing it. (My initial proposal: rejected.)
2. **Harness reports facts; OS interprets meaning.** The tightened `agent_did_useful_work` (Session 10 commit `d914d76`) is observation-only — domain-agnostic. Detection of "agent didn't fulfill skill intent" lives in evals (Phase E) and observability dashboards on top of LangSmith — NOT in the harness.
3. **Path 3 for evaluations** stays — the existing `evaluations` repo eventually becomes a kernel adapter (`system_type: evaluation`).
4. **Bug fixes are 100% a priority alongside roadmap progress.** The session's mix of feature work + bug fixing was deliberate.

**Cleanup state at close:**
- EC `69ea1bca23eefe641ea13f44` — suspended (kill switch held)
- Diana@CKSpecialty `69ea56250a2b41b7696076b3` — reset to `received`, no Company link, no Contact link, version 11
- No orphan auto-created Companies (verified via mongosh — last 30 min count = 0)
- All commits pushed to indemn-os main: `956d7d5` (LangSmith), `db97694` (CLI fix), `d914d76` (completion check), `5ec4e9f` (skills lib dir — deployed but didn't fully solve)

**Top priority for Session 12:** Fix deepagents skill discovery so the agent actually reads the email-classifier associate skill. Hypothesis space + diagnostic steps in Session 11 handoff Section 3. Likely a one-line fix at the `create_deep_agent` call site (try absolute path `/workspace/{activity_id}/skills` instead of relative).

---

**Session 2026-04-28 (evening — LangSmith wired in; Session 9's EC kill-switch root cause found + fixed; Path 3 evals architecture aligned) — Phase B1 substantially de-risked. EC verified working end-to-end on Diana@CKSpecialty.**

The session that turned Session 9's "research-level skill-compliance gap" into a 5-minute root-cause investigation. The EC failure was diagnosed not as a skill-following problem but as a kernel CLI bug — Bug #34 in `os-learnings.md`. Without LangSmith we'd have spent the session iterating skill content blindly.

**LangSmith tracing wired into async-deepagents harness** (vision §2 item 7 — foundation B1 milestone). Three env vars on Railway `indemn-runtime-async` (`LANGSMITH_API_KEY` from AWS Secrets `indemn/dev/shared/langsmith-api-key`, `LANGSMITH_TRACING=true`, `LANGSMITH_PROJECT=indemn-os-associates`) + a `config={metadata, tags, run_name}` dict on `agent.ainvoke()` so traces are queryable by `entity_id` / `message_id` / `associate_name` / `runtime_id` / `correlation_id`. Auto-tracing happens via deepagents being LangGraph-based. Project `19302981-aa9e-4869-9137-59a38d7646df`. **How to use: `CLAUDE.md` "When working on X → Debugging an associate's behavior (LangSmith tracing)".**

**Architecture decision (Path 3): evaluations service as future kernel adapter/integration.** Read the `evaluations` repo (separate Indemn repo at `/Users/home/Repositories/evaluations/`, port 8002, LangSmith-based, rubric + custom LLM judges, component-scoped, supports single-turn / multi-turn-replay / multi-turn-simulated). Three forks designed: standalone service learns to target OS associates / native OS evaluation kernel primitives / hybrid where OS gets `Run` + `eval` capability and the evaluations service stays as the LangSmith engine. **Picked Path 3.** Eventually the evaluations service becomes a kernel adapter (`system_type: evaluation`, provider: `langsmith` or `indemn-evals`) so any associate gets evals as a first-class capability. Native OS evals stays a future fork-session — design doc should land in `../product-vision/`.

**Three sequential agent runs on Diana@CKSpecialty's email (`69ea56250a2b41b7696076b3`)** isolating each issue:
- Run #1 (pre-LangSmith): EC auto-created Company `69f0df1bbca2d725ce90ed58`. Session 9 attributed to "skill compliance gap" — wrong.
- Run #2 (LangSmith on, CLI broken — trace `019dd522-…`): EC auto-created Company `69f0ee5b78abc86f9ca2a648`. **Trace showed `entity-resolve` returned HTTP 422** — CLI bug, agent fell through to create.
- Run #3 (CLI fixed, EC v4 — trace `019dd589-…`): Resolve worked. EC tried `email create` instead of `update` (forgot the email in context already exists), failed with E11000, narrated "email already exists", harness silent-completed.
- Run #4 (EC v6 + tightened completion — trace `019dd5c1-…`): ✅ Diana → `classified` linked to existing CK Specialty (`69eaa394...`). No dupe Companies. Version 6→8.

**Three kernel changes shipped to main:**
1. **Commit `956d7d5` — LangSmith wiring** (cherry-picked into main from `feat/langsmith-tracing-async-harness`): `harnesses/async-deepagents/{main.py + pyproject.toml + uv.lock}`.
2. **Commit `db97694` — kernel CLI fix**: `indemn_os/main.py:202` `_COLLECTION_LEVEL_CAPS = {"fetch_new", "entity_resolve"}` (was `{"fetch_new"}` — the entity_resolve omission was Session 9's "skill compliance gap" root cause). One-line fix.
3. **Commit `d914d76` — harness completion check tightened**: `agent_did_useful_work` now requires ≥1 *successful* mutating CLI call (was: any *attempted* mutating call OR any non-empty narrative content). Domain-agnostic — checks tool result success markers via `tool_call_id` matching. 24/24 tests passing including the new Diana failure pattern test.

**EC skill v6 deployed** (content_hash `5bbfdd13ad26c8db…`) with two new Hard Rules — #8 "resolve errors are showstoppers, not fallbacks; never fall through to create on resolve failure" and #9 "never call `indemn email create` — this skill operates on existing emails". Plus new Step 0 "What you're processing (READ THIS FIRST)" emphasizing the email entity in input context IS the email to classify.

**State cleanup:** Rolled back the auto-created Company `69f0ee5b78abc86f9ca2a648`. Reset Diana to `received` via mongosh (state machine doesn't allow `classified → received`). Run #4 then verified Diana correctly lands at `classified` linked to canonical CK Specialty.

**Per OS vision discussion (Craig + Claude): harness reports facts, OS interprets meaning.** The harness's tightened check is observation-only — "did any mutation succeed?" — not domain-aware. Detection of "agent didn't fulfill skill intent" lives in evals (Phase E) and observability dashboards on top of LangSmith traces. NOT in the harness. (My initial proposal was harness checks "did the watched entity transition" — Craig pushed back: that's domain knowledge that doesn't belong in glue code. Some associates legitimately don't transition their watched entity.)

**EC v6 partial (still needs work):** Run #4 trace shows EC still skipped Contact resolve (Step 3) and picked a Company candidate when multiple at 1.0 (should have been needs_review per Hard Rule #3). Both non-destructive. Defer to v7 (more explicit Step 3 enforcement) or to Path 3 eval-set coverage. Remaining gap is small enough that broad reactivation could be considered after v7, but eval-set is the durable answer.

**Bug filed for follow-up:** `kernel/cli/app.py` has the same divergence as `indemn_os/main.py` — no `_COLLECTION_LEVEL_CAPS` set at all. Lower priority since the harness uses the published `indemn_os` CLI, but worth a separate fix. Test `tests/unit/test_adapter_cli_papercuts.py:92` already pins both surfaces against drift.

**Status of pipeline associates after this session:** Email Classifier — suspended (deliberate after verification). Touchpoint Synthesizer — suspended (unchanged). Intelligence Extractor — active (untouched). The silent-stuck-state regression that previously caught IE is now fixed at the harness layer.

---

**Session 2026-04-28 (afternoon — Phase B1 entity-resolution skill propagation + trace-as-build-method captured) — Skills shipped, but reactivation surfaced a skill-compliance gap; halted before broad reactivation.**

Captured the **trace-as-build-method principle** in CLAUDE.md (#20), vision.md (§4), and roadmap.md (Phase B preamble): every skill, watch, capability, and dashboard goes through a trace first — pick a real scenario, run the procedure yourself using the `indemn` CLI on live data, the skill is the writeup of what worked, activation comes after the trace produces the right state. Discovered as Phase A's shake-out method, elevated to canonical building method for all of Phase B+.

Built the **Phase B1 plan artifact** `artifacts/2026-04-28-phase-b1-data-state-and-trace-plan.md` (404 lines) — captures live OS state inventory, structural gaps, lightweight cleanup pass (Path A), trace plan (B1.1 → B1.7), explicit out-of-scope list, decisions log, and execution checklist. The artifact is the working memory for Phase B1.

**Path A executed cleanly.** Activated `entity_resolve` capability on Contact (email + name strategies); added `domain` strategy to Company `entity_resolve` (alongside the existing name + fuzzy-name). Bulk-deleted **79 orphan Companies** (cohort=null, post-Apr-23, no inbound references) — Companies 314 → 235. Resolved **91 high-volume Contact dupes** across 10 groups (service@eventguard.ai × 37, jcdp@gicunderwriters.com × 15, schyzhov@family-first.com × 12, kyle@indemn.ai × 7, raqueltillman@tillmaninsadv.com × 6, four ourbranch.com × 5, cam@indemn.ai × 4) — Contacts 424 → 333. Reclassified 10 Email.sender_contact references to canonicals via API before delete to keep audit trail clean.

**Phase B1 trace executed across 3 scenarios** acting as Email Classifier on real emails: S1 known Contact + Company (jli@levelequity.com — clean, classified) → S2 new Contact at known Company (brian.coburn@stratixadvisory.com — Contact created, classified) → S3 truly new domain (j.kamrath@quadient.com — needs_review). Inline cleanup of LE × 4 + Justin Li × 3 dupes that the trace surfaced (these were below the volume threshold targeted by A3). Trace also covered Touchpoint Synthesizer for S1 + Intelligence Extractor for the new Touchpoint via Option B navigation — all worked end-to-end as Claude.

**Email.interaction → Email.touchpoint rename completed as root-cause fix** (NOT a workaround per the trace-as-build-method principle). Used `indemn entity migrate` to rename the field across 1139 Email documents + the EntityDefinition's field key, then `PUT /api/entitydefinitions/Email/modify` to update `relationship_target` from `Interaction` to `Touchpoint`. Verified end-to-end by setting `Email.touchpoint` on a real email. Audited all 26 entities — only Email had the leftover. The `interactions` MongoDB collection kept (it's the kernel chat/voice session log, different entity, IDs do not overlap with `touchpoints`).

**Three skills updated and deployed:**
- **Email Classifier v3** (`content_hash 4f35d9ffb5a6e273`) — resolve-before-create with explicit decision tree (single 1.0 / multi 1.0 / fuzzy / 0 outcomes), "Never auto-create Company" rule, irrelevant detection, domain stripping, valid `how_met` enum values.
- **Touchpoint Synthesizer v6** (`content_hash 8e7847c8b329f6ba`) — Step 1a idempotency check via Email.touchpoint back-reference (catches manual-trace dupes + reprocess-replay dupes), domain-gated Contact auto-create, Option B source pointers mandatory.
- **Intelligence Extractor v3** (`content_hash b65cee8d4064a30e`) — explicit two-step Option B navigation (no `--include-related` since `source_entity_id` is polymorphic), mandatory Touchpoint transition to `processed` at end (every time, even on thin content), investor/partner side rule (Signals only).

**Reactivation test (B1.5) — kill switch fired.** Reactivated Email Classifier and ran `indemn email reprocess` on Diana@CKSpecialty's email. EC processed 4 messages during the brief activation. Justin Li's email (single 1.0 match for both Contact + Company) classified correctly — clean ✅. Diana's email (multi-candidate case — 2 1.0 Contacts + 1 1.0 Company by domain) **violated the skill rule**: EC auto-created a NEW Company despite "Never auto-create a Company" being explicit, did NOT mark needs_review on multi-match ambiguity, did NOT transition the email. **Suspended EC immediately. Rolled back the auto-created Company. Reset Diana's email to received.** Verified the v3 skill content IS in the database (correct content_hash, "Never auto-create" + "Multiple 1.0 candidates → needs_review" both present) — so the skill loaded but the agent didn't comply on the harder branch. **This is a skill-compliance gap requiring deeper investigation. Reactivation halted.** Possible remedies: more explicit decision-tree language, force_reasoning rule that vetoes Company create from EC entirely, eval-driven fine-tuning. Out of scope for this session.

**Bugs reported to os-learnings.md (per fork session's queue):**
1. **Silent-stuck-state regression** (🟡 Partial regression) — Apr 27 `agent_did_useful_work()` helper has a coverage gap. When agent makes read-only `indemn` calls AND ends with "nothing to extract" final content, message marked completed even though the entity wasn't transitioned. Surfaced on IE running on the manual Touchpoint (Touchpoint stuck at `logged`, 0 entities created, message completed in 18s).
2. **`--include-related` doesn't follow polymorphic Option B `source_entity_id` refs** (🟡 Workaround in skill) — the field has `is_relationship: false` because the target type is polymorphic (Email | Meeting). Worked around in IE skill v3 with explicit two-step navigation. Long-term kernel option: `is_polymorphic_relationship: true` with `target_type_field: "source_entity_type"`.
3. **List endpoint filter regression** (🟡 Regression) — `data` filter on non-relationship fields silently ignored, even unknown-field filters return 200. The Apr 27 `_parse_list_filter` fix appears to have regressed. Workaround in TS skill: prefer back-reference fields for idempotency.

**Status of pipeline associates after this session:** Email Classifier — suspended (kill switch). Touchpoint Synthesizer — suspended (unchanged). Intelligence Extractor — active (was active before, untouched). Three new skill versions deployed but only IE's hasn't been triggered autonomously yet.

**Where this leaves Phase B1:** Most B1 items are in good shape (entity_resolve activated correctly, Path A cleanup done, skills written). The skill-compliance gap on multi-candidate cases blocks broad reactivation. Single-email reprocess (with kill-switch readiness) is still safe IF we pick single-1.0-match scenarios; multi-candidate scenarios surface the gap and need to be handled before letting EC run on the queue.

---

**Session 2026-04-28 (morning — trace-showcase HTML + share to Cam + Kyle) — Deliverable 2 complete. Phase A fully closed.**

Built `artifacts/2026-04-27-alliance-trace-showcase.html` — single self-contained HTML file (833 lines, all CSS inline, system fonts, no external resources) using the GR Little v2 Kyle-validated visual language as the base. Five-section spine: (1) v2 proposal cover hero with 3-card Phase strip + iris CTA button linking to the Drive PDF, (2) vertical timeline of the 8 Alliance events Feb 1 → Apr 27 with source pills (Meeting / Email+Doc / Slack / Slack DM / Slack MPDM / OS) + per-event entity-pill chips, (3) extraction table of 10 quotes-to-entities-to-proposal-lines with source-tag pills capturing the multi-source provenance (Apr 8 meeting, Apr 7-8 prep thread, Capability Doc, v1 proposal, etc.), (4) mechanism table with PROPOSAL "you are here" + Demo "skipped" honest about Alliance's actual journey, (5) 6-column proposal-as-spine showing v1 (sent → under_review) → v2 (rendered, now). Footer has the full constellation entity-ID inventory.

**Uploaded the v2 styled PDF to Cam's proposals folder on Drive** (`1k0_SYLdYtlM40y6W-ZAMV3Trr9hIq2Ph`) — file ID `1pM3tYg6rHzG8RW6xU_titouiq_iddhIC`, named "Alliance Insurance Services Proposal 4-27-26 (v2 — Renewal Stewardship Wedge).pdf". v2 now lives next to v1 in Cam's portfolio. Drive link is wired into the showcase HTML's hero panel.

**Posted to the MPDM channel `mpdm-cam--kwgeoghan--craig-1`** (Cam + Kyle + Craig, channel `C0A3B18LY07`) as a thread reply to Cam's Apr 21 message *"This is an opportunity to build our proposal output based on an actual case — can we discuss whether we can turn something around quickly and use this as an operational build?"* — with `reply_broadcast=True` so the message also surfaces at channel-top level. Tagged Cam (`<@U084CSZV49G>`) and Kyle (`<@UFK4DNNHG>`). HTML file uploaded twice — once into the thread and once at channel-top level — so it's visible from both views (Slack thread broadcasts don't carry thread file attachments to the main channel feed). Message frames the work as: customer-success-system work with Kyle, sales lifecycle modeled as entities, all Alliance data traced through, v2 proposal generated, looking to ship as a prototype product Cam + Kyle can both use and give feedback on. Proposal generation is the last step in the puzzle — the system covers everything that leads up to it.

**Phase A is now fully complete.** Two stages traced (DISCOVERY via GR Little, PROPOSAL/NEGOTIATION via Alliance) + both Kyle-facing artifacts shipped (GR Little visual Apr 24, Alliance showcase Apr 28). Mechanism validated across two distinct stages. Phase B (build to autonomous) is the next work.

**Session 2026-04-27 (evening — styled-PDF iteration) — Deliverable 1 complete. Deliverable 2 (Kyle showcase HTML) deferred to next session.**

Built the reusable Indemn proposal rendering pipeline: `templates/proposal/{template.hbs, template.css, saas-agreement.partial.hbs, assets/}` + `tools/render-proposal.js` (Node + puppeteer-core + handlebars). Rendered Alliance v2 to a 9-page styled PDF that matches Cam's portfolio (Alliance v1, Charley, Branch, Johnson, GIC, Physicians Mutual, Arches) — verified through several rounds of visual diff against Cam's actual PDFs. Updated Document `69efbdea4d65e2ca69b0dd80` in OS with `mime_type: application/pdf`, `file_size: 178935`, content pointer to the rendered file. Documented the recipe + 15 style guidelines + JSON contract in the new `skills/artifact-generator.md` skill. Outstanding: deliverable 2 (Kyle-facing trace-showcase HTML for Alliance) is the next-session top priority — visual template lives at `artifacts/2026-04-24-information-flow-v2.html`.

**Session 2026-04-27 (morning — vision-and-roadmap crystallization + Alliance v2 trace) — Both deliverables shipped.**

**Phase A2 Alliance trace executed end-to-end.** Walked Alliance through 8 timeline steps (Feb 1 discovery call → Feb 11 v1 proposal sent → Feb 18-19 Capability Document → Feb 26 PDF DM to Ryan → Apr 7-8 Retention Associate prep thread → Apr 8/9 renewal-wedge pivot meeting → Apr 21 BT Core integration emails → Apr 27 Artifact Generator produces v2). Acted as each associate (Email Classifier, Touchpoint Synthesizer, Intelligence Extractor, Artifact Generator) playing the role the autonomous flow would. Wrote real entities to the OS via `indemn` CLI throughout — not bulk-loaded.

**Outputs:**
- `artifacts/2026-04-27-alliance-trace-plan.md` — pre-execution plan, reusable template for any future trace
- `artifacts/2026-04-27-alliance-trace.md` — the trace narrative, every step with trigger / associate / inputs / outputs / gaps
- `artifacts/2026-04-27-alliance-proposal-v2.md` — **the v2 Alliance proposal artifact for Cam** (the deliverable). Renewal-wedge Phase 1 + lighter Phase 2/3. Anchored on real Alliance numbers (9,516 accounts / 16,744 policies / 3,946 in next 90 days / 77% callback rate). Compound-outcome packaging.
- `fork-prompts/os-bugfix-resume.md` — living resume prompt for parallel OS-bugfix sessions

**Hydrated entity graph in dev OS `_platform` org:** 1 Company refined, 2 Contacts (Christopher Cook + Brian Leftwich), 1 Meeting, 3 Documents (v1 + Capability Document + v2 rendered), 7 Touchpoints, 6 Operations, 8 CustomerSystem (incl. BT Core, carrier portal cluster, Conga Sign), 7 Opportunities (mapped to AssociateTypes + Phases), 2 Proposals (v1 → rejected/should-be-superseded; v2 → internal_review), 3 Phases on v2, 5 Decisions, 9 Commitments, 13+ Signals, 1 Playbook record (PROPOSAL stage — reusable for all customers).

**OS gaps surfaced + logged to `os-learnings.md`** (in addition to the bugs already fixed by the parallel bugfix session — Touchpoint Option B, silent stuck-state, cache leak, API 500-detail, Meeting create / Bug #30, entity-skill JSON examples, Bug #29 route eviction): Bug #30 fix needs propagation to all entities (esp. Email.external_ref); stale entity-skill rendering (skills aren't auto-regenerated when generator changes); Email.interaction → Touchpoint rename gap; **Slack ingestion is foundational, not deferrable Phase F**; Document.source enum missing slack_file_attachment; Proposal state machine lacks `superseded` transition; Bug #10 backfill continues to bite; Touchpoint scope/Contact-resolution chicken-and-egg.

**Bugfix session in flight in parallel** (forked from this main session at the start of the trace). Per `os-learnings.md` updates: Touchpoint Option B 🟢, silent stuck-state 🟢, cache leak 🟢, API 500-detail 🟢, entity-skill JSON examples 🟢, Bug #30 sparse-on-nullable 🟢, Bug #29 route eviction 🟢. Substantial unblocking landed. The shared-context-update mechanism (this main session logs findings to os-learnings.md → bugfix session reads them on resume → no human-in-the-middle handoff) worked end-to-end as designed.

---

**Session 2026-04-27 (vision-and-roadmap crystallization) — `vision.md` and `roadmap.md` created at project root. Comprehensive pre-scoped roadmap established.**

Long brainstorm with Craig to refine and crystallize the customer-system vision and roadmap into shared context for future work and parallel sessions. Established the customer-system equivalent of the OS repo's documentation system: `vision.md` (slow-changing end-state) + `roadmap.md` (living source of truth, where-we-are + path) at project root, both referencing depth in `os-learnings.md` and dated artifacts.

**Vision crystallized.** Dual-track frame: customer-system is the first build on the OS AND the mechanism by which the OS becomes capable of running all our systems. End objective is to use the OS the way it was designed; bug resolution is a means; usability is the end. Compute is not a constraint, so the OS bug list converges to zero on what we choose to prioritize. Forking parallel sessions is first-class.

**End-state articulated as:** foundation (9 structural capabilities — entity model complete, entity-resolution kernel capability, pipeline associates running hands-off, watch cascade reliable, constellation queryable, Temporal manageable, OTEL+LangSmith observable, OS dogfooded as designed, bugs converged) + two threads on top (meeting view, proposal generation). Hydration of all customers/prospects implied in achieving foundation, not separate. Horizons (2) all-Indemn-on-OS and (3) external-customer-ready visible but not in scope.

**Roadmap pre-scoped:** Phase A (shake-out traces, in flight — A1 GR Little done, A2 Alliance proposal today) → Phase B (build to autonomous: B1 foundation hardening, B2 hydration including Drive ingestion, B3 stage research + Playbook completion, B4 autonomous artifact production) → Phase C (dashboarding) → Phase D (adoption, light) → Phase E (evaluations + continuous improvement loop, feedback mechanism active throughout) → Phase F (Slack ingestion, deferred). Continuous threads: OS bug convergence (forks start at Phase B), OS-side milestones, shared-context-update mechanism. Persistent-AI loops explicitly out of scope (separate future system).

Today's session A2 (Alliance proposal trace) about to begin.

**Session 2026-04-22/23/24 (roadmap session) — Entity model designed, implemented, pipeline proven E2E. Team email backfill complete.**

Deep brainstorming session with Craig redesigning the entity model, then executed Waves 1-3.

**Design phase:** Produced two companion documents: field-level specs (entity-model-brainstorm.md) and design rationale (entity-model-design-rationale.md). 10 new/renamed entities, 4 updated, 1 removed. 3 associate skills designed. 4 watches defined. 8 design principles established.

**Implementation phase (Waves 1-3 complete):**
- Wave 1: All entity definitions created on live OS (26 entities total). Fixed missing status fields on 6 entities.
- Wave 2: 3 skills created (email-classifier, touchpoint-synthesizer, intelligence-extractor v2 with execute tool instructions), 3 roles with watches (email_classifier, touchpoint_synthesizer, intelligence_extractor), 3 associate actors created and activated with async runtime.
- Wave 3: Gmail adapter built and deployed. `indemn email fetch-new --data '{"user_emails": ["kyle@indemn.ai"], "since": "2026-04-21"}'` works. Emails ingested from Kyle's Gmail.

**Pipeline proven E2E:** Email created → watch fires → Email Classifier uses `execute` tool with `indemn` CLI → classifies email, links Company → Touchpoint Synthesizer fires → creates Touchpoint entity → Intelligence Extractor fires. Full cascade working.

**Infrastructure bugs fixed:** Queue processor WorkflowAlreadyStartedError (Python 3.12 + wrong temporalio exception class). Runtime service token regenerated. Entity `touchpoint` field renamed from `interaction` (naming conflict with kernel entity).

**Pipeline complete:** ~930 emails ingested (Kyle full week + team unique emails). 284 irrelevant, 180 processed E2E, 114 needs_review. 20+ Touchpoints, 20+ Tasks, 16+ Signals, 9+ Commitments, 4+ Decisions extracted. Queue drained to zero. 30 concurrent max reliable for one Railway container.

**Remaining tasks:**
- Set up recurring email fetch (scheduled associate)
- Alliance email backfill (manual curation via gog for historical emails)
- Meeting backfill (full 30-day history)
- Wave 4: human enrichment (Operations, Opportunities, CustomerSystem via CLI)
- Wave 5: proposal generation

**Session 2026-04-21 (roadmap session) — Meeting ingestion pipeline built E2E. Employee entity seeded. Actors cleaned up.**

Google Meet API adapter captures everything: conference records, structured transcripts (speaker+timestamp, 30-day expiry), Gemini smart notes, recordings, Calendar attendees. 20 meetings ingested for April 20-21. 15 employees seeded from Google Admin SDK + Slack + Actor linkage. 11 junk actors deleted, 3 fixed, 3 created. `indemn meeting fetch-new` works end-to-end. `fetch_new` kernel capability is a new generic pattern (collection-level, creates entities from external systems).

**Session 2026-04-21 (customer-system session) — P0 + P1 complete. System ready for team.**

Read Kyle's EXEC folder (PLAYBOOK-v2, data dictionaries, 6 leads, MAP) and Cam's Proposals folder (6 customer proposals). Deal entity extended, SuccessPhase entity created, 6 deals populated. P0: trace bug, deep links, Kyle login, activity feed. P1: detail view with inline editing + side panel, custom domains (os.indemn.ai + api.os.indemn.ai), CLI published (v0.1.0 on GitHub), repo setup with README + getting-started + white paper + domain-modeling skill + `indemn init` with Session Startup protocol.

**Previous: Session 2026-04-20** — UI polished (15 improvements), assistant refactored to split pane with streaming + entity rendering. CI fully green.

**Previous: Session 2026-04-14/15 (session 1)** — Design and data prep. Full context gathered, problem statement, domain model, data CSVs prepared.

### What's Done
- All source materials gathered and inventoried (28 Kyle docs, CRM sheet, 10+ transcripts, Ganesh's repo)
- Problem statement: 7 concepts with evidence, success criteria, risks
- System capabilities: 17 functional areas, ~130 specific capabilities attributed to sources
- Vision & trajectory document: phased roadmap, shared with Kyle and Cam
- Phase 1 domain model: 14 entities (11 domain + 3 reference), fields, state machines, relationships
- Entity criteria framework for OS domain modeling (7 tests)
- Data prep: 87 companies, 92 contacts, 24 associate types, 2 conferences — imported and live
- 14 human actors with team_member role
- UI: auto-generated views, search/filter/sort/pagination, state transitions, changes timeline, assistant split pane, activity feed, inline editing, detail panel
- **Meeting ingestion pipeline**: Google Meet API → Meeting entities with transcripts, notes, participants, recordings
- **Employee entity**: 15 team members seeded with Google IDs, Slack IDs, Actor linkage
- **Deal entity**: 6 active deals for Kyle's prospects (FoxQuilt, Alliance, Amynta, Rankin, Tillman, O'Connor)
- **SuccessPhase entity**: created, ready for per-deal phase data
- **Custom domains**: os.indemn.ai (UI), api.os.indemn.ai (API)
- **CLI v0.1.0 published**: install script + GitHub release
- **`fetch_new` kernel capability**: generic collection-level pattern for entity ingestion from external systems
- **Google Workspace Integration**: domain-wide delegation, Meet API + Calendar API + Admin SDK + Drive API

### What's Next
1. **Fix Kyle actor** — re-set password on correct actor ID (cleanup deleted the old one)
2. **Employee resolution in meetings** — match participants to Employees + Contacts, populate team_members/contacts fields
3. **Meeting classification** — customer vs internal, link to Companies
4. **Extraction associate** — process meetings into Tasks, Decisions, Signals, Commitments
5. **Full 30-day meeting backfill** — currently only April 20-21 ingested
6. **Kyle's prospect dashboard** — Deals + Tasks + assignees + due dates
7. **SuccessPhase data** — populate when phase info comes from Kyle syncs
8. **Staleness detection** — watches not configured yet
9. **UI: `[object Object]` fix** — participants list field renders as `[object Object]` in table

### Key Numbers
| Entity | Count |
|--------|-------|
| Companies | 88 (87 + Amynta) |
| Contacts | 92 |
| Deals | 6 |
| Meetings | 20 (April 20-21) |
| Employees | 15 |
| Human Actors | 15 (cleaned up) |
| Associate Types | 24 |
| Conferences | 2 |
| Stages | 7 |
| Outcome Types | 4 |
| Entity Definitions | ~20 (domain + reference + new) |

## External Resources

| Resource | Type | Link |
|----------|------|------|
| OS API | Railway | https://api.os.indemn.ai |
| OS UI | Railway | https://os.indemn.ai |
| Chat Runtime | Railway | wss://indemn-runtime-chat-production.up.railway.app/ws/chat |
| indemn-os repo | GitHub | craig-indemn/indemn-os (main branch) |
| OS repo | GitHub | os-roadmap branch |
| Kyle's EXEC docs | Local | artifacts/context/kyle-exec/ |
| Cam's Proposals | Google Drive | Folder `1k0_SYLdYtlM40y6W-ZAMV3Trr9hIq2Ph` |
| Shared folder (Kyle + Cam) | Google Drive | `1KKH8juzCqVyRQ36h72nnB9Djdjy8m_j1` |
| White Paper PDF | Google Drive | `1Cr_F_K3a_I_iul7HgJqXv1IY8KieyS40` |
| Indemn CRM InsurTechNY | Google Sheet | `1B3QnzfS8IEM7cMN3ar9gSFRw8K8_viFmH-dEajQ9tQg` |

## Artifacts

| Date | Artifact | Ask |
|------|----------|-----|
| 2026-04-30 | [pricing-call-and-sheet-source-material](artifacts/2026-04-30-pricing-call-and-sheet-source-material.md) | Source material for the associate-pricing-framework work — Cam/Kyle/Craig pricing call decisions (Enterprise/Core split), Cam's pricing spreadsheet schema, existing Working Copy annotations Craig had begun. Anchor for the framework work. |
| 2026-04-30 | [associate-pricing-framework](artifacts/2026-04-30-associate-pricing-framework.md) | The working doc — reverse-engineer Indemn's actual delivery (skills + integrations + channels composed into associates) from current customers, map to Cam's 47-row pricing catalog, surface gaps. Living document that accumulates per-customer detail; § 0 has full session handoff for resumption; § 2.05 tracks catalog gaps. **Cam-assigned action item from Apr 30 pricing call.** As of 2026-05-01: 9 of 18 customers walked, framework rules established + applied retroactively, 4 catalog gaps tracked. |
| 2026-04-14 | [context/craigs-brain-dump](artifacts/context/2026-04-14-craigs-brain-dump.md) | Record Craig's raw notes from conversations with Kyle, George, Ganesh |
| 2026-04-14 | [context/source-inventory](artifacts/context/2026-04-14-source-inventory.md) | Complete inventory of all source materials — docs, sheets, transcripts, repos |
| 2026-04-14 | [problem-statement](artifacts/2026-04-14-problem-statement.md) | Comprehensive problem statement — 7 concepts with evidence, success criteria, risks |
| 2026-04-14 | [domain-model-thinking](artifacts/2026-04-14-domain-model-thinking.md) | Entity criteria framework, kernel vs domain boundary, design principles |
| 2026-04-14 | [system-capabilities](artifacts/2026-04-14-system-capabilities.md) | Comprehensive capabilities list — 17 areas, ~130 capabilities, attributed to sources |
| 2026-04-14 | [vision-and-trajectory](artifacts/2026-04-14-vision-and-trajectory.md) | Full vision document for Kyle — problem, capabilities by phase, trajectory |
| 2026-04-14 | [phase-1-domain-model](artifacts/2026-04-14-phase-1-domain-model.md) | Phase 1 spec — 14 entities with fields, state machines, relationships |
| 2026-04-19 | [known-issues](artifacts/2026-04-19-known-issues.md) | Kernel bugs (3 fixed, 5 open) + data quality notes |
| 2026-04-19 | [ui-evaluation](artifacts/2026-04-19-ui-evaluation.md) | Comprehensive browser evaluation — 10 working, 20 gaps with priority tiers |
| 2026-04-19 | [demo-readiness](artifacts/2026-04-19-demo-readiness.md) | CEO demo assessment — recommended flow, verified features, 14 known limitations for post-demo |
| 2026-04-20 | [session-handoff](artifacts/2026-04-20-session-handoff.md) | Full handoff — reading protocol, pre-flight, architecture, what works, what needs work |
| 2026-04-20 | [ui-and-assistant-session](artifacts/2026-04-20-ui-and-assistant-session.md) | UI polish (15 improvements) + assistant UX refactor (split pane, streaming, entity rendering) |
| 2026-04-20 | [meeting-ingestion-plan](artifacts/2026-04-20-meeting-ingestion-plan.md) | Meeting ingestion + prospect delivery plan — entity refinement, Google adapter research |
| 2026-04-21 | [meeting-ingestion-checkpoint](artifacts/2026-04-21-meeting-ingestion-checkpoint.md) | Mid-session checkpoint — all knowledge, decisions, open questions about meeting ingestion |
| 2026-04-21 | [meeting-ingestion-session](artifacts/2026-04-21-meeting-ingestion-session.md) | Full session record — adapter rewrites, entity work, data quality audit |
| 2026-04-21 | [session-handoff (customer-system)](artifacts/2026-04-21-session-handoff.md) | Parallel session handoff — Deal entity, SuccessPhase, UI work, domains, CLI |
| 2026-04-21 | [context/kyle-exec/](artifacts/context/kyle-exec/) | Kyle's EXEC documents — playbook, data dictionaries, prospect list, MAP |
| 2026-04-21 | [unified-handoff](artifacts/2026-04-21-unified-handoff.md) | Universal session handoff — reading protocol, all context, all work, what's next |
| 2026-04-21 | [context/kyle-craig-call-transcript](artifacts/context/2026-04-21-kyle-craig-call-transcript.txt) | Full 40K transcript of Craig+Kyle call — prospect strategy, deal priorities, next phase |
| 2026-04-22 | [entity-model-brainstorm](artifacts/2026-04-22-entity-model-brainstorm.md) | Entity model evolution — field-level specs for all 22 entities, relationships, state machines |
| 2026-04-22 | [entity-model-design-rationale](artifacts/2026-04-22-entity-model-design-rationale.md) | WHY the model is designed this way — thinking, tradeoffs, rejected alternatives, OS vision fit, Alliance test |
| 2026-04-23 | [playbook-as-entity-model](artifacts/2026-04-23-playbook-as-entity-model.md) | Key insight: the playbook IS the entity model. Gaps drive next steps. Proposal emerges when entities are complete. |
| 2026-04-23 | [system-flow-v4](artifacts/2026-04-23-system-flow-v4.html) | Visual diagram for Kyle — timeline as spine, Alliance journey, entity nesting, associate connectors |
| 2026-04-23 | [implementation-session](artifacts/2026-04-23-implementation-session.md) | Full session record — what was built, fixes, learnings, next steps, commits |
| 2026-04-23 | [pipeline-operations-guide](artifacts/2026-04-23-pipeline-operations-guide.md) | How to run, monitor, troubleshoot the pipeline — auth, CLI, Temporal, concurrency, known issues |
| 2026-04-24 | [playbook-entity-vision](artifacts/2026-04-24-playbook-entity-vision.md) | Aligned vision for Playbook entity — sales motion as data, stage-indexed, terminus is Proposal, closes the loop with artifact generation at every stage |
| 2026-04-24 | [trace-plan-and-design-notes](artifacts/2026-04-24-trace-plan-and-design-notes.md) | GR Little trace plan + open design question on reliable Company/Contact identity (creation on first touch is fine; problem is duplicate identification) |
| 2026-04-24 | [extractor-pipeline-gap](artifacts/2026-04-24-extractor-pipeline-gap.md) | **Fundamental pipeline gap found** — Intelligence Extractor has no path from Touchpoint to source Meeting/Email content. Three fix options (skill recipe / Touchpoint source pointer / kernel reverse-lookup). Plus: cross-invocation tool cache leak, silent workflow stuck-state |
| 2026-04-24 | [extractor-procedure-and-requirements](artifacts/2026-04-24-extractor-procedure-and-requirements.md) | **The procedure the autonomous Extractor must follow** (step-by-step) + **9 OS capabilities** it needs. Reproduces what Claude did manually for GR Little. Top-4 priority list for making the Extractor autonomous |
| 2026-04-24 | [grlittle-followup-email-draft](artifacts/2026-04-24-grlittle-followup-email-draft.md) | **The Kyle-facing demo artifact** — follow-up email draft for GR Little Apr 22 discovery call. Shows the Playbook's `artifact_intent` + extracted Tasks/Decisions/Commitments/Signals rendering into the email |
| 2026-04-24 | [information-flow (HTML)](artifacts/2026-04-24-information-flow.html) | **Visual one-pager v1** — end-to-end information flow, narrative form. Superseded by v2 after Apr 24 design dialogue |
| 2026-04-24 | [information-flow-v2 (HTML)](artifacts/2026-04-24-information-flow-v2.html) | **Visual one-pager v2 (Kyle-facing)** — email hero, temporal timeline, extraction quote→entity→email-line table, mechanism table (stage-by-stage), Proposal spine growth. Less text, more visual, shows same-mechanism-every-stage pattern |
| 2026-04-24 | [design-dialogue-playbook-artifact-proposal](artifacts/2026-04-24-design-dialogue-playbook-artifact-proposal.md) | **Key design dialogue** — Playbook consulted twice (by Extractor and Artifact Generator), Proposal hydrated from DISCOVERY forward as the spine, Opportunity-vs-Problem question, Document-as-artifact pattern. Refinements to apply + open questions |
| 2026-04-24 | [context/kyle-craig-sync-transcript](artifacts/context/2026-04-24-kyle-craig-sync-transcript.txt) | **Full Apr 24 Kyle+Craig sync transcript** — Gemini auto-notes + verbatim transcription. Pulled from Drive after the meeting. |
| 2026-04-24 | [kyle-sync-recap](artifacts/2026-04-24-kyle-sync-recap.md) | **Recap of Apr 24 Kyle sync** — Kyle's reactions to the email/diagram (loved it, "too long, ship as v0, socialize per meeting"), 6 priorities for what's next (dashboards top), team direction, action items |
| 2026-04-25 | [session-handoff-and-roadmap](artifacts/2026-04-25-session-handoff-and-roadmap.md) | **Single starting point for the next session** — what we did Apr 24, current entity state, architectural decisions aligned, open questions, bug status, and the agreed 4-phase roadmap (Hydration → Reliability → Artifact path → Adoption) with parallel observatory track |
| 2026-04-27 | [CLAUDE.md (project)](CLAUDE.md) | **Project orientation document — read FIRST in every session.** Carries the cumulative thinking as substance, not just structure. Sections: vision (substantive), unifying frame (customer-system IS the OS first build, not a sister project), **the journey across sessions** (Apr 14 → Apr 27 with WHY at each step), **The OS — what it is and how to internalize it** (6 primitives, 7 kernel entities, save_tracked invariant, selective emission, the 8-step process, REQUIRED OS reading from /Users/home/Repositories/indemn-os/), **Learnings flow back to the OS** discipline (every gap is OS work; fork strategy; real examples from Apr 24; pointer to `os-learnings.md` as the running register), **cumulative thinking distilled** (problem, entities-are-the-system, flow, Playbook insight, Apr 24 refinements, Alliance test), **19 foundational design principles** with feedback-memory references, current state, what's next, **two-track REQUIRED READING** (project + OS), Files Index (project + OS repo paths + companion product-vision project), When-working-on-X router (incl. OS-side work), INDEX.md as living history pointer, OS bug capture and fork strategy, What-you-DO-NOT-do, tone-and-voice closing. |
| 2026-04-27 | [os-learnings.md](os-learnings.md) | **Running register of OS-relevant findings** — bugs, capability gaps, design questions, ergonomics — surfaced by customer-system work. Ranked by impact: critical/blocking (Touchpoint source pointers / Option B, Entity-resolution capability, Bug #29, Bug #23), high (Bugs #16/17/22/9, --include-related reverse refs, cross-invocation cache leak, silent workflow stuck-state, list endpoint filtering, Bug #10, #18), medium (Bugs #25, #26, #27, #19, #20, #21, generated entity skill, #5, #6, #11, #12, #15, #2, #3, #4, #7, #8, #13), open design (Opportunity vs Problem, Document-as-artifact for emails, 12 sub-stages, origin tracking, Playbook hydration). Plus historical fixed (#1, #14, infrastructure fixes pre-Apr 24). The handoff document — read at start of any OS-touching session; append when findings surface. |
| 2026-04-27 | [vision.md](vision.md) | **Slow-changing end-state articulation for the customer-system project.** Three sections: (1) the dual-track frame (customer-system is the first build on the OS AND the mechanism by which the OS becomes capable of running all our systems; using the OS as designed is the end objective; bug resolution is a means); (2) end-state articulated as foundation (9 structural capabilities) + two threads on top (meeting view + proposal generation), with horizons (2) all-Indemn-on-OS and (3) external-customer-ready as visible but out-of-scope context; (3) the breakthrough as the lens (entity-model-IS-playbook → Proposal-as-spine → same mechanism every stage). Cross-links to roadmap, os-learnings, key artifacts, OS docs. Lean spine file at project root — depth lives in artifacts. |
| 2026-04-27 | [roadmap.md](roadmap.md) | **Living source of truth for how we get to the vision.** Updated every session that moves work forward. Where-we-are-now snapshot. Phases A→F pre-scoped (A shake-out traces in flight — GR Little done, Alliance today; B build-to-autonomous with sub-phases B1 foundation hardening, B2 hydration including Drive ingestion, B3 stage research + Playbook completion, B4 autonomous artifact production; C dashboarding; D adoption; E evaluations + feedback loop; F Slack ingestion deferred). Continuous threads: OS bug convergence (forks start at Phase B), OS-side milestones, shared-context-update mechanism. Persistent-AI loops explicitly out of scope. Cross-links to os-learnings, dated artifacts, OS docs. Supersedes 2026-04-25 session-handoff-and-roadmap as the active roadmap. |
| 2026-04-27 | [fork-prompts/os-bugfix-resume.md](fork-prompts/os-bugfix-resume.md) | **Living resume prompt for the parallel OS-bugfix session.** Living document — paste-ready prompt + Recent fork-session progress snapshot (currently shows 7 fixes/in-progress). Used after `/clear` in the bugfix session to bootstrap from durable shared context (`os-learnings.md` is the work queue). Sister fork-prompts will go in this directory for any future parallel-session work. |
| 2026-04-27 | [artifacts/2026-04-27-alliance-trace-plan.md](artifacts/2026-04-27-alliance-trace-plan.md) | **Pre-execution plan for Alliance v2 trace.** Sources, what gets written to OS (group-by-group), sequence, questions for Craig. Reusable template for any future trace. |
| 2026-04-27 | [artifacts/2026-04-27-alliance-trace.md](artifacts/2026-04-27-alliance-trace.md) | **The Alliance trace narrative.** Walked Alliance through 8 timeline steps (Feb 1 → Apr 27) acting as each associate (Email Classifier, Touchpoint Synthesizer, Intelligence Extractor, Artifact Generator). Every step documented with trigger / associate / inputs / outputs / gaps. Hydrated 60+ entities total (1 Company refined, 2 Contacts, 1 Meeting, 3 Documents, 7 Touchpoints, 6 Operations, 8 CustomerSystem, 7 Opportunities mapped to AssociateTypes + Phases, 5 Decisions, 9 Commitments, 13+ Signals, 1 Playbook record). |
| 2026-04-27 | [artifacts/2026-04-27-alliance-proposal-v2.md](artifacts/2026-04-27-alliance-proposal-v2.md) | **The v2 Alliance Proposal — the deliverable for Cam.** Renewal-wedge Phase 1 (Retention Associate compound = Inbox + Knowledge + custom skills, $2,500/mo month-to-month). Phase 2 (Lead Capture bilingual). Phase 3 (Service expansion 24% high-automation tier $5K-10K/mo 12-month). Anchored on real Alliance numbers + named operations + named carriers + the BT Core integration approach. Includes line-to-entity mapping table proving every claim traces to a real OS entity. **Markdown form — next session will re-render styled to match Cam's actual proposal templates from Drive.** |
| 2026-04-27 | [artifacts/2026-04-27-session-handoff-alliance-trace.md](artifacts/2026-04-27-session-handoff-alliance-trace.md) | **Comprehensive session-handoff for 100% knowledge transfer to next session. READ THIS FIRST in next session.** Captures what we set out to do, what happened (3 deliverables shipped today: vision/roadmap crystallization + Alliance v2 trace + parallel bugfix fork landing 7 bugs), current entity state with all key OS IDs, decisions aligned, open questions, gaps surfaced, AND — most importantly — Craig's two explicit next-session priorities: (1) re-render v2 proposal styled to match Cam's existing proposal templates from Drive `1k0_SYLdYtlM40y6W-ZAMV3Trr9hIq2Ph`; (2) build customer-facing trace-showcase HTML for Kyle in the spirit of `2026-04-24-information-flow-v2.html`. Includes the start-of-session protocol + what NOT to redo. **Updated 2026-04-27 evening with Session 7 addendum: deliverable 1 complete (styled PDF rendered + template/renderer/skill shipped); deliverable 2 still pending.** |
| 2026-04-27 | [templates/proposal/](templates/proposal/) | **Reusable Indemn proposal template** — `template.hbs` (Handlebars master HTML), `template.css` (brand styling: Barlow @font-face, eggplant text, iris/lilac swoosh, full-grid tables, centered ALL-CAPS SaaS title), `saas-agreement.partial.hbs` (standard 8-section SaaS Agreement, identical across customers), `assets/` (Indemn iris logo + 4 Barlow font weights). Edit centrally; every customer proposal picks up changes on next render. |
| 2026-04-27 | [tools/render-proposal.js](tools/render-proposal.js) | **Node renderer for proposals.** Uses puppeteer-core against system Chrome + handlebars. Args: `--data <json> --out <pdf> [--html <html>] [--template <hbs>]`. Produces brand-consistent PDFs. Footer is an inline SVG swoosh + white logo (CSS filter) + page number; per-page footer via puppeteer's `displayHeaderFooter` + `footerTemplate`. |
| 2026-04-27 | [artifacts/2026-04-27-alliance-proposal-v2.json](artifacts/2026-04-27-alliance-proposal-v2.json) | **Structured input for the renderer** — what the future autonomous Artifact Generator associate produces from the entity graph. Section types: framing, anchor, phase, table, raw_html, investment_summary, next_steps. Each phase: name + Focus + 3 bullets (Capability/Outcome/Investment). |
| 2026-04-27 | [artifacts/2026-04-27-alliance-proposal-v2.pdf](artifacts/2026-04-27-alliance-proposal-v2.pdf) | **The styled v2 proposal — the deliverable for Cam.** 9 pages, ~175 KB. Cover / Unlocking Revenue Capacity / Addressing the Implementation Gap / Three-Phase Roadmap (WEDGE/WALK/RUN) / Implementation Timeline / Implementation Roles & Commitment + Operational Guardrails / Investment Summary + Next Steps / SaaS Agreement / Acceptance & Authorization. Brand-consistent with Cam's portfolio (verified via visual diff against Alliance v1, Charley, Branch, Johnson, GIC, Physicians Mutual, Arches). |
| 2026-04-27 | [skills/artifact-generator.md](skills/artifact-generator.md) | **Artifact Generator associate skill — the recipe for producing stage-appropriate artifacts.** Covers: trigger conditions, 7-step procedure (resolve Playbook → gather entity inputs → compose JSON → render → update Document → transition Proposal → mark Touchpoint processed), JSON schema with section-type reference, **15 style guidelines distilled from Cam's portfolio + earned through Alliance v2 iteration** (brevity-over-completeness, no direct address, no v1/v2 framing, section-per-page rhythm, 28pt heading sweet spot, full-grid tables, etc.), file locations, future evolution. The future autonomous associate consumes this skill. |
| 2026-04-27 | [artifacts/2026-04-27-alliance-trace-showcase.html](artifacts/2026-04-27-alliance-trace-showcase.html) | **Kyle-facing Alliance trace-showcase visual — the Phase A2 deliverable 2 (next to the styled PDF as deliverable 1).** Single self-contained HTML file (833 lines, all CSS inline, system fonts, no external dependencies) using the Apr 24 GR Little v2 Kyle-validated CSS as the base. Five sections: (1) proposal cover hero with 3-card Phase strip (WEDGE / WALK / RUN) + iris CTA button to the Drive PDF, (2) vertical timeline of the 8 Alliance events Feb 1 → Apr 27 with source pills (Meeting / Email+Doc / Slack / Slack DM / Slack MPDM / OS) and per-event entity-pill chips, (3) extraction table — 10 quote-to-entity-to-proposal-line rows showing how every claim in v2 traces back to source (Christopher's Apr 7 stewardship-prep ask, Apr 8 renewal-wedge pivot, Peter's data validation, Capability Document, v1 proposal), (4) mechanism table with PROPOSAL stage "you are here" + Demo "skipped" honest about Alliance's actual journey, (5) 6-column proposal-as-spine showing v1 (sent → under_review) → v2 (rendered, now) with all 3 Phases populated under v2. Footer carries the full Alliance constellation entity-ID inventory. Shared in the `mpdm-cam--kwgeoghan--craig-1` channel (Cam + Kyle + Craig) on 2026-04-28 as a thread reply with `reply_broadcast=True`. |
| 2026-04-28 | [Drive: Alliance v2 PDF in Cam's proposals folder](https://drive.google.com/file/d/1pM3tYg6rHzG8RW6xU_titouiq_iddhIC/view) | **The styled v2 proposal uploaded to Drive** — file ID `1pM3tYg6rHzG8RW6xU_titouiq_iddhIC` in folder `1k0_SYLdYtlM40y6W-ZAMV3Trr9hIq2Ph` (Cam's proposals folder, where v1 + the rest of Cam's portfolio live). Filename "Alliance Insurance Services Proposal 4-27-26 (v2 — Renewal Stewardship Wedge).pdf". Shareable to Cam + Kyle via folder inheritance. Drive link is the canonical "view the full proposal" target wired into the trace-showcase HTML's hero panel. |
| 2026-04-28 | [artifacts/2026-04-28-session-9-handoff.md](artifacts/2026-04-28-session-9-handoff.md) | **LOAD-BEARING SESSION-9 HANDOFF — read first in next session.** Hard-codes the two-track mandatory reading list (Track 1 customer-system artifacts + Track 2 OS docs at `/Users/home/Repositories/indemn-os/`) with absolute paths and a coverage check (sample questions to verify reading is loaded). Captures the Session 9 work end-to-end: trace-as-build-method captured durably; Phase B1 plan artifact built; Path A cleanup (79 orphan Companies + 91 dupe Contacts deleted, capability activations); Phase B1 trace through 3 scenarios; Email.interaction → Email.touchpoint rename completed as ROOT-CAUSE FIX; three skills updated (EC v3, TS v6, IE v3); B1.5 reactivation kill switch fired (Diana@CKSpecialty multi-candidate case violated skill rule); 3 OS bugs reported. Next-session priority: close the EC skill-compliance gap (3 options outlined — more-explicit language, force-reasoning veto rule, eval-driven testing). Plus do-not-skip reminders codified from this session's discipline failures (don't skip Track 2; don't skip Kyle transcript; don't write workarounds when root cause is fixable; don't propose recommendations without plans; trace-as-build is mandatory). The handoff is itself a template for future sessions — should be written at end of every substantive session. |
| 2026-04-28 | [artifacts/2026-04-28-phase-b1-data-state-and-trace-plan.md](artifacts/2026-04-28-phase-b1-data-state-and-trace-plan.md) | **Phase B1 working plan — entity-resolution skill propagation** (404 lines). Captures live OS data state inventory (Companies 314 / Contacts 424 / Emails 1,138 / Touchpoints 78 + dupe maps + pipeline associate state); 3 structural gaps (Contact entity_resolve not activated; Company entity_resolve missing domain strategy; Touchpoint Option B backfill); Path A lightweight cleanup pass (capability activations → bulk-delete orphans → resolve high-volume Contact dupes); Phase B1 trace plan B1.1 → B1.7 (pick scenarios → trace as Email Classifier → trace as Touchpoint Synthesizer → write skills → reactivate one at a time → watch full cascade); explicit out-of-scope list (9 items deferred); decisions log (8 entries appended through execution including the kill-switch reactivation finding); execution checklist. The artifact is the working memory for Phase B1 and gets carried forward across sessions. |
| 2026-04-28 | [artifacts/2026-04-28-shared-roadmap-with-kyle.md](artifacts/2026-04-28-shared-roadmap-with-kyle.md) | **Shared roadmap with Kyle — alignment artifact from the 4/28 sync.** Short Kyle-facing doc (~430 words) distilling the customer-system roadmap into named deliverables, named customers, explicit deferreds. Five immediate items: customer-snapshot view, voice-dump ingestion, Armadillo/Amenta/TFG end-to-end runs, proposal generator iteration from Alliance v2 baseline, playbook hydration from 1-month historical data. Kyle as guinea pig (daily use end-of-week target or within 2 weeks); team adoption (Cam, George, Peter, Ganesha) is the objective after. Cam owns proposal close substance; we iterate the mechanism. Deferreds named: 12-20 sub-stages (emerges from playbook hydration), insurance-program data model (long-horizon foundation for multi-carrier OS — EventGuard template scaled), Stripe/AI-associate metering (parallel track), full historical hydration past 1 month. Pushed to Drive (`1VZMcoLY212RL5iyzekJ0pLONqfPE5IlF`, in Kyle+Cam shared folder `1KKH8juzCqVyRQ36h72nnB9Djdjy8m_j1`); shared with Kyle as writer; Slack DM sent 2026-04-29. Living doc — revisit once Kyle has daily-use behind him. |
| 2026-04-29 | [artifacts/2026-04-29-armadillo-followup-email-draft.md](artifacts/2026-04-29-armadillo-followup-email-draft.md) | **Follow-up email draft for Armadillo from the Apr 28 discovery call.** Generated per the DISCOVERY-stage Playbook `artifact_intent` (1) thank + reinforce specific pain by name, (2) propose concrete next step, (3) include materials David asked for, (4) acknowledge champion. Iterated for warmth: references "smack across the face" verbatim from David's call, the 9pm-Sunday framing for time-extender, the Marlon ↔ CoverWallets connection. Markdown source for the email body that lands in the trace-showcase HTML. Includes line-to-entity mapping table (every claim traces back to extracted entities from Touchpoint `69f22385…446a`). Mechanism explanation at the bottom — same `(Deal + Touchpoints + intel + Playbook + raw transcript) → Artifact Generator` shape as GR Little / Alliance, only the Playbook record differs. |
| 2026-04-29 | [artifacts/2026-04-29-armadillo-trace-showcase.html](artifacts/2026-04-29-armadillo-trace-showcase.html) | **Kyle-facing HTML showcase for the Armadillo trace.** Five-section spine following the GR Little v2 pattern: (1) email hero with the rendered draft + headers, (2) vertical timeline (Apr 2 InsurtechNY intro → Apr 28 discovery → Apr 28-29 trace + intel → Apr 29 artifact "you are here"), (3) extraction table (10 rows mapping transcript quotes with timestamps + speaker → entities created → email lines fed), (4) mechanism table with Discovery as "you are here" + Contact stage completed + future stages preview, (5) proposal-as-spine showing 0 Phases / no terms yet at Discovery + projection through Demo / Proposal / Negotiation / Verbal / Delivery. All CSS inline, no external dependencies. Shared with Kyle 2026-04-29 as a Slack file attachment (not a Drive link — Drive renders HTML as raw source). Caveat-framed in the share message: "still in flux, building intuition for what each stage produces, not a polished deliverable." Section 6 ("R&D findings") was drafted then removed before sending — internal-only, not for Kyle; gaps are tracked separately in `os-learnings.md`. |
| 2026-04-30 | [skills/log-touchpoint.md](skills/log-touchpoint.md) | **`log-touchpoint` operating skill — manual touchpoint entry via assistant.** TD-1 sub-piece 6. Walks the assistant through collecting customer Company / participants / date / scope / verbatim summary. Resolves Company + Contacts via `entity-resolve` first (no fabrication, no auto-create without confirmation). Creates Touchpoint with `source_entity_*=null` (manual path bypasses TS but still triggers IE on `Touchpoint logged`). Uploaded + assigned to OS Assistant (skills count 23→24). Designed for chat-deepagents (web) and the future voice-deepagents harness (canonical rebuild pending). |
| 2026-04-30 | [artifacts/2026-04-30-slack-adapter-design.md](artifacts/2026-04-30-slack-adapter-design.md) | **Slack adapter design + scaffold (TD-1 sub-piece 4).** 265 lines. SlackMessage entity definition (16 fields + state machine). Adapter module skeleton at `kernel/integration/adapters/slack.py` (httpx-based Slack Web API, conversations.list/history with cursor pagination, file attachments → Documents). Architectural decisions resolved: direct Slack API (not third-party SDKs), all public/private channels, no DMs initially, polling first (Events API later), per-message granularity, `external_ref = "{channel_id}:{slack_ts}"` composite. Implementation completed in Session 14 (commit `c87376d`); live fetch blocked by Bug #45 — Integration dispatch lookup not finding active Integration. |
| 2026-04-30 | [artifacts/2026-04-30-voice-deepagents-runbook.md](artifacts/2026-04-30-voice-deepagents-runbook.md) | **voice-deepagents harness runbook — describes v1.** TD-1 sub-piece 7. **Note: v1 (commit `62f47f9`) is architecturally wrong per `docs/architecture/realtime.md` and will be DELETED + REBUILT next session.** v1 used `livekit.agents.Agent` with single custom `execute` tool — does NOT use deepagents framework, harness_common modules, Interaction/Attention lifecycle. v2 must mirror chat-deepagents structure (deepagents `create_deep_agent` + harness_common + three-layer config + DeepagentsLLM adapter wrapping the agent for LiveKit AgentSession). Runbook captures deployment env vars (LIVEKIT_URL, DEEPGRAM_API_KEY, CARTESIA_API_KEY, GCP_*, INDEMN_*), Railway deploy command, verification scenario. **Update after v2 rebuild lands.** |

## Decisions

- 2026-05-01: **Pricing Framework — Bundle-by-task rule for associate mapping** (load-bearing). Bundle when SAME agent does multiple tasks in one conversation, OR multiple agents are *orchestrated together* toward one objective. Split when DIFFERENT agents have DIFFERENT objectives, no shared orchestration. Applied retroactively across customers — JM 3→2, UG Mobile Home 2→1, Silent Sport 2→1, Branch 2-bundled→5-distinct.
- 2026-05-01: **Pricing Framework — Two skill types: tool skill (atomic, high reuse) + pathway skill (workflow, medium reuse).** Browser automation, field writes, navigation are IMPLICIT in `web operator` system integration type — not separate skills. Each pathway is composed FROM tool skills + integration capabilities + customer-specific logic.
- 2026-05-01: **Pricing Framework — Paths ARE skills.** Skill granularity is at the path level (end-to-end process), not function-call level. Skill specificity is emergent (LOB / carrier / system / customer rule encoded in skill name; no pre-baked specificity column).
- 2026-05-01: **Pricing Framework — Built-only catalog.** Only catalog what's actually built. Don't include things from proposals or planned work. But "built" includes prototypes — don't filter by live-vs-prototype.
- 2026-05-01: **Pricing Framework — Discipline:** surface findings → discuss with Craig → only write to doc after agreement. No willy-nilly authoring. No LOE estimates without grounding. No tier classifications (Core/Soon/Enterprise) — those are Craig's, not mine.
- 2026-04-30: **Pricing Framework — Cam's Enterprise vs Core product split.** Enterprise = custom proposals via the customer-system AI engine (the work in TD-1). Core = standardized outcomes, embedded pricing, ≤7-day deploy KPI, sold via website with self-service proposal generation. Flywheel: Enterprise IP → push down to Core → near-zero implementation lift.
- 2026-04-30: **Pricing Framework — Implementation cost formula:** `(channel integration cost) + (skill development cost) + (system integration cost)`. Channels and systems are at the deal level, NOT bound to individual skills. Multiple skills share the same channel + system integrations on a given customer.
- 2026-04-30: **Pricing Framework — Customer walk roster:** 12 active customers + 6 prospects with build history = 18. AI_Investments tier dropped to 2 (Family First, Silent Sport) per Craig; rest (mShift, Bonzah, InsuranceTrak, Surety First, eSpecialty, ConvergentRPS, Overlook VC, Sotant) excluded as not having material build/integration history worth reverse-engineering.
- 2026-04-14: This is the first implementation on the Operating System — proves and refines the OS domain modeling process
- 2026-04-14: Company entity covers the full lifecycle from prospect through churned
- 2026-04-14: AI populates everything — system designed for extraction, not manual entry
- 2026-04-14: One role for now (team_member, full access)
- 2026-04-14: Meeting intelligence (Decisions, Commitments, Signals, Tasks) are separate entities, not fields on Meeting
- 2026-04-14: Task is unified across all sources (meetings, implementations, manual) — one entity, one queue
- 2026-04-20: Assistant is a resizable split pane, not an overlay — peer to entity views
- 2026-04-20: UI deploys require manual `railway up` — not auto-deploy from git push
- 2026-04-21: Google Workspace adapter uses Meet REST API as primary discovery (not Drive filename search)
- 2026-04-21: `fetch_new` is a collection-level kernel capability — generic pattern for any entity type
- 2026-04-21: Meeting entity is agnostic to Google — adapter maps Meet/Calendar/Drive data to Meeting fields
- 2026-04-21: One associate for full meeting pipeline — fetch + extract (not split into two)
- 2026-04-21: Employee entity is domain data (customer success system), linked to Actor (OS primitive) via actor_id
- 2026-04-21: Participants field stores structured data (name, user_id, email, join/leave, attended/invited)
- 2026-04-21: Calendar attendees merged with Meet participants — shows both who was invited and who joined
- 2026-04-21: Company matching and meeting classification are post-processing (associate job, not adapter)
- 2026-04-21: Deal entity extended with deal_id, next_step, next_step_owner, use_case, proposal_candidate
- 2026-04-21: SuccessPhase entity — per-deal phased progression with entry criteria and go/no-go signals
- 2026-04-22: Entity model evolution — 7 new entities (Email, Document, Interaction, Operation, Opportunity, Proposal, Phase) designed in brainstorm
- 2026-04-22: Proposal is source of truth for what we deliver — the document is a rendering, not the source
- 2026-04-22: Phase replaces SuccessPhase — phases always live on Proposal, no phases without a proposal
- 2026-04-22: AssociateDeployment renamed to Associate — Associate is the deployed instance, AssociateType is the catalog
- 2026-04-22: No fluff fields — every field is structured data, entity relationship, or source-of-truth content. No keyword summaries.
- 2026-04-22: Documents for narrative, entities for structured facts — both live in the system, linked together
- 2026-04-22: Interaction covers both external (with customer) and internal (work done for customer) — captures full effort and timeline
- 2026-04-22: Alliance Insurance is the first target customer for full data hydration
- 2026-04-23: "Interaction" renamed to "Touchpoint" — naming conflict with kernel Interaction entity (chat/voice sessions)
- 2026-04-23: Associate skills must include explicit `execute` tool instructions with `indemn` CLI examples — without this, the agent doesn't know how to interact with the OS
- 2026-04-23: Gmail adapter built into Google Workspace integration — `fetch_emails()` method, `fetch_method` config parameter on `fetch_new` capability
- 2026-04-23: Queue processor bug fixed — `WorkflowAlreadyStartedError` removed from temporalio SDK, replaced with `RPCError` + `RPCStatusCode.ALREADY_EXISTS`
- 2026-04-23: Runtime service token regenerated — stored on Platform Admin actor + AWS Secrets Manager + Railway env var
- 2026-04-23: Pipeline E2E proven — Email → watch → Email Classifier (execute + indemn CLI) → Touchpoint Synthesizer → Touchpoint created
- 2026-04-23: Associate skills must be loaded via deepagents progressive disclosure — write to filesystem, pass paths. NEVER load into system prompt.
- 2026-04-23: Entity skills accessed on demand via execute("indemn skill get <Entity>") — NOT pre-loaded on associates
- 2026-04-23: 30 concurrent is the reliable ceiling for one Railway container — above that, asyncio.to_thread starves
- 2026-04-23: CLIError and ValidationError added to Temporal non-retryable errors — prevents infinite retry on bad data
- 2026-04-23: Duplicate key errors (E11000) in fetch_new treated as skips — handles cross-mailbox dedup edge cases
- 2026-04-23: When deploying workflow timeout changes, must redeploy temporal worker AND terminate running workflows — Temporal bakes timeouts at dispatch time
- 2026-04-24: Team email backfill complete — ~930 emails across 11 team members, pipeline drained to zero
- 2026-04-24: Playbook entity direction aligned — stage-indexed entity (one record per Stage), terminus is Proposal. Every stage produces an artifact via `(Deal + Touchpoints + extracted intel + Playbook for Deal.stage) → artifact`; Proposal generation is just artifact generation at the PROPOSAL stage. Playbook hydrates from historical extractions, NOT imported from Kyle's PLAYBOOK-v2. Full vision in `2026-04-24-playbook-entity-vision.md`.
- 2026-04-24: Do NOT route around pipeline bugs with deterministic matchers — LLM can classify email/meeting to Company; the 446-Company issue is an orchestration/skill/data-flow bug, not an LLM-capability bug. Find root cause, don't bypass.
- 2026-04-27: Vision document split established — `vision.md` (slow-changing end-state) at project root + `roadmap.md` (living source of truth) at project root. Both reference depth in os-learnings.md and dated artifacts. The customer-system equivalent of the OS repo's documentation system. Lean spine files; depth elsewhere.
- 2026-04-27: Customer-system "done" articulated as foundation (9 structural capabilities) + two threads on top (meeting view + proposal generation). Hydration of all customers/prospects implied in achieving foundation, not a separate item. Horizons (2) all-Indemn-on-OS and (3) external-customer-ready visible but explicitly out of this roadmap's scope.
- 2026-04-27: Comprehensive pre-scoped roadmap — Phases A→F + continuous threads (OS bug convergence, OS-side milestones, shared-context-update mechanism). All work is pre-scoped here, not deferred to "what emerges later." This session was for scoping all of it.
- 2026-04-27: OS bug list converges to zero on items we choose to prioritize. Compute is not a constraint. Forking parallel sessions to fix bugs is first-class operating mode, starting as soon as Phase B begins.
- 2026-04-27: LangSmith on all harnesses + deep OTEL tracing as foundation requirement (B1). Evaluations as feedback mechanism active throughout from now (Phase E starts in usage immediately, full coverage future build).
- 2026-04-27: Persistent-AI loops (the "Walker said yes → Peter scrapes → Ganesha reviews → Kyle shares" pattern Kyle described Apr 24) are explicitly OUT of scope for this roadmap. Separate part of the system, future design after Phase B/C in motion. The Commitment + Task entities support it structurally; the wiring is its own design problem.
- 2026-04-27: Phase A complete after A2 (Alliance proposal trace). Two stages traced (DISCOVERY + PROPOSAL/NEGOTIATION) is sufficient mechanism validation. Intermediate stages (CONTACT, DEMO, VERBAL/SIGNED) hydrate naturally from B3 stage research, not from additional shake-out traces.
- 2026-04-27: Trace-mode discipline confirmed — trace as if the OS were running per the vision. Act AS each associate, document every step (trigger / associate / inputs / outputs / gaps), write entities to OS via CLI throughout. NOT a bulk hydration pass. Each entity is created at the step in the timeline where the autonomous flow would create it.
- 2026-04-27: Slack ingestion is FOUNDATIONAL, not deferrable Phase F. Multiple Alliance Touchpoints are Slack-sourced (Capability Document thread, Apr 7-8 Retention Associate prep, Apr 21 mpdm packaging). Roadmap.md updated to move Slack from Phase F to early Phase B.
- 2026-04-27: Shared-context-update mechanism worked end-to-end as designed. Main session logs findings to os-learnings.md → bugfix session reads them on resume → no human-in-the-middle handoff. Result: 7 OS bugs fixed/in-progress in parallel during the trace, several of which directly unblocked the trace as it progressed (Bug #30 Meeting create; API 500-detail). Living `fork-prompts/os-bugfix-resume.md` is the operational discipline for future parallel-session work.
- 2026-04-27: v2 Alliance proposal generation pattern confirmed — `(Deal at PROPOSAL stage + recent Touchpoints + extracted intel + Playbook for stage + raw source content from prior versions + example proposals from Drive) → Artifact Generator → rendered Document`. Same mechanism as GR Little's DISCOVERY follow-up email; only the Playbook record changes per stage. Validates the Apr 23 breakthrough across two distinct stages now.
- 2026-04-27: Proposal rendering tooling chosen — HTML→PDF via puppeteer-core against system Chrome, with Handlebars templating. Rationale: HTML/CSS allows non-developers to edit the template, Chrome rendering is industry-best, no Chromium download needed. Template + renderer + assets live at `templates/proposal/` and `tools/`; pattern is documented in `skills/artifact-generator.md`. Reusable across all customers and all proposal stages.
- 2026-04-27: Cam's portfolio brevity is the standard — section-per-page rhythm (9 pages: Cover / Unlocking Revenue Capacity / Implementation Gap / Three-Phase Roadmap / Implementation Timeline / Roles & Commitment + Guardrails / Investment Summary + Next Steps / SaaS / Acceptance), ~80 words per opener, ~5 lines per phase. NO direct address to customer, NO v1/v2 framing, NO stat dumps. Earned through several rounds of "this looks like trash compared to the original" feedback during the styled-PDF iteration. Captured as 15 style guidelines in `skills/artifact-generator.md`.
- 2026-04-27: Proposal swoosh shape canonical form — SVG path with rounded TOP-LEFT corner only, sharp 90° bottom-left, flat top, bleeds RIGHT and BOTTOM. Path: `M 1000,100 L 1000,15 L 380,15 Q 290,15 290,55 L 290,100 Z`. Logo rendered WHITE on the swoosh via `filter: brightness(0) invert(1)` on the iris-source PNG.
- 2026-04-28: Trace-showcase HTML pattern adopts the Apr 24 GR Little v2 visual language verbatim and adapts the section-by-section content for the PROPOSAL stage. Five-section spine (artifact hero → temporal flow → extraction → mechanism → spine) is canonical. Vertical timeline (vs the GR Little 4-event horizontal grid) is the right shape for 8+ events. Source-pill variety (Meeting / Email+Doc / Slack channel / Slack DM / Slack MPDM / OS) is a strength to surface — shows the OS handles all touchpoint types uniformly. Mechanism table being honest about non-canonical journeys (Demo "skipped" for Alliance) reinforces that the model represents real customer paths, not idealized ones.
- 2026-04-28: v2 proposal PDF uploaded to Cam's proposals folder on Drive (`1k0_SYLdYtlM40y6W-ZAMV3Trr9hIq2Ph`). Sits next to v1 in Cam's portfolio. Drive link is wired into the showcase HTML hero panel as the "View the full proposal (PDF) →" CTA. Document entity `69efbdea4d65e2ca69b0dd80` in the OS could now be updated with `drive_file_id` + `drive_url` to keep the entity graph in sync with reality — pending; deferred to next OS-touching session.
- 2026-04-28: Phase A2 fully closed — both deliverables shipped, both shared with Cam + Kyle. Phase A complete after A1 (GR Little discovery) + A2 (Alliance proposal) — sufficient mechanism validation across two distinct stages. Next session opens Phase B (build to autonomous), starting with B1 foundation hardening (entity-resolution skill propagation to Email Classifier + Touchpoint Synthesizer is the most direct unblock since the kernel piece already shipped 2026-04-27).
- 2026-04-28: Slack file-broadcast quirk — `files_upload_v2` with `thread_ts` puts the file in the thread only; `chat_postMessage` with `reply_broadcast=True` puts the text at channel-top + thread but does NOT carry thread file attachments to the channel feed. To get a file visible at channel-top alongside a broadcast reply, upload the file twice (once to thread, once to channel-top). Operational note for future Slack shares.
- 2026-04-28: Fork-session burst #4 (waves 1-6, continuous session) cleared the load-bearing OS work. 9 fixes shipped, 320 unit tests, every fix verified live: Bug #23/#24 (bulk-delete operator filters + workflow counts), Bug #32 (preview ObjectId/retry — pre-existing latent), Bug #10 (reprocess primitive), Bug #9 (boundary coercion + auto_resolve flag on FieldDefinition), Bug #11/#20/#21/#28 (CLI papercuts + queue stats alias), always-fresh entity skill rendering, Bug #30 propagation via auto-emit partialFilterExpression. **The kernel is in genuinely solid shape now.** What's left in os-learnings.md is mostly customer-system domain work (skill updates for Bug #16/#17, Email.interaction → Touchpoint rename, etc. — these belong to the main session) plus one substantive OS feature (`--include-related` reverse relationships) and a handful of small papercuts. The next fork-session burst may be smaller in scope. The customer-system-vs-OS distinction matters: a "🔴 Open" row in os-learnings.md isn't necessarily kernel work.
- 2026-04-28: `auto_resolve: bool = False` flag on `FieldDefinition` enables LLM-friendly relationship resolution at the API boundary. Setting `auto_resolve=true` on (e.g.) `Email.company` means a POST/PUT with `{"company": {"name": "Acme"}}` will trigger `entity_resolve` against Company and auto-link if there's exactly one 1.0 candidate (else 400 with the candidate list). This is the cleaner alternative to updating the Email Classifier + Touchpoint Synthesizer skills to call resolve manually — the kernel boundary handles it transparently. Decision pending which entities get the flag and whether main session prefers the skill-update path or the auto_resolve path for Bug #16/#17 closeout.
- 2026-04-28 (Session 10): **LangSmith tracing is the foundation B1 milestone for vision §2 item 7.** Wired into async-deepagents harness (env vars + `RunnableConfig` metadata on `agent.ainvoke()`). Project `indemn-os-associates` (`19302981-aa9e-4869-9137-59a38d7646df`). Auto-tracing of LLM/tool/state-graph calls is sufficient because deepagents is LangGraph-based. **How to query traces is documented in CLAUDE.md "When working on X → Debugging an associate's behavior".** This single addition turned Session 9's "research-level skill-compliance gap" into a 5-minute root-cause investigation (the actual bug was a kernel CLI routing issue, not a skill problem). Earned its place immediately.
- 2026-04-28 (Session 10): **Path 3 chosen for evaluations architecture** — eventually the existing `evaluations` repo (port 8002, LangSmith-based) becomes a kernel adapter (`system_type: evaluation`, provider: `langsmith` or `indemn-evals`). Native OS evaluations as kernel primitives (Path 2) is too big a build right now. Standalone-service-only (Path 1) is too separate from the OS vision. Hybrid (Path 3) — OS gets `Run` + `eval` capability with the evaluations service as the LangSmith engine — is the right shape long-term. Customer-system uses LangSmith API directly + borrows rubric patterns from the evaluations repo without taking the dependency for now. Native OS evals design doc deferred to a separate fork-session.
- 2026-04-28 (Session 10): **EC v6 deployed** with new Step 0 ("the email entity in your context IS the email to classify — never create") + Hard Rules #8 (resolve errors are showstoppers, never fall through to create) and #9 (never call `indemn email create`). Content_hash `5bbfdd13ad26c8db…`. End-to-end verified on Diana@CKSpecialty's email (LangSmith trace `019dd5c1-3aab-7ea3-aa08-d007044570a1`) — clean classify + link to existing Company, no auto-create. Partial gaps remaining (Step 3 Contact resolve still skipped; multi-1.0 still picks one) — defer to v7 or eval coverage.
- 2026-04-28 (Session 10): **Kernel CLI bug fixed** — `indemn_os/main.py:202` had `_COLLECTION_LEVEL_CAPS = {"fetch_new"}`, missing `entity_resolve`. Caused `entity-resolve` calls to route through the entity-level path which list-and-loops with `?limit=1000` (API caps at 100) → HTTP 422. **This was Session 9's "skill compliance gap" root cause** — agent saw the tool error and fell through to create. Fixed via commit `db97694` (one-line + comment requiring sync with `kernel/capability/__init__.py`). Followon: `kernel/cli/app.py` has the same divergence (no `_COLLECTION_LEVEL` set at all) — filed for a separate fix.
- 2026-04-28 (Session 10): **Harness completion check tightened** — `agent_did_useful_work` now requires ≥1 *successful* mutating CLI call (was: any *attempted* mutating call OR any non-empty narrative content). Domain-agnostic — checks tool result success markers via `tool_call_id` matching. Per OS vision discussion (Craig + Claude): the harness reports observable facts about tool execution; detection of "agent didn't fulfill skill intent" lives in evals (Phase E) and observability dashboards on top of LangSmith — NOT in the harness. The original hypothesis ("check if watched entity transitioned") was too domain-aware. Commit `d914d76`.
- 2026-04-28 (Session 11): **DON'T inline associate skills into system prompts.** OS vision requires deepagents progressive disclosure to work properly. Inlining bypasses the mechanism instead of fixing it. My initial proposal (a `_load_associate_skills_inline` helper that builds a system_prompt extension) was explicitly rejected: "BAD! TERRIBLE DESIGN AND NOT ALIGNED WITH THE VISION". Reverted. The right fix is making the harness write skills in the structure deepagents expects + pass the right path.
- 2026-04-28 (Session 11): **EC v7 deployed** (content_hash `897d05babf0d3b83…`). Hard Rule #10 (Contact resolve mandatory) + #11 (Contact-first ordering). Strengthened Step 4 multi-1.0 ambiguity language with Diana@CKSpecialty as worked example. Multiple-1.0 across ANY combination of `matched_on` fields → needs_review (most-specific match is NOT a tiebreaker). **However, agent has not actually read v7 due to Bug #35 — see below.**
- 2026-04-28 (Session 11): **Bug #35 NEW — deepagents skill discovery broken in our LocalShellBackend setup.** Despite the harness writing `/workspace/{activity_id}/skills/email-classifier/SKILL.md` correctly + (per commit `5ec4e9f`) passing the LIBRARY directory path to `create_deep_agent(skills=[...])`, the system prompt continues to report "(No skills available yet. You can create skills in act-…/skills)". **Every single iteration of the EC skill (v3 → v7) has been theatre — agent never read any of it.** The improved behavior in Session 10 (linking to existing Company) and Session 11 (calling Contact resolve) came from the agent reading the auto-generated entity skills via `indemn skill get <Entity>`, NOT from the associate skill. Confirmed via direct system-prompt inspection in LangSmith trace `019dd602-ddb9-7f03-946c-fcee7413e11f`. Hypothesis space + diagnostic steps for Session 12 documented in `artifacts/2026-04-28-session-11-handoff.md` Section 3.
- 2026-04-28 (Session 11): **Document `69efbdea4d65e2ca69b0dd80` synced** with Drive metadata: `drive_file_id: 1pM3tYg6rHzG8RW6xU_titouiq_iddhIC`, `drive_url: https://drive.google.com/file/d/1pM3tYg6rHzG8RW6xU_titouiq_iddhIC/view`. Entity now matches reality (v2 PDF lives in Cam's Drive folder). Carry-over item from Session 10 closed.
- 2026-04-29: **Customer-system shared roadmap aligned with Kyle** — artifact `2026-04-28-shared-roadmap-with-kyle.md`, Drive doc `1VZMcoLY212RL5iyzekJ0pLONqfPE5IlF`. Short Kyle-facing distillation of the 4/28 sync. Five immediate deliverables (customer-snapshot view, voice-dump ingestion, Armadillo/Amenta/TFG end-to-end runs, proposal generator iteration from Alliance v2 baseline, playbook hydration from 1-month historical data). **Kyle is the guinea pig** — daily use starting end-of-this-week (target) or within 2 weeks; team adoption (Cam, George, Peter, Ganesha) is the objective after. Cam owns proposal close substance; we iterate the mechanism. Explicit deferreds: 12-20 sub-stages (emerges from playbook hydration), insurance-program data model (long-horizon foundation for multi-carrier OS — EventGuard template scaled to 100 programs), Stripe / AI-associate metering (parallel technical-product track), full historical hydration past 1 month (after team-use proves stability). Doc shared with Kyle as writer + Slack DM sent on 2026-04-29 with "sharing it with you here first" framing. Living doc — revisit once Kyle has daily-use behind him.
- 2026-04-29 (Session 12): **Hard Rule #1 inverted in EC** (v7 → v9 in DB, content_hash `9eef4959ae701614`). Old rule "Never auto-create a Company" replaced with "Resolve before create on Company" — symmetric to Hard Rule #2 for Contact. Rationale: the original conservatism was warranted before `entity_resolve` was reliable (pre-Bug-#34 / pre-Bug-#36); with both fixed, resolve-first IS the dedup defense and blanket prohibition was blocking legitimate new-prospect autonomous onboarding. Step 5 decision table updated so the 0/0 outcome auto-creates Company + Contact instead of stalling at `needs_review`. Verified live on Armadillo's first-contact email — autonomous flow created Armadillo Company + Matan Slagter Contact + David Wellner Contact. (Parallel session deployed v8 with the same intent earlier; this session deployed v9 layering in the worked example for Armadillo.)
- 2026-04-29 (Session 12): **Bug #36 root cause was deeper than first framing** — `**params` on the public adapter methods silently absorbed any unknown kwargs (`until`, `query`, `mailbox`, anything dropped without warning). Fix in `kernel/integration/adapters/google_workspace.py` (commit `477a98f`) plumbs `until` end-to-end and replaces `**params` with `**unknown_params` raising `AdapterValidationError`. The Outlook adapter has the same `**params` pattern and is being propagated in a follow-up. Operationally — every adapter contract should fail loud on unknown kwargs.
- 2026-04-29 (Session 12): **Bug #37 fix shape — opt-in tolerance via `_DomainQuery.to_list(skip_invalid=False)`.** Default strict (preserves migrations / audit code's loud failure on malformed rows); the user-facing list route opts in with structured logging that names the bad doc `_id`. Cleaner than per-API-route try/except sidecar shape. Lands at `kernel/entity/base.py` (commit `a5aa89f`); 2 bad Email rows identified in dev (`69ea548e…6e92` Oneleet, `69ea556f…7387` Linear Orbit) — data cleanup is customer-system domain work, not bundled.
- 2026-04-29 (Session 12): **Armadillo traced end-to-end through the OS as designed** — first new-prospect trace post-Hard-Rule-#1-inversion. Demonstrates the autonomous flow works: Email Classifier on a never-before-seen domain → resolve returns 0/0 → Company + Contact auto-created → email transitioned to `classified`. Touchpoint Synthesizer + Intelligence Extractor walked manually for the rest of the trace (Deal-creator associate doesn't exist yet; Employee `entity_resolve` not activated; both logged as gaps). 14 entities extracted by IE against the DISCOVERY Playbook for the Apr 28 call. Artifact (follow-up email + trace-showcase HTML) shipped to Kyle.
- 2026-04-29 (Session 12): **Slack file attachment > Drive link for HTML artifacts.** HTML uploaded to Drive renders as raw source in the Drive viewer (not as the rendered page). Slack file attachment lets the recipient download → open in browser → see the rendered version. Operational note for future Kyle-facing trace artifacts; we replaced the original Drive-link Slack DM with a Slack-file-upload version mid-flight today.
- 2026-04-29 (Session 13): **Roadmap restructured as Tangible Deliverables (TD-1 through TD-11)**, replacing the prior Phase A → F structure. Same vision, same work, different organization. Phases were fuzzy on what's tangibly shipped to whom; TDs make it explicit. TD-1/2/3 detailed at full fidelity (~170-280 lines each); TD-4 through TD-11 structurally aligned (~30-70 lines each, deeper detail filled in when each TD is approached for execution).
- 2026-04-29 (Session 13): **7-associate cascade architecture for TD-2.** Per Craig's principle (one associate per significantly-different trigger/entities/context/skill): EC + MC + SC + TS (gains Deal-creation atomic with Touchpoint creation) + IE + Proposal-Hydrator + Company-Enricher. Source-classifiers per source type (Email/Meeting/SlackMessage have fundamentally different data shapes); TS source-agnostic. Cascade depth 4-5; well within kernel's depth-10 circuit-breaker. Folding more responsibilities into existing associates would conflate triggers; splitting further would over-fragment shared context.
- 2026-04-29 (Session 13): **ReviewItem entity = universal escape valve for human review.** Per Craig: any associate that hits any issue creates a ReviewItem and continues. Never blocks. Reviewer role watches `ReviewItem created`. Reviewing IS training data (corrections feed back as test cases for the originating associate, per TD-9). Generic across all entity types. Source-classifier total-failure exception: source transitions to `needs_review` (terminal) AND creates ReviewItem; cascade halts there until reviewer resolves. Replaces the per-entity `flag_for_review` field anti-pattern.
- 2026-04-29 (Session 13): **Per-event Touchpoints — 1:1 with discrete source-events; threading is metadata, not a primary entity.** A Touchpoint is a discrete event in time. Every email, every reply 3 days later, every Slack message, every meeting → its own Touchpoint. No standalone Thread entity. Threading reconstructed via `Email.thread_id` / `SlackMessage.thread_ts` metadata + `thread_id` querying. Touchpoints are immutable snapshots (modulo Reviewer cleanup); customer constellation evolves because the SET of Touchpoints grows, not because individual Touchpoints mutate. UI presents threading as visual grouping (subtle indent/connector) — not data-model grouping.
- 2026-04-29 (Session 13): **Slack adapter design.** Direct Slack API (NOT agent-slack — that's third-party); all channels via Slack admin permission (Craig grants workspace-wide); DMs out of scope (team uses channels for customer-relevant chatter); per-message granularity (one SlackMessage per message, with `thread_ts` metadata); polling at 5min initially, Slack Events API webhook later post-TD-2; file attachments → Documents; adapter is "dumb" — no classification, no Company resolution; SlackClassifier (TD-2) handles classification.
- 2026-04-29 (Session 13): **Drive ingestion — pull all, classify lazily.** Drive becomes a central searchable space; Documents created with `company = null` by default; folder context preserved as `Document.source_folder_path` metadata for use as a hint at classification time, not auto-applied. Three lazy-classification paths: (a) at Touchpoint extraction by IE when a Document is referenced, (b) manually via UI in TD-3, (c) future workflow-driven (when documents created out of customer-assigned tasks). Documents that never get classified live in the central space; queryable by folder/content/type/date. Incremental fetch by `modifiedTime` watermark — same pattern as Email/Meeting/Slack.
- 2026-04-29 (Session 13): **Document entity is source-agnostic.** Same fields accommodate Drive + email-attachment + Slack-attachment + manual-upload origins. No source-specific top-level fields. Each adapter extracts whatever metadata its source provides through the existing generic Document fields. Maximum metadata extraction at creation time across all sources.
- 2026-04-29 (Session 13): **Manual Touchpoint entry uses the existing OS kernel-level Deployment pattern** — per-actor `default_assistant` associate (Deployment per UI surface: web Deployment for chat, voice Deployment for push-to-talk). New domain skill `log-touchpoint` added to each team member's assistant. The assistant collects Touchpoint fields via conversation, calls `indemn touchpoint create` via execute(). No special manual-entry infrastructure. Touchpoint cascades through IE → Proposal-Hydrator like any autonomous one (IE reads `Touchpoint.summary` directly when no source_entity_id).
- 2026-04-29 (Session 13): **TD-3 stack: React + Vite + shadcn matching Ganesh's customer-success repo conventions** (`https://github.com/ganesh-iyer/implementation-playbook`). Direct OS API access (no adapter pattern). Reuse existing chat-deepagents + voice-deepagents harnesses (refinements from async-deepagents skills-via-CLI refactor applied). WebSocket real-time updates aligned with `realtime.md`. Component-level architecture after auditing Ganesh's repo for conventions. Visual language matches the GR Little / Alliance trace-showcase HTMLs Cam + Kyle validated. Single-scrolling per-customer page with inline timeline expansion. Mobile responsive.
- 2026-04-29 (Session 13): **TD-3 page list — 7 pages.** `/` (personalized dashboard, role-aware widgets, assistant prominent), `/customers` (list), `/customers/<id>` (per-customer constellation — central artifact, 5-section spine matching trace-showcase), `/meetings` (list — addresses real team gap; today's tools are siloed), `/meetings/<id>` (per-Meeting detail with transcript player + smart notes; TD-7 enriches further), `/touchpoints/<id>` (deep-linkable), `/documents` (browse + central searchable space), `/documents/<id>` (content preview + Assign Company action + CLI-friendly identifier). ReviewItems surfaced contextually rather than dedicated `/reviews` page.
- 2026-04-29 (Session 13): **Personalized dashboard at `/` is role-aware, NOT a customer list.** Same primitive entity-rendering components, different default arrangements per role (Cam/George/Ganesha/Peter/Kyle each get different prominent widgets). Assistant integrated as a CORE element (proactive surfacing, quick-action chips, recent autonomous actions widget). Customer list lives at `/customers`.
- 2026-04-29 (Session 13): **Stage progression UI is descriptive, not prescriptive.** Per Craig: populated/unpopulated entities at a stage show as STATUS, not stage-advancement criteria. Stage transition criteria will be defined separately in TD-4 research after we mine historical patterns; UI updates to incorporate them when defined. "Advance to next stage" is a manual decision with no entity-completion gate.
- 2026-04-29 (Session 13): **TD-4 process — fully conversational design via Claude Code; no automation.** Three phases: (1) Research session — read every hydrated Touchpoint across every customer, identify patterns, determine WHAT stages actually exist (current 6 is hypothesis to test against data); (2) per-stage deep-dive defining `entry_signals`/`required_entities`/`expected_next_moves`/`artifact_intent`; (3) mostly-static refinement (no Playbook-Researcher associate; refinement happens conversationally over time). The 5 long-running open design Qs fold into this research, resolved as they surface.
- 2026-04-29 (Session 13): **Document-as-artifact for emails — RESOLVED. Email entity with `status: drafting`.** Drafted follow-up emails (produced by Artifact Generator in TD-5) live as Email entities in a new `drafting` state. Drafts queryable as Emails (consistent with the rest of the email lifecycle). PROPOSAL deck stays as Document (different mime_type, different entity). Resolves the long-running open question from Apr 24 design dialogue.
- 2026-04-29 (Session 13): **Artifact Generator — one associate, Playbook-driven, multi-deployment.** Single associate watches `Touchpoint:processed`; reads `Playbook[Deal.stage].artifact_intent` to decide WHAT to produce per stage. Stage-specific behavior comes from Playbook records, not associate identity. Deployed in three modes: async (auto-runs on Touchpoint:processed), realtime chat (Cam invokes "let's draft v3 of Alliance" in UI), realtime voice (push-to-talk). Same associate, multiple Deployments. Per the flexibility-of-deployment principle.
- 2026-04-29 (Session 13): **Flexibility-of-deployment principle (architectural).** Associates can be deployed async OR realtime (chat/voice). It's a deployment decision, not an architectural constraint. Same skill, same associate, different harness + Deployment per use case. Applies to Artifact Generator (TD-5/TD-8) prominently but generalizes to any associate where iterative human-in-loop work is valuable.
- 2026-04-29 (Session 13): **TD-9 evaluations — three-source structure** (build-time + retroactive + failure-driven) using LangSmith API directly for now. Path 3 kernel-adapter integration (existing evaluations repo becomes `system_type: evaluation` adapter) deferred to TD-11 timeframe when OS hardens for external customers. ReviewItem corrections automatically become test cases for originating associates (the "reviewing IS training data" flywheel made concrete).
- 2026-04-29 (Session 13): **TD-10 Commitment-Tracker associate** — both event + schedule triggers (event for immediacy, schedule for overdue sweeps); notifications via OS queue + Slack DM (no email); escalation chain at Commitment-level field with Role defaults (e.g., Walker's role's escalation = `[Peter, Ganesha, Kyle]`). Walker → Peter → Ganesha → Kyle pattern works end-to-end.
- 2026-04-29 (Session 13): **TD-11 detailed alignment deferred to `../product-vision/`** (the OS-level project). The customer-system project's roadmap doesn't carry the heavy OS-platform-readiness alignment work. Path 3 kernel-adapter integration likely lands here. Detailed work happens when Indemn-the-company is fully running on its own OS (TD-1 through TD-10 complete).
- 2026-04-29 (Session 13): **No more "open questions to resolve" sections in TD entries** — per Craig's "no open questions" principle. Architectural decisions are resolved in alignment conversations (like this one) BEFORE the TD entry gets detailed; TD entry captures resolved design with rationale, not unresolved Qs. Open design questions for the broader project still live in INDEX.md § Open Questions; per-TD architectural Qs get resolved before the TD ships its detailed section.
- 2026-04-30 (Session 14): **Bug #42 root cause is Gemini 2.5 Flash MALFORMED_FUNCTION_CALL on the deepagents `write_todos` schema, NOT a skill-content / agent-compliance issue.** Resolved by switching runtime defaults (async-deepagents-dev + chat-deepagents-dev + voice-deepagents-dev) to `gemini-3-flash-preview/global`. Per-actor `llm_config` overrides remain available as escape hatches. Future `gemini-3-flash-preview` GA promotion will retire the preview-tier note.
- 2026-04-30 (Session 14): **TD-1 Drive adapter built inline, not deferred.** Original roadmap framing ("Drive adapter exists; needs systematic ingestion wired") understated scope — adapter had only `_drive_service` helper. Per Craig's "we are implementing EVERYTHING to completion," built `fetch_documents()` end-to-end this session: 6 unit tests GREEN, 30-day backfill returned 1161 fetched / 493 created, Drive-Fetcher actor live. Sets the precedent for any roadmap-understated scope: build it in-session if the actual scope is achievable.
- 2026-04-30 (Session 14): **Slack adapter built per design doc** (commit `c87376d`). 9 unit tests GREEN. SlackMessage entity created. Slack Integration entity active. Bot token in AWS Secrets at `indemn/dev/integrations/slack-oauth`. Live fetch BLOCKED by Bug #45 (`resolve_integration` not finding the active Integration); next-session priority.
- 2026-04-30 (Session 14): **voice-deepagents harness MUST mirror chat-deepagents architecture** — deepagents `create_deep_agent` + harness_common (Interaction lifecycle, Attention with heartbeat, runtime registration, backend factory) + three-layer LLM config merge + LiveKit only handles audio I/O via DeepagentsLLM adapter. Per `docs/architecture/realtime.md` voice harness lifecycle (Interaction creation → Attention with `purpose=real_time_session` + heartbeat → build deepagents agent → events stream subprocess → process voice frames → close). v1 (commit `62f47f9`) violated this fundamentally; v2 rebuild required and is highest-architectural-fidelity priority for next session. Per-actor default_assistant pattern remains deferred (shared OS Assistant suffices for now).
- 2026-04-30 (Session 14): **LangSmith query gotcha — `order: "desc"` (lowercase) required.** `order: "DESC"` returns HTTP 422 silently and parses to 0 results in scripts that read the `runs` field. The fork session's "0 traces visible" Bug #41 verification symptom was this query bug. CLAUDE.md § 8 LangSmith debugging recipe should add a one-liner.
- 2026-04-30 (Session 14): **Three runtime layer flips matter.** `async-deepagents-dev` + `chat-deepagents-dev` + new `voice-deepagents-dev` runtimes all on `gemini-3-flash-preview/global`. Bug #42 fix cascades to all 23+ associates without per-actor llm_config overrides. Per-actor overrides are escape hatches, not the default mechanism.

## Open Questions

- When to do full 30-day meeting backfill (Meet API conference records expire after 30 days)
- How Granola meeting data integrates (some meetings use Granola instead of Google's native transcription)
- How to handle meetings where nobody enabled recording/transcription (process issue)
- SuccessPhase data — what phases look like for each deal type
- Staleness detection thresholds — what's "stale" for each entity type
- UI rendering of nested dict lists (participants field shows `[object Object]`)
- Interaction entity: how Slack messages become Interactions without noise, is summary field sufficient, effort tracking
- Operation entity: field categories are naive — need real customer data to inform structure
- Company profile enrichment: what structured fields capture "understanding their business"
- LineOfBusiness entity: timing TBD, will exist eventually
- How intelligence entities (Task, Decision, Signal, Commitment) connect to new entities
- OS wiring: how watches and associates automate the discovery-to-proposal flow
- CLI: `email list` times out with large datasets because body field (full HTML) makes responses huge. Need `--fields` or `--exclude-fields` option on list commands to select/exclude fields. Email-specific but should be a general CLI capability.
- Harness: verify entity actually transitioned before marking message complete (defense-in-depth for associate failures)
- UI rendering of nested dict lists (participants field shows `[object Object]`)
- 2026-04-27: Does the shared-context-update mechanism (CLAUDE.md + INDEX.md + vision.md + roadmap.md + os-learnings.md) scale to N parallel sessions? Probably not as-is. Refinement is its own work item that surfaces during Phase B as parallel sessions multiply. May need: leveraging the existing session-manager system at `systems/session-manager/`, a more structured handoff protocol, or agent-based reconciliation. Meta-thread that runs throughout Phase B.
- 2026-04-27: Where exactly do the 12 sub-stages with archetype multi-select live structurally — Stage entity gets a `parent_stage` field for hierarchy + a multi-select archetype field on Deal? Or sub-stages as separate Stage records? Or `qualification_state` as a separate field on Deal? Resolved during B3 stage research, when real customer journeys inform what the right shape is.
- 2026-04-27: How does the OS preserve transcripts/files past the source's retention window? Apollo/Granola/Slack 30-day retention loses ground-truth content that the OS could otherwise re-process later. Should we copy transcripts into Document at ingestion time? When? With what rules (full transcript vs summary; retention policy; storage cost)?
- 2026-04-27: How is "scope" determined for Touchpoints when participants aren't yet Contacts (chicken-and-egg with Touchpoint Synthesizer creating both in same cascade)? Default to external when participants are unknown then revise? Or contact-resolve before Touchpoint create?
